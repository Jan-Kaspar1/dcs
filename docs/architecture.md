# Architecture decisions

## Bootstrap baseline

The repository begins as a Rust workspace with a `dcs-core` library reserved for shared contracts. No controller or plant-model API has been chosen yet. Python standard-library tooling coordinates development; it is not the plant runtime.

The project vision in `AGENTS.md` is the source for product decisions. All initial I/O is simulated, and development excludes physical plant equipment and live deployment.

## Recording decisions

When choosing a shared contract, module boundary, persistence format, or runtime behavior, the planner records the problem, chosen approach, alternatives, consequences, and affected tickets here. Changes are committed and pass the same CI gate as implementation. Accepted decisions must distinguish implemented behavior from proposed behavior.

## Decisions

### 1. Workspace crate layout

- **Status:** Accepted and implemented: `dcs-core`, `dcs-model`, `dcs-runtime`, `dcs-sim`, and `dcs-blocks` all exist, joined by `dcs-monitor` (decision 8's transport, #20), `dcs-demo` (the M1 example loop, #12), `dcs-sim-net` (the remote simulated driver and shared plant server, #31 and #68), `dcs-assembly` (decision 6's model-driven wiring, #19, and the device-kind driver registry, #47), `dcs-controller` (decision 9's paced binary with active/standby modes, #19, #48, and #32), and `dcs-plant` (the standalone shared-plant server, #81).
- **Problem:** The system needs crate boundaries so that shared contracts, the plant model, the executor, simulated I/O, and reusable control components can evolve independently with an explicit dependency direction, instead of growing as one tangled crate.
- **Chosen approach:** One Cargo workspace. `dcs-core` holds shared contracts (signal types, quality, component and I/O traits). Planned members: `dcs-model` (plant model types and serialization), `dcs-runtime` (cyclic executor and component model), `dcs-sim` (simulated I/O backend), and `dcs-blocks` (reusable control components). Dependencies point inward: `dcs-model`, `dcs-runtime`, `dcs-sim`, and `dcs-blocks` may depend on `dcs-core`; `dcs-core` depends on none of them.
- **Alternatives considered:** A single monolithic crate (simpler, but couples contract stability to every feature); one crate per device or per component (fragments the workspace and multiplies publishing/versioning overhead).
- **Consequences:** Shared contracts change in one place and downstream crates break visibly at compile time. Each planned crate must be added as a workspace member before use. Component and test code can depend only on the layers it needs, keeping builds incremental.
- **Affected tickets:** #4 (this record); the planned M1 tickets that create `dcs-model`, `dcs-runtime`, `dcs-sim`, and `dcs-blocks`.

### 2. Signal representation

- **Status:** Accepted and implemented in `dcs-core` (issue #5): `Sample { value: Value, quality: Quality, tick: Tick }` with `Value` = `Bool`/`Int`/`Float`, `Quality` = `Good`/`Uncertain`/`Bad` carrying a `QualityReason`, and `Tick` a logical `u64` counter.
- **Problem:** Control logic and the monitoring UI need one uniform way to represent a process value that may be good, bad, stale, or substituted, and to know when it was sampled — bare scalars cannot express this.
- **Chosen approach:** A signal is a typed value plus a quality flag plus a timestamp: conceptually `Signal<T> { value: T, quality: Quality, timestamp: Tick }`, defined in `dcs-core`. `Quality` starts as a small enum (good, bad, uncertain/substituted) and may grow. The timestamp is a virtual tick count (see decision 4), not wall-clock time.
- **Alternatives considered:** Bare typed values with no quality (loses diagnostics the vision requires); OPC UA-style status codes and source/server timestamps (broad but heavyweight for a first contract); bespoke per-signal structs (no uniform handling in the executor or UI).
- **Consequences:** Every logical I/O point carries quality, so components must define behavior for non-good inputs and the executor can propagate bad quality to outputs. Timestamps are comparable only within a run's tick domain, which keeps tests deterministic.
- **Affected tickets:** #4; the M1 ticket implementing `dcs-core` signal contracts, and every later component and driver ticket.

### 3. Plant model serialization format

- **Status:** Accepted and implemented in `dcs-model` (#7): JSON documents carry a top-level `version` accepted only as `MODEL_VERSION` (currently 1), validation follows parsing, and optional fields extend the schema without a version bump (`Signal::unit`, `Signal::description`, `Signal::group` — display metadata grouping a signal under a plant area or unit for the monitoring page, #86). The derived `SignalIndex` (#23) and the `dcs-model` CLI (#36) build on it.
- **Problem:** The plant model is the single contract shared by engineering data, controllers, and the monitoring UI. It needs a serde-based on-disk format that is versioned, diffable, and readable by tooling outside the Rust workspace.
- **Chosen approach:** JSON via `serde`/`serde_json`. Every document carries an explicit top-level `version` field; the loader accepts only known versions and migration happens on load. The plant model is treated as an engineered artifact, typically produced and consumed by tools rather than hand-edited.
- **Alternatives considered:** TOML — friendly for hand-editing and comments, but awkward for the deeply nested device/signal/mapping lists the model needs and less natural for a web-based monitoring UI. RON — Rust-native with comments, but essentially no non-Rust tooling, which undermines the model's role as a cross-language contract.
- **Consequences:** Any tool that can parse JSON can read or generate the model, including the future monitoring UI. Lack of comments means explanatory text lives in model fields, not alongside the data. All model documents must state a version, so schema changes are explicit rather than silently ambiguous.
- **Affected tickets:** #4; the M1 ticket implementing `dcs-model`, and every later ticket that loads or emits plant configuration.

### 4. Execution model

- **Status:** Accepted and implemented in `dcs-runtime` (#9): `Executor::scan` advances a virtual `Tick` and runs apply-commands → read inputs → step components → write outputs in order; nothing reads a wall clock.
- **Problem:** The controller must execute reproducibly: the same plant model and input sequence must produce the same outputs on any host, and tests must be able to step through runs without real-time waits.
- **Chosen approach:** A deterministic fixed-step scan. Each cycle reads all inputs, steps components in a defined order, then writes all outputs. Time is a virtual tick counter advanced by the executor; components never read a wall clock. Pacing a run to real time, when needed, is the outer driver's concern, not the components'.
- **Alternatives considered:** Event-driven or `async`-task execution (flexible but ordering and reproducibility become scheduling-dependent); wall-clock timers inside components (nondeterministic, untestable); variable-step execution (breaks fixed-scan assumptions familiar from PLC runtimes).
- **Consequences:** Runs are deterministic and replayable; `dcs-sim` and tests can advance the executor one tick at a time. Components must express delays and periods in ticks. Real-time jitter is isolated to the pacing layer and cannot leak into control logic.
- **Affected tickets:** #4; the M1 ticket implementing `dcs-runtime`, and all component and simulation tickets.

### 5. I/O abstraction

- **Status:** Accepted. The contract side is implemented in `dcs-core` (issue #6): `IoDriver` provides untyped `read`/`write` over `PointId` returning `Result<_, IoError>` (unknown point, disconnected, timeout, type mismatch — each carrying the point identity), and `Input<T>`/`Output<T>` handles give components typed logical I/O with strict, never-coercing value matching. `SimDriver` implements the contract (#8); the TCP remote driver in `dcs-sim-net` implements it too (#31, decision 13).
- **Problem:** Per `AGENTS.md`, control logic must be hardware-independent: the same component must work whether a point is backed by local I/O, EtherCAT, Ethernet, or simulation, without code changes.
- **Chosen approach:** Components declare typed logical I/O points — a name, a signal type (decision 2), and a direction. Binding a logical point to a physical device channel lives in the plant model (decision 3); resolving that binding and talking to field hardware lives in drivers beneath `dcs-runtime`. Component code never names a device, bus, or address.
- **Alternatives considered:** Injecting driver handles into components (couples logic to a driver API); per-component mapping config (duplicates mapping outside the plant model, breaking the single-contract goal); a global string-keyed I/O registry (loses compile-time typing).
- **Consequences:** Components are portable and fully testable against `dcs-sim`. Rewiring hardware is a plant-model edit, not a code change. Mapping errors surface at model load or driver initialization rather than mid-scan.
- **Affected tickets:** #4; the M1 tickets implementing `dcs-core` I/O traits, `dcs-model` mapping, `dcs-runtime` drivers, and `dcs-sim`.

### 6. Model-driven assembly

- **Status:** Accepted and implemented (#19): `dcs-assembly` resolves a validated `PlantModel` into a `ChannelMap`, a `PointMap`, and per-instance port bindings — synthesizing internal point pairs for port-to-port links — and `assemble` constructs every component through an explicit `ComponentRegistry` keyed on `kind`, with unknown kinds, unbound ports, and mapping mismatches failing before any scan. `dcs-demo` retains its own fixed-registry assembly as the M1 example (#12). The device-kind factory registry is implemented (#47): `DriverRegistry::standard` resolves the `sim*` prefix to local simulated channels and the exact `sim-tcp` kind to `RemoteDriver`, `resolve_drivers` builds each declared device through its kind's factory, and `DriverPlan::build` finishes the driver side as a `FanoutDriver` presenting one `IoDriver` surface over the backends.
- **Problem:** The plant model declares component instances by opaque `kind` string plus parameters and port wiring, while the executor takes constructed components and a resolved point map. Something must turn a validated model into a configured driver and executor — and that translation has to live in a crate whose dependency direction respects decision 1.
- **Chosen approach:** A new workspace crate `dcs-assembly`, depending on `dcs-core`, `dcs-model`, `dcs-runtime`, and `dcs-sim`. It provides a component registry mapping each `ComponentInstance.kind` string to a constructor that receives the instance's parameters and resolved port-to-point bindings and returns `Box<dyn Component>`; reusable components from `dcs-blocks` register their kinds there. A builder takes a validated `PlantModel`, resolves devices into a driver-side `ChannelMap` and `SimDriver`, resolves `io_points` into the executor's `PointMap` (synthesizing internal points for port-to-port connections), constructs each component through the registry, and returns the configured executor and driver. Registration is explicit: an unknown kind is a build error naming the component instance, raised before any scan runs.
- **Alternatives considered:** Construction inside `dcs-runtime` (couples the deterministic executor to the model schema); inside `dcs-model` (inverts the dependency direction — the serialization contract would depend on the runtime); global self-registration via link-time tricks like `inventory`/`linkme` (component kinds silently missing when a translation unit is not linked); assembly code in each binary (duplicates the model-to-runtime logic across binaries and tests).
- **Consequences:** Adding a device or component kind means registering a constructor — instantiation, wiring checks, and diagnostics come from the shared layers instead of per-layer engineering. Model instantiation errors surface at build time, before the first scan. `dcs-model` stays serialization-only and `dcs-runtime` stays model-agnostic, so both remain independently testable.
- **Affected tickets:** #18 (this record); the M2 tickets creating `dcs-assembly`, the component registry, and `dcs-blocks` components that register with it.

### 7. Operator command path

- **Status:** Accepted and implemented (#20): `Command`/`CommandReceipt`/`CommandOutcome`/`CommandError` live in `dcs-core`; `Executor::submit_command` validates against the point map and queues, the scan head applies queued commands before the input read, and the receipt log is readable via `Executor::receipts`. Commands target `PointId`s (`Command::WriteValue`, plus the `ForcePoint`/`UnforcePoint` pair #84 adds under decision 21) and component instances (`Command::SetParameter`, #82, decision 20); writable targets are model-declared under #49 (decision 18).
- **Problem:** The monitoring and control UI must change a run's behavior — setpoints, mode switches, manual output overrides — through the same unified contract, and every attempted change needs an explicit accept/reject answer. The deterministic scan must define exactly when a command takes effect, or command ordering becomes timing-dependent and runs stop being reproducible.
- **Chosen approach:** A serde `Command`/`Receipt` pair defined in `dcs-core`, beside `TelemetrySnapshot`, so any transport or UI needs only the shared contracts. A `Command` names its target (a component instance or a point), an operation, and parameters. Commands are applied at one documented point: at the scan boundary, before the input-read phase of the next scan — never mid-step — so a queued command's effect begins at a known tick and the read → step → write order is preserved. Every command produces exactly one `Receipt`: accepted, reporting the tick it applied at; or rejected with a structured reason (unknown target, invalid or mistyped value, operation not permitted in the current state). Rejections are also visible through telemetry diagnostics so the UI can display them.
- **Alternatives considered:** Applying commands the moment they arrive, mid-scan (nondeterministic — the effect depends on where in the cycle the message lands); commands as raw driver writes bypassing components (loses component validation, scoping, and auditability); fire-and-forget commands with no receipt (the UI cannot distinguish failure from delay).
- **Consequences:** Command handling is deterministic and replayable: same model, same input sequence, same commands at the same ticks produce the same run. Targets opt into the operations they accept; everything else rejects with a reason rather than failing silently. The receipt contract gives the UI a uniform answer surface for every operator action.
- **Affected tickets:** #18 (this record); the M2 tickets defining `Command`/`Receipt` in `dcs-core` and wiring command application into the executor boundary; the monitoring transport ticket carrying commands and receipts.

### 8. Monitoring transport

- **Status:** Accepted and implemented in `dcs-monitor` (#20): a synchronous `tiny_http` server holding the executor behind one mutex serves `GET /snapshot`, `GET /receipts`, `POST /command`, and `POST /scan`, with a matching `MonitorClient`. The signal-index endpoint is served as `GET /signals` (#34), bounded history and the journal as `GET /history` and `GET /journal` (#35, decision 17), and the live page at `GET /` (#34, #51); `dcs-controller` serves the surface alongside its paced loop (#48). `dcs-ctl` is the operator command-line tool over the same surface (#153): each subcommand wraps one `MonitorClient` call — reads printing the served payloads, commands printing the receipt or the named `SwitchError` — adding no endpoints and no wire types.
- **Problem:** The monitoring and control UI needs a wire protocol to fetch telemetry snapshots and signal metadata and to submit commands with receipts. The choice should serve the future browser-based UI directly rather than through a gateway, without pulling a heavy stack into the controller.
- **Chosen approach:** HTTP with JSON bodies on the controller process: GET endpoints return the `TelemetrySnapshot` and the model's signal index; a POST endpoint accepts a `Command` and answers with its `Receipt`. Rationale: JSON is already the contract's serialization (decision 3), an embedded HTTP server needs no async runtime or external service, and browser `fetch` plus every scripting tool can consume it. Snapshots report state, not history, so simple polling matches their semantics; if faster updates are needed later, server-sent events or a WebSocket can be layered on without changing the contract types.
- **Alternatives considered:** Newline-delimited JSON over raw TCP (simple and streamable, but browsers cannot open raw TCP — the UI would need a proxy, undermining the direct-access goal); WebSocket as the first transport (native push and browser-friendly, but adds framing and async machinery for a payload that is pull-shaped today); an industrial protocol such as OPC UA (rich semantics, but a heavy dependency premature for a first monitoring path).
- **Consequences:** The first UI and all tests need only HTTP and JSON; wire types are the same serde contracts the runtime already produces. Transport stays replaceable — the contracts live in `dcs-core` and the server only adapts them — so polling cadence and any later push upgrade are deployment choices, not contract changes.
- **Affected tickets:** #18 (this record); the M2 ticket adding the monitoring server to the controller process; future UI tickets.

### 9. Controller process

- **Status:** Accepted and implemented (#19, #48): `dcs-controller <model.json>` loads and validates the model, builds the `SimDriver` and `Executor` through `dcs-assembly` with the `dcs-blocks` registry, paces scans to wall-clock via `--scan-ms`, and — with `--listen` — serves the monitor's endpoints through `Monitor::bind_paced`, refusing `POST /scan` with `409` while the wall clock owns the schedule. `--driven` (#64) is the alternative for an externally paced run: the monitor serves unpaced and each `POST /scan` carries the scan cycle's wiring — a tracking standby's checkpoint pull and the field-owning peer's plant step — inside the request's boundary. `--check` (#160) is the engineering compile-check — chosen as a flag on the existing invocation rather than a `check` subcommand, so the model path stays the single positional argument: it loads, validates, and assembles the model through the standard driver and component registries, prints a deterministic summary of what assembled (devices and components by kind, declared and served points), and exits without a scan running or a listener binding; run-mode options are rejected rather than ignored. Docker packaging exists (#39). `dcs-demo` (#12) remains the fixed-registry M1 example.
- **Problem:** The stack needs a runnable deliverable: a process that loads a plant model, assembles its driver and executor, paces scans to wall-clock time for real operation, and exposes monitoring — without letting wall-clock concerns leak into components, which decision 4 forbids.
- **Chosen approach:** A thin `dcs-controller` binary crate taking a model path (e.g. `dcs-controller <model.json>` plus a scan-period option). It loads and validates the model through `dcs-model`, builds the driver and executor through `dcs-assembly` (decision 6), then runs a loop that paces `Executor::scan` to wall-clock and applies pending commands at the scan boundary (decision 7). The monitoring HTTP endpoints (decision 8) are served alongside the loop, sharing the executor behind the monitor's mutex (#48): paced scans run through `Monitor::paced_scan`, and `POST /scan` is refused (`409`) while pacing because the wall clock owns the scan schedule — externally requested scans would inject ticks no schedule accounts for. The binary holds no control logic and no component code: pacing, transport, and process lifecycle live in this outer shell; everything inside the executor remains virtual ticks.
- **Alternatives considered:** Pacing inside `Executor::run` (leaks wall-clock into the deterministic core and makes runs non-reproducible); per-component timers (rejected with decision 4); control logic compiled into the binary (defeats model-driven assembly — the plant would be code, not a model).
- **Consequences:** The determinism boundary is explicit: inside the executor everything is tick-domain and reproducible; outside it the process paces, serves, and can be stopped, updated, or replaced. The binary is the unit that redundancy (decision 10) later checkpoints and swaps. Tests keep using the unpaced executor, so pacing code stays thin and separately exercisable.
- **Affected tickets:** #18 (this record); the M2 ticket creating the `dcs-controller` binary; the M3 tickets that wrap it in peer management.

### 10. Redundancy direction

- **Status:** Proposed — direction only, recorded as the basis of milestone M3. The enabler it relies on is implemented: the executor's virtual-tick determinism (#9) makes transferred state meaningful. The state-capture half is now also implemented (#21): `dcs-core` carries the `StateMap`/`StateError` contract and driver hooks on `IoDriver`, components carry `capture_state`/`restore_state`, `SimDriver` transfers the simulated field state, and `Executor::checkpoint`/`restore` move a run between executor instances. Peer management and I/O-ownership arbitration are implemented under #46 (decision 15) and proven end-to-end under #64 — the scripted two-process hot swap over the shared simulated plant.
- **Problem:** Per `AGENTS.md`, a redundant controller instance on another machine must take over without interrupting the process: the active controller keeps running while the standby is updated, replaced, or assumes control. This needs a recorded direction before M3 tickets decompose it.
- **Chosen approach (proposed):** Active/standby peers running the same plant model. The standby periodically receives a deterministic checkpoint — executor tick, component state, scan image, and driver-side state — in a serde-serializable form like the rest of the contract, and restores it so that on switchover its next scan produces outputs identical to what the active would have produced (bumpless switchover). Determinism (decision 4) is what makes this work: same model plus same state plus same inputs gives the same outputs on any host. Components and drivers gain checkpoint/restore hooks; a component that cannot checkpoint is reported when the model is built, not at failover. Switchover is a peer-level decision that also moves the field-facing identity — monitoring endpoint and I/O write ownership — to the new active.
- **Alternatives considered:** Restart-based redeploy (interrupts the process — the vision rules it out); lockstep parallel execution of both controllers (doubles I/O ownership questions and adds arbitration complexity for no first-version benefit); stateless takeover replayed from an input journal (requires recording every input and unbounded replay to reconstruct state).
- **Consequences:** Checkpoint/restore becomes a contract on components and drivers, adding hooks to `dcs-core`, `dcs-runtime`, and `dcs-sim` in M3 tickets. I/O ownership transfer — which peer may write the field — needs defined arbitration, initially simulated. The monitoring path (decisions 7 and 8) must follow whichever peer is active, so the UI treats the pair as one logical controller.
- **Affected tickets:** #18 (this record); all M3 "Redundant hot-swap controllers" tickets.

### 11. Checkpoint wire format and versioning

- **Status:** Accepted and implemented (#21): `dcs-runtime::Checkpoint` is a serde type carrying `tick`, `components` (a `BTreeMap` of `StateMap` keyed by component name), `driver` (an `Option<StateMap>`), `outputs` (the scan image's `Out` samples), and `internal` (image-carried `In` samples); `dcs-core` supplies the `StateMap`/`StateError` vocabulary. The deliberate gap this paragraph once recorded — no `version` field, compatibility enforced structurally rather than negotiated — is closed under #66 (decision 27): the checkpoint now carries `format_version` and `model_fingerprint`, and the negotiation runs ahead of the structural checks described here.
- **Problem:** M3 moves a run between controller instances. The checkpoint needs a wire representation consistent with the rest of the contract, and a standby must reject a checkpoint produced by a different model, component set, or build — restoring one silently would leave the standby diverged while looking converged.
- **Chosen approach (implemented):** JSON via serde, like every other contract type (decisions 3, 8). Mismatch rejection is structural and layered rather than version-numbered. Malformed JSON or schema drift fails at deserialization. The checkpoint's component set must equal the standby's registered set exactly — extra names fail `RestoreError::UnknownComponent`, omissions `MissingComponent`. Each element then strictly validates its own `StateMap`: `require_*`/`optional_*` accessors yield `MissingField`/`IncompatibleField`, `ensure_known_fields` rejects fields the element never captured, and out-of-domain values yield `InvalidValue` — every error naming the element and field. Restore is all-or-nothing: any failure returns `RestoreError` and yields no partially restored executor, and the supplied driver validates its whole section before applying any of it. No top-level `version` is carried because a checkpoint is transient peer traffic between same-build instances sharing one model; if checkpoints later cross builds or persist, a version field and migration enter then, mirroring decision 3.
- **Alternatives considered:** An explicit `version` field with on-load migration like the model document (premature — version negotiation would only mask the build skew the strict field checks already catch); an opaque binary encoding (efficient but uninspectable and inconsistent with the JSON contract); lenient restore that ignores unknown fields (silently drops state a newer peer captured — exactly the quiet divergence the design rejects).
- **Consequences:** A restore answers "equivalent run or named failure" — never a partial one. The strictness cuts both ways: a standby built from a different model revision, or components whose captured fields drift, reject rather than degrade, so peer compatibility is same-build, same-model by construction. Each component's `StateMap` field vocabulary is de facto wire contract and must deploy to both peers together.
- **Affected tickets:** #21 (implemented); #32 consumes the format; #46 relies on its tick alignment.

### 12. Peer transport

- **Status:** Implemented (#32). `GET /checkpoint` on the active's monitor serves the serde `Checkpoint`; the standby pulls it — `MonitorClient::checkpoint` — once per scan cycle and applies it in place at its own scan boundary. The pull direction is the implemented one, as proposed below: the standby controls its own resync cadence and an unreachable active is a named, recoverable `Degraded` state rather than corrupted state.
- **Problem:** Checkpoints must move from the active controller to the standby — and later, role and promotion traffic with them. The choice is between reusing the monitoring HTTP+JSON transport and standing up a dedicated peer protocol.
- **Chosen approach:** Reuse the decision-8 transport. `Checkpoint` is already a serde JSON contract and `dcs-monitor` already serializes executor access behind one mutex, so a peer endpoint on the same server carries checkpoints — `GET /checkpoint` for standby pull (the natural shape: the standby controls its own resync cadence and survives active restarts by retrying); `POST /checkpoint` for active push was the rejected direction. Peer traffic and UI traffic share one listener, one serialization discipline, and one executor lock, so a served checkpoint is always a consistent between-scans capture — the active paces its own scans through `Monitor::paced_scan` under that lock — and a standby applies what it receives at its own scan boundary.
- **Alternatives considered:** A dedicated peer protocol on raw TCP (a second wire stack and framing layer for one message type — no first-version benefit); the remote driver's plant protocol implemented in `dcs-sim-net` under #31 (wrong direction — that is controller-to-field, not controller-to-controller); a brokered message bus (an external service M3 does not otherwise need).
- **Consequences:** One transport implementation serves UI and peers; the monitor's serial, one-request-at-a-time handling keeps checkpoint capture and application points deterministic. Under the implemented pull model an unreachable active is a named transfer failure, not corrupted state. Checkpoint cadence is a deployment choice: the payloads are pull-shaped already, so no push machinery is required.
- **Affected tickets:** #33 (this record); #32 implemented it; decision 8 and #20 supply the transport.

### 13. Standby field observation

- **Status:** Implemented. The contract half landed in #21: `IoDriver::capture_state` defaults to `None` and `SimDriver` captures and restores its full simulated field state — point samples, injected faults, process-element accumulators, driver tick. The shared-field half landed in #31: `dcs-sim-net`'s `PlantServer` serves one `SimDriver` plant as a documented line-delimited JSON protocol over TCP (read, write, explicit step, fault injection), and `RemoteDriver` implements `IoDriver` against it — the field-observing kind, `capture_state` left at its `None` default, with server loss surfacing as `Disconnected`/`Timeout` and point failures carried as their `IoError`s.
- **Problem:** A standby must run scans against field values, but "the field" differs per driver kind. A standby-local `SimDriver` holds the process state inside itself and starts at initial values unless state is transferred; a real fieldbus driver holds no authoritative state at all — the plant is the state, observed through its own channels.
- **Chosen approach:** Per driver kind, expressed in the checkpoint's `Option<StateMap>` driver section (decision 11). A standby-local `SimDriver` restores the captured section and thereafter steps its own copy of the simulated process. A field-observing driver — real hardware, or the remote simulated driver implemented under #31 — leaves `capture_state` unimplemented, the checkpoint carries no driver section, and restore skips it: the standby reads the actual shared process through its own connection. For M3's two-process demonstration the remote simulated driver is the field-observing kind: both peers attach to one shared simulated plant over TCP (implemented in `dcs-sim-net`, #31), so the standby observes what the active's writes actually did, proving the abstraction boundary redundancy depends on.
- **Alternatives considered:** Always restoring driver state (incoherent for field-observing drivers — they hold no state to restore); a standby scanning without any field view until promoted (cannot track the run or expose divergence before switchover); both peers writing the field continuously (violates single-writer ownership — see decision 15's gate).
- **Consequences:** Driver state capture is a per-driver-kind contract: checkpoint consumers must tolerate a missing driver section, and driver implementors decide which kind they are. The two standby modes differ in fidelity — a standby-local `SimDriver` diverges if it misses a checkpoint, while a shared-field standby tracks continuously — and #32's tests (`dcs-controller/tests/standby.rs`) exercise both: the local-sim standby resynchronizes its private plant from the driver section, the remote standby tracks the shared plant and is output-quiescent behind `dcs_runtime::WriteGate`.
- **Affected tickets:** #21 (implemented contract); #31 (implemented shared-field driver); #32 implemented the two standby modes.

### 14. Internal and operator points

- **Status:** Implemented (#30): channel-less internal points are first-class model declarations, served from the executor's scan image. `Command::WriteValue` applies at the documented scan boundary (#20), restricted by the model-declared `writable` flag (#49, decision 18). `dcs-demo`'s own `assemble` keeps the precursor mechanism — port-to-port carriers as `Out`/`In` pairs on a reserved simulated device joined by a driver `Loopback` (#12).
- **Problem:** Two point populations are not field I/O: operator values — setpoints and mode switches that live in the controller as command targets — and port-to-port carriers, the component-to-component signals the model wires without a channel. Both need a model representation consistent with the single-contract goal, observable in telemetry, and checkpointed like field points.
- **Chosen approach:** An `io_point` may declare no `channel`; it must then carry an `initial` value of its declared `value_type` (a channel-bound point must not declare one). In `dcs-runtime` the point map records `internal: Option<Value>` per point: the image seeds internal points at wiring, an internal `In` point holds its initial until a command writes it through the receipt-answered command path, and an internal `Out` point records component writes for monitoring — the driver is never touched. `PointMap::with_internal_link` routes an internal `Out` sample onto an internal `In` point at each scan's input phase — the image-carried equivalent of a field loopback, delivering one scan later. In `dcs-assembly`, channel-less points join the point map without a channel binding; port-to-port connections synthesize a linked internal pair, and a point-to-point connection between declared internal points becomes a link — a mixed field/internal pair is `MixedPointLink`. Direction and value-kind checks apply to internal points exactly as to field points, and malformed declarations are named errors in validation (`MissingInitial`, `InitialKindMismatch`, `FieldInitial`) and assembly (`InvalidInternalPoint`). The `writable` flag (#49) then narrows the command surface to model-declared points, making operator values an explicit, audited subset rather than "any known point".
- **Alternatives considered:** Commands addressed to component parameters directly (parameter maps are construction-time input, not scan-time state — a point target keeps the command path uniform); port-to-port wiring as invisible executor plumbing (the carrier would be absent from the point map, telemetry, and checkpoints — an unobservable signal); keeping the demo's synthesized-device convention as the permanent answer (works, but every model consumer must rediscover it — the schema should carry the concept).
- **Consequences:** Internal points make operator values and inter-component signals first-class citizens of the one model: visible in `TelemetrySnapshot`, writable through the receipt-answered command path, and carried in checkpoints — the `outputs` section covers internal `Out` samples and a new `internal` section carries internal `In` samples, so a commanded setpoint survives switchover rather than reverting to its declared initial. The `initial` and `channel` fields serialize as optional, so documents written before internal points existed still load under the same model version.
- **Affected tickets:** #12 and #20 implemented the precursor mechanics; #30 implemented the model-level contract; #49 implemented the writable flag; #33 (this record).

### 15. Switchover semantics

- **Status:** Implemented (#46). The role contract is `dcs-core`'s serde `Role` (`active`, `standby`, `promoting`, `demoting`), `StandbySync`, `RoleReport`, and `SwitchError`; `dcs_runtime::Peer` is the role machine wrapping the executor, applying `promote`/`demote` at the request's scan boundary — the gate moves with the request, the reported role settles on the first scan under the new mode — and refusing with the named `SwitchError`s (`NotConverged`, `AlreadyActive`, `NotActive`). The monitor serves them as `GET /role`, `POST /promote`, and `POST /demote`, journals each reported transition as `JournalEvent::RoleChanged`, and refuses commands on a non-active peer with `CommandError::NotActive`. `dcs-controller --standby ADDR --listen ADDR` is the promotable standby; every instance whose driver surface reaches the shared field assembles behind a `WriteGate` — a `--remote` attachment behind full coverage, a registry fan-out declaring `sim-tcp` devices behind `closed_covering` over the `DeviceBackend.field_facing` points so a tracking peer's local simulated backends keep stepping while its field writes are quiesced (`FanoutDriver::step_local` likewise leaves field-facing backends' clocks to the field owner). The acceptance test `dcs-controller/tests/promotion.rs` proves bumplessness over the shared plant: after the switch the promoted peer's snapshots equal the uninterrupted run the demoted peer still computes. The process-level scripted run — a `dcs-plant-server` owning the plant, two `dcs-controller --driven` peers on one `sim-tcp` model, and an uninterrupted reference run — lands under #64 (`dcs-controller/tests/hot_swap.rs`), deterministic across repeated runs.
- **Problem:** A tracking standby must be able to become the active without a missed, duplicated, or divergent field write — the vision's hot-swap requirement. This needs recorded semantics before code: who may write the field, when the change takes effect, and what "bumpless" demands.
- **Chosen approach (implemented):** A role contract — `active`, `standby`, and transition states — as serde-serializable monitoring types, so the UI treats the pair as one logical controller. While standby, the executor scans and tracks checkpoints (decisions 11–13) but its `Out` writes are quiesced behind a write gate at the driver boundary — a gating `IoDriver` wrapper or a mode on the shared remote driver — while `In` reads still observe the field. A promotion request lifts the gate at a documented scan boundary, so the first post-promotion scan writes exactly what the checkpointed run would have written — bumpless by determinism (decision 4). Demotion re-quiesces the old active, preserving the invariant that exactly one peer writes the field at a time. Promotion is operator- or test-triggered; automatic failure detection is out of scope. Promoting a standby that has not converged, or promoting twice, fails with a named error rather than diverging.
- **Alternatives considered:** Lockstep both-write redundancy (rejected with decision 10 — double writers need arbitration at every field device); a halted standby cold-started on failure (missed scans and lost state — a restart, not a swap); mid-scan promotion (a timing-dependent effect, against decision 4's determinism); automatic heartbeat failover (premature machinery — a follow-up once the manual semantics are proven).
- **Consequences:** "Bumpless" is a property of a converged standby: the gate, the promotion boundary, and the convergence check are all required pieces. The gate must sit at the field-facing boundary — quiescing the standby's writes to the shared field without freezing the image-carried or locally simulated state it needs to keep tracking. Role reporting on the monitoring surface gives the UI its single-logical-controller view (decision 10).
- **Affected tickets:** #33 (this record); #46 implemented it; #32 supplied the tracking standby and #31 the shared field.

### 16. Component self-description

- **Status:** Accepted and implemented (#50, #61, #62): `ComponentDescriptor`/`PortDescriptor`/`PortRole`/`ParameterDescriptor`/`ParameterRange` are serde contract types in `dcs-core`; `Component::describe` derives a correct default and every `dcs-blocks` kind overrides it beside its `KIND` and `from_parameters`; `TelemetrySnapshot.descriptors` serves one descriptor per component in scan order. Descriptor-driven range enforcement of operator tuning is implemented (#82, decision 20), and the descriptor-driven faceplates are implemented (#62): the page renders one faceplate per instance from the served descriptors — role hints picking the conventional elements, parameters listed with kinds and ranges, diagnostics joined by name — degrading to the generic name-plus-diagnostics-plus-wired-points view for a kind with no custom descriptor.
- **Problem:** The monitoring UI must render each component instance — a faceplate with its live values, tunable settings, and state — without hand-building a view per component kind, or the vision's "no per-layer engineering" goal fails at the UI layer. What the UI can fetch today does not say how to draw a component: the snapshot carries only diagnostics counters per instance, and the model's `kind`/`ports`/`parameters` describe wiring, not semantics — nothing says which port is the process value versus the setpoint, or which parameters are operator-tunable.
- **Chosen approach (implemented):** A serde `ComponentDescriptor` type in `dcs-core` — beside the other contract types the UI already consumes — with one descriptor per registered component kind. A descriptor names the kind and describes its surface: ports (name, direction, value kind, and a small closed set of semantic roles such as process-value, setpoint, command, feedback), parameters (name, value kind, required or optional, constraint hints), and faceplate hints (which elements the default rendering shows and which are trend candidates). Each `dcs-blocks` kind supplies its descriptor beside `KIND` and `from_parameters`; the component registry (decision 6, #19) collects them and the monitor serves them to the UI over the decision-8 transport. The page joins three already-modeled things — descriptor (kind semantics), `ComponentInstance` (this instance's wiring and parameters), and live point telemetry (the values) — and renders any registered kind generically. An instance whose kind serves no descriptor degrades to a generic name-plus-diagnostics-plus-wired-points view, never an error.

The port-to-point wiring the join needs stays inside the serde contract (#62): `PortDescriptor` carries an optional `point` — the `PointId` the instance's port is bound to — which the executor annotates when it serves the descriptor, joining each port to the point its same-named declared `IoRequirement` resolved to. A kind-level `describe()` reports `None` — the binding is instance wiring, not kind semantics — and the field serializes only when populated, so payloads predating it still parse and a port no declared requirement backs serves as unwired. The alternative — the monitor serving the loaded model's component and connection sections for the page to re-resolve — was rejected: it exports the wiring resolution to every consumer and duplicates the executor's already-checked binding work, where the annotation keeps the whole contract in the shared serde types.
- **Alternatives considered:** Per-kind UI code in the page (per-kind engineering is exactly what the vision rules out); serving per-kind view templates (a second artifact to version and ship, plus the safety surface of remotely supplied markup); deriving the faceplate from `IoRequirement`s alone (they carry resolved per-instance point bindings — name, direction, kind — but no semantic roles or parameter semantics; the descriptor adds exactly that layer); embedding descriptors in the model document (duplicates kind-level data into every model and splits the source of truth between schema and component code — the registry keeps one).
- **Consequences:** Adding a component kind means supplying one descriptor next to its constructor — control behavior, monitoring data, and UI representation arrive together, per the vision. The descriptor vocabulary is a contract like every other `dcs-core` type, so UI, runtime, and blocks version together; it must stay small and generic, and kinds needing genuinely bespoke views degrade to the fallback rather than growing the vocabulary. Descriptor serving joins the metadata surface, so the UI needs no extra transport.
- **Affected tickets:** #53 (this record); #50 implements the contract and per-kind descriptors; #61 completed the per-kind descriptors; #62 implements the faceplates and the bound-point annotation; relies on #19 (registry), #34 (metadata serving), and decisions 6 and 8.

### 17. History and journal consumption

- **Status:** Accepted and implemented (#35): `dcs-monitor` records a per-point history ring and the transition journal once per completed scan — after the write phase, never mid-scan — bounded by `MonitorConfig` with oldest-first eviction and never-reused `seq`s, and serves `GET /history` (repeatable `point` selection plus `since`) and `GET /journal?since=…`. The contract types (`HistorySample`/`PointHistory`, `JournalEntry`/`JournalEvent`) live in `dcs-core` per decision 8; journaled events are quality changes, settled command receipts, and component step failures — role changes (decision 19) join when #46 lands. The page panes consuming the streams — per-point trends on `/history` and the journal pane on `/journal`, both polled on the snapshot's cadence — are implemented (#51). Implemented basis: `TelemetrySnapshot` deliberately reports state, not history (#10); the executor's in-memory receipt log is the first journal-like record, served by `GET /receipts` (#20); all timestamps are already virtual `Tick`s (decisions 2, 4).
- **Problem:** Trend and event views need sequences, not point-in-time state: a trend wants each point's recent samples and a journal wants the run's transitions — commands applied or rejected, quality changes, component errors. A UI polling `/snapshot` cannot reconstruct either: samples between polls are lost, and a transition that begins and ends between polls is invisible.
- **Chosen approach (implemented):** The producer records bounded history server-side, in the tick domain. A per-point ring retains each point's recent `Sample`s; a transition journal is an ordered log of events — command applied/rejected, point quality transitions, component step errors, and later role changes (decision 19) — each stamped with its tick. Both carry fixed capacity with oldest-first eviction and a monotonically increasing sequence number per stream, so a consumer detects a gap instead of silently missing entries. The monitor serves them as GET endpoints with since-cursors — `GET /history` (point selection plus since) and `GET /journal?since=…` — and the UI polls them incrementally on the same cadence it already polls `/snapshot`: order-of-seconds, far slower than the scan period, refetching only entries newer than its last seen sequence. Tick stamps keep journal, history, snapshots, and receipts in one comparable timebase.
- **Alternatives considered:** Reconstructing history by polling snapshots at scan rate (a lagging page still misses samples, and per-point series would transfer unboundedly); unbounded server-side logs (memory growth a long-running controller cannot accept — #35 specifies bounded); push streaming over SSE or WebSocket (decision 8 already deferred push — cursor polling fits the pull-shaped contract, and a push layer can be added later without changing these types); persisted history on disk (durability machinery M4 does not need — history is volatile diagnostics and re-accumulates after a restart).
- **Consequences:** Executor and monitor gain fixed-size buffers — memory stays bounded as a deployment constant — and the UI stays poll-shaped, consistent with decision 8. Consumers must tolerate sequence gaps; the numbers make them visible rather than silent. The journal becomes the audit surface for operator actions, complementing per-command receipts with one ordered run record.
- **Affected tickets:** #53 (this record); #35 implements the buffers and endpoints; #51 builds the consuming panes; relies on the #20 transport and the #10 snapshot contract.

### 18. Operator command surface

- **Status:** Implemented (#49): `IoPoint` carries the optional `writable` flag, validation rejects it on `Out` points, assembly propagates it into the `PointMap`, `submit_command` refuses unmarked points and every `Out` point with `CommandError::NotWritable` at submission, the signal index reports the mark, and the page's command form offers only writable points. The command contract (#20, decision 7) supplied the receipts and the scan boundary; the channel-less internal points most operator values occupy came from #30 (decision 14).
- **Problem:** As implemented, the command surface is "every mapped point": `WriteValue` accepts field `In` points the process drives and `Out` points a component overwrites next scan, so writes that cannot meaningfully stick are still accepted. The model does not say which points an operator may command, and the UI cannot distinguish a legitimate setpoint entry from a write destined to be overwritten — it learns only through receipts after the fact.
- **Chosen approach:** The plant model declares the writable surface: an optional `writable` marker on `IoPoint` (decision 14, #49), defaulting to unset so documents predating it load unchanged under the same model version. The mark is honored on `In` points only, and the recorded rule for `Out` points is rejection — `WriteValue` commands never target them, and validation reports `writable` on an `Out` point as `WritableOut`, because control logic stays authoritative over outputs inside a scan and operator influence on an output is engineered through components. The flag rides the point into the executor's point map (`PointSpec::writable`), and `submit_command` rejects a command on an unmarked point or any `Out` point at submission with `CommandError::NotWritable` carrying the point — before queuing, alongside the existing unknown-point and type-mismatch rejections. Per kind: a writable field `In` point's command write is forwarded to the driver at the scan head and read back by the same scan's input phase — documented operator substitution of the input image, holding until the field side asserts a different value — while a writable internal `In` point takes the write in the image and holds it until the next command, the common target for operator values like setpoints. Writability joins the point metadata the UI already fetches (the signal index, #34), so the page renders command affordances only where commands can succeed and everything else stays read-only telemetry.
- **Alternatives considered:** Inferring writability from direction or kind (no direction maps to "operator-writable" — field `In` points are driven by the process, internal `In` points are not, and only the model knows which is which); an executor-side allowlist configured beside the model (a second authority for the same fact, breaking the single-contract goal); UI-only hiding of non-writable affordances (cosmetic — the controller still accepts the commands and the journal still records them).
- **Consequences:** The writable surface becomes explicit, auditable model data; rejection happens at submission with a named reason, keeping the receipt contract uniform (decision 7). Because the flag is schema-optional (decision 3) it defaults to non-writable — models must opt operator points in, and existing models gain the restriction without a version bump. The UI's affordance set derives from the same metadata fetch that drives its displays.
- **Affected tickets:** #53 (this record); #49 implements the flag and the rejection; #30 supplied the internal points operator values occupy; #20 implemented the permissive baseline; decisions 7 and 14.

### 19. Monitoring under redundancy

- **Status:** Implemented — contract under #46, the pair view under #63; the divergence gate of decision 26 joins the same role surface under #88. `GET /role` serves the serde `RoleReport` — `Role` plus the standby's `StandbySync` convergence — on each peer's monitor; `POST /promote` and `POST /demote` answer the post-change report or `409` with the named `SwitchError`; commands submitted to a peer not reporting settled `active` are refused with a `CommandError::NotActive` rejection receipt; and each reported transition is journaled as `JournalEvent::RoleChanged`, keeping roles, history, and snapshots on the one tick timebase. The view half: `dcs-monitor::PairClient` is configured with both peers' monitor addresses, polls each `/role`, sources data from the settled-active peer (a `promoting` peer while a switch settles), routes commands only to a peer reporting `active` with one re-poll-and-retry on a `NotActive` rejection, and reports an unreachable peer as the named `PeerStatus::Unreachable` redundancy fault; the page carries the same logic — configured by `?peer=<host:port>` URL parameters beside its serving origin, rendering the active's telemetry plus a per-peer pair-health section, merging trend and journal feeds across a source switch by the shared tick domain, with cross-origin reads enabled by `Access-Control-Allow-Origin` on the JSON endpoints. Implemented basis: `dcs-monitor` serves one executor's snapshot, signal index, receipts, history, journal, commands, scan stepping, checkpoints, and the page (#20, #34, #35, #51, #32), hosted inside `dcs-controller` (#48).
- **Problem:** Under M3 the UI faces two controller processes — an active and a standby — that are one logical controller to the operator (decision 10). The UI needs a recorded rule for which peer to display and command, how it behaves across a promotion, and how to avoid presenting the quiesced standby as a second controller or a divergent plant.
- **Chosen approach (implemented contract):** The pair exposes role on the monitoring surface: each peer reports its role — `active`, `standby`, and the transition states of decision 15 — as the serde `RoleReport`, served on its own monitor at `GET /role`. The UI is configured with both peers' addresses, polls each for role alongside its normal cadence, and renders one logical controller from the active's telemetry; the standby contributes pair health — reachability and checkpoint convergence — not a second faceplate set. Commands go only to the peer reporting `active`; a command landing on a standby or mid-transition peer is rejected with `CommandError::NotActive` and the UI retries after re-polling roles. Tick continuity is the divergence check: after promotion the new active's snapshots continue the same tick sequence, so the history and journal panes (decision 17) carry on seamlessly.
- **Alternatives considered:** A gateway endpoint fronting the pair and hiding roles entirely (another deployed component, and the UI learns nothing about redundancy health); polling both peers and merging their telemetry (merge rules risk double-rendering the quiesced standby's suppressed writes); DNS- or virtual-IP-based failover (network machinery, and the standby becomes unobservable — no convergence badge); presenting the pair as two controllers (violates the vision's one-logical-controller requirement).
- **Consequences:** Redundancy becomes visible without being duplicative: one faceplate set plus pair health. Role reporting joins the monitoring contract so every consumer handles pairs uniformly, and transition windows resolve by re-poll rather than push machinery — consistent with decision 8. An unreachable standby is displayed as a redundancy fault, not a plant fault, so the UI needs per-peer connectivity handling regardless.
- **Affected tickets:** #53 (this record); #46 implemented roles and promotion; #32 supplied the tracking standby and #31 the shared field; decisions 10 and 15 record the direction and semantics; the M4 page ticket #63 consumes the logical view.

### 20. Component-targeted commands

- **Status:** Accepted and implemented (#82): `Command::SetParameter { component, name, value }` addresses the component by the name its descriptor and diagnostics report, `Executor::submit_command` validates it against the component's declared `ParameterDescriptor`s — existence, value kind, and a declared `ParameterRange` — and applies it at the same scan head as `WriteValue` through the `Component::apply_parameter` hook, whose default is a named `UnsupportedParameter` rejection. Every `dcs-blocks` kind implementing the hook folds its writable parameters into `capture_state`, so a tracking standby inherits runtime tuning through the ordinary checkpoint (decision 11).
- **Problem:** Operators tune a running plant — PID gains, alarm limits, filter constants — without redeploying it. Today the only command target is a point, and component parameters are construction-time input read once by `from_parameters`: changing one means editing the model and reassembling the executor, which is a redeploy, not an operator action. The component-instance half of decision 7's "a component instance or a point" target remains unimplemented.
- **Chosen approach (proposed):** A new serde `Command` variant — `set_parameter { component, name, value }` — addressing the component by the name its descriptor and diagnostics report, applied at the same documented scan boundary as `WriteValue` and answered by the same receipt contract. The `Component` trait gains an `apply_parameter` hook whose default is a named rejection, so kinds opt in rather than silently accepting nothing; `dcs-blocks` kinds implement it for the parameters their descriptors declare. Validation treats the descriptor as the authority: unknown component, unknown or undeclared parameter, value-kind mismatch, out-of-range against a declared `ParameterRange`, and `set_parameter` on a kind exposing no writable parameters each reject with a named `CommandError` carrying the component. The carryover obligation: a tuned parameter is run state, so implementors must fold it into their `capture_state` vocabulary — a tracking standby inherits runtime tuning through the ordinary checkpoint (decision 11), and a kind that tunes without capturing diverges on switchover.
- **Alternatives considered:** Retuning through a revised model document (a redeploy that breaks the very run being tuned; the deliberate rolling-update path of decision 25 is intentionally heavyweight); routing tuning through shadow `In` points a component reads as parameters (works on the implemented path, but splits the tunable surface across point wiring and parameter maps, and no declared range bounds the write); a free-form command string (untyped, unauditable, unenforceable — the receipt contract cannot name what failed).
- **Consequences:** Tuning becomes a receipted, journaled operator action whose enforcement authority is the same descriptor that draws the faceplate — the metadata that shows a parameter also bounds its edit. Parameters join checkpointed run state, so each kind's `StateMap` vocabulary grows and deploys to both peers together, per decision 11's strictness. Kinds that must stay fixed simply keep the default rejection.
- **Affected tickets:** #89 (this record); #82 implements it; relies on #20 (command path), #50 and #61 (descriptors), and #21 (checkpoint carryover); decisions 7 and 16.

### 21. Input forcing

- **Status:** Accepted and implemented (#84): `Command::ForcePoint`/`Command::UnforcePoint` are serde commands in `dcs-core`; the executor holds a deterministic `BTreeMap` force set applied at the scan-head command boundary; `TelemetrySnapshot.forces` lists `ForcedPoint { point, value }` entries for the page's badge; the `Checkpoint` carries a `forces` section validated on restore (`RestoreError::UnknownForce`/`IncompatibleForce`); and the journal records both commands as ordinary `CommandSettled` receipts beside the `QualityChanged` transition to `Substituted` their application produces. Implemented basis going in: `QualityReason::Substituted` already names exactly this case in the signal model (#5, decision 2); `Command::WriteValue` to an `In` point already stages a value the scan observes once — but the field driver owns the point thereafter, so wherever the field keeps driving, the substitution is one-shot (#20); and the journal, snapshot, and checkpoint machinery a force set rides on are in place (#35, #10, #21).
- **Problem:** Commissioning and fault-finding need the control-systems notion of a force: pin a field input at an operator-chosen value across scans while the rest of the plant keeps running. A `WriteValue` to an `In` point cannot express this — the driver's next read reasserts the real value, or a loopback or process element overwrites it — and nothing marks a substitution as persistent and deliberate.
- **Chosen approach (implemented):** A dedicated `force_point`/`unforce_point` command pair — chosen over a flag on `WriteValue` so the one-shot write path keeps its existing contract unambiguous — acting on a model-declared *writable* `In` point. While forced, the scan's input image holds the forced value stamped `Quality::Uncertain(QualityReason::Substituted)` every scan and the driver read is bypassed, so components and the UI see substitution, never false `Good` data; a `WriteValue` to a forced field point still reaches the driver — the force overrides the image, not the field — so release observes the field's current value. Release resumes live reads at the next scan boundary — the same documented application point every command uses — so the releasing scan already reads the field again. The force set is run state: listed in the `TelemetrySnapshot` so the page can badge forced points, journaled on force and release (decision 17), and carried in checkpoints so a standby preserves forces through a switchover — a checkpointed force entry is accepted only where the standby's map serves a writable `In` point of matching kind. Forcing a non-writable point or an `Out` point rejects at submission with a named `CommandError`; the writable flag (decision 18) thus bounds the force surface exactly as it bounds ordinary writes. The same surface covers writable internal `In` points — a force overrides even an internal link's routing onto the point — and `unforce_point` on an unforced point applies as a no-op.
- **Alternatives considered:** Driver-side forcing, e.g. a `SimDriver` fault-style override (bypasses the executor's image, cannot reach field-observing drivers, and reports no substituted quality into the scan); forcing stamped `Good` (hides the substitution the quality model exists to expose); engineering an `OverrideSelect` per forceable input (a control-logic feature — permanent interlocking in the model, not an ad-hoc commissioning action).
- **Consequences:** A force is an explicit, audited, checkpointed deviation between the field and the scan image: the journal answers when it was applied and released, the snapshot answers what is forced now, and a restored standby forces identically. Determinism is preserved — forces apply and release at the scan boundary like every other command. The writable surface a force acts on is implemented under #49 (decision 18).
- **Affected tickets:** #89 (this record); #84 implements it; depends on #49 (writable points); relies on #20, #35, and #21; decisions 2, 7, 14, and 18.

### 22. Driver health and scan overruns

- **Status:** Implemented (#83): `TelemetrySnapshot` carries an `io_health` section — `IoHealth` holding `failed_reads`/`failed_writes` totals, a `consecutive_failures` streak over boundary operations, the `last_error` as an `IoFault` (tick, point, direction, `IoError`), the `scan_overruns` count, and the driver's `DriverDiagnostics` (`link` as a `LinkState`, `last_error` description) — all serde types in `dcs-core`; the executor counts at the read and write boundaries where it already degrades samples or fails the scan; `IoDriver::diagnostics` defaults to `None` and `RemoteDriver` implements it, `WriteGate` forwards it, `FanoutDriver` aggregates it per backend; the paced scan loop feeds overruns through `Executor::record_scan_overrun` reached via `Peer`/`Monitor`; and the monitoring page renders the section as its own line beside per-point quality.
- **Problem:** Field-communication trouble currently surfaces only per point — a `Bad` sample here, a `ScanError` there — so the UI cannot answer "is the I/O subsystem healthy", nor distinguish one bad sensor from a dead field link: a `RemoteDriver` whose plant server is down reports the same per-point errors a single failed channel would, and link state is invisible. A paced controller that overruns its scan period likewise degrades silently — nothing counts it.
- **Chosen approach (implemented):** A named I/O-health section on `TelemetrySnapshot`, as serde contract types in `dcs-core` like every other monitoring type. The executor collects counters at the boundaries it already owns: failed-read and failed-write totals, a consecutive-failure count reset by any successful boundary operation, and the last `IoError` with the tick and point it hit. Beneath that, an optional `IoDriver::diagnostics` hook — default `None`, mirroring `capture_state`'s per-kind optionality — lets a driver report transport-level health: `RemoteDriver` reports link state and its last protocol failure, so a dead plant server reads as link degradation distinct from per-point faults. Scan overruns are the third surface: the pacing shell counts scan periods it overshot and feeds the counter through `Executor::record_scan_overrun` — wall-clock failure data enters the snapshot from outside the executor, which stays tick-domain per decision 4.
- **Alternatives considered:** Deriving health from per-point quality in the UI (loses the link-versus-point distinction and forces every consumer to re-aggregate); logging overruns to stderr only (invisible to the contract, the snapshot, and the journal); letting the executor detect overruns from a wall clock (rejected with decision 4 — nondeterminism inside the core); a mandatory driver-diagnostics method (breaks drivers that have nothing transport-level to report — optionality mirrors `capture_state`).
- **Consequences:** I/O health becomes contract data the UI, the journal, and tests read identically — the page renders it as a health line separate from per-point quality — and driver diagnostics stay per-kind optional so `SimDriver`, `RemoteDriver`, and future fieldbus drivers implement or skip it honestly; wrapping drivers forward or aggregate (the gate passes the field link's health through; the fan-out reports any reporting backend's link). The counters describe the producing run's own driver experience and are not checkpointed — a standby's health is its own driver's. Overrun reporting keeps pacing's wall-clock concern outside the deterministic core — the counter is a report, never an input — and identical runs produce identical health payloads.
- **Affected tickets:** #89 (this record); #83 implemented it; relies on #9, #10, #31, and #48; decisions 4 and 5.

### 23. Signal display grouping

- **Status:** Implemented (#86): `Signal` carries an optional `group` field under the schema's optional-field convention — documents predating it load unchanged, no version bump (decision 3); `SignalIndex` entries carry the group through per point; validation treats it as display metadata with no wiring rules; the `dcs-model` CLI reports a `signal_groups` count; and the page organizes its point listing under the declared groups, filing ungrouped signals under the documented `"ungrouped"` default.
- **Problem:** The monitoring page lists every signal flat. A real plant's signals belong to areas and units — grouping that is engineered knowledge about the plant. Today the only place to put it is page code or name conventions, which splits the single-contract goal at the presentation layer.
- **Chosen approach (implemented):** Grouping is engineered model data: an optional `group` field on `Signal`, following the same schema-optional convention as `unit`/`description`. `SignalIndex` carries the group through per point, the `dcs-model` CLI reports group counts, and the page groups its listing by it — ungrouped signals landing under the documented `"ungrouped"` default group.
- **Alternatives considered:** Deriving groups from device ids or `PointId` ranges (physical device topology is not the operator's plant-area structure); a page-side grouping configuration (a second authority for engineered data, off the single contract); name-prefix conventions like `AREA.SIGNAL` (unvalidated, and conflates a signal's identity with its organization).
- **Consequences:** UI organization becomes diffable, validated model data — the page needs no per-plant code, and future consumers get the same grouping for free. The field is display metadata only: it constrains nothing the controller does, keeping the schema addition cheap and backward compatible.
- **Affected tickets:** #89 (this record); #86 implemented it; relies on #23 (signal index) and #34 (metadata serving); decision 3 supplies the versioning rule.

### 24. Plant-side dynamics as declared data

- **Status:** Implemented (#81): `dcs-plant-server --dynamics FILE` merges a plant-side dynamics document — a JSON list of `ProcessElement` declarations in the `ChannelMap` vocabulary (`first_order_lag`, `second_order_lag`, `integrator`, `dead_time`, `noise`, `bool_flow`, `flow_sum`, `scaled_flow`) — into the resolved `sim_channel_map`, validating each element as it merges so a rejection names the element and the point it drives. The document deliberately carries no `version`: it is a plain declaration list beside the model, and widening the element vocabulary extends it the way `ChannelMap` itself evolves. Basis: the `ProcessElement` vocabulary — `FirstOrderLag` and `Integrator` (#8), `DeadTime` (#37), `SecondOrderLag` (#135), `Noise` (#156), `BoolFlow` and `FlowSum` (#200), `ScaledFlow` (#252) — stepped deterministically by `SimDriver::step` and checkpointed as element accumulators (#21); `dcs-assembly::sim_channel_map`'s open seam (#19); `PlantServer` serving a map's elements to remote drivers (#31). The Bool-gated and summed variants widen the vocabulary past SISO Float ends: a `bool_flow`'s gate is the one `Bool` element input, and `flow_sum`'s declared `inputs` list is the vocabulary's one multi-input shape — `ProcessElement::input` reports `Option<PointId>` for the single-input variants while `inputs` enumerates every variant, so actuator commands close a simulated loop without new stepping machinery. The second-order lag's recorded parameterization is `time_constant` (τ = 1/ωₙ) plus `damping_ratio` (ζ) — the standard `τ² y'' + 2ζτ y' + y = u` form, chosen over a two-time-constant cascade because ζ < 1 produces the overshoot the parameter-tuning path needs — stepped by the exact zero-order-hold discretization like the first-order lag's exact update. The noise element's recorded parameterization is `amplitude` plus `seed`: each `Good`-input step draws once from a splitmix64 generator — the `u64` state advanced by the golden-ratio increment, then the standard mix, the top 53 bits scaled into [0, 1) — and outputs `u + amplitude·(2x − 1)`, holding the output within `u ± amplitude`. A seeded generator was chosen over a declared disturbance table because it gives an unbounded, exactly replayable deviation sequence without authoring data: identical seeds replay identical sequences, distinct seeds produce distinct ones, and the carried `u64` state checkpoints and restores mid-sequence like every element accumulator. The scaled flow's recorded naming is `scaled_flow`/`ScaledFlow` (#252): `y = gain · u` re-evaluated each step, a `Float` command scaled into the signed rate downstream `flow_sum`/`integrator` elements consume — negative gains declaring draws as well as feeds — added so a document can express "drain proportional to the Float speed demand", which `bool_flow`'s fixed on/off rates and `flow_sum`'s verbatim sum cannot; stateless like them, so `dt` never scales the output and `initial` covers reads before the first `Good` step.
- **Problem:** The simulated plant's behavior — which elements drive which points, their time constants, delays, and initial values — is real engineered content, but it lived in Rust call sites: `dcs-demo` built its `FirstOrderLag` inside `assemble`, and a shared plant server could not serve a declared plant without code. The obvious alternative — process elements inside `PlantModel` — would couple the control contract to testbench physics: a real plant has no such declaration, and the model's job is to describe the controller's view, not the world's.
- **Chosen approach (implemented):** Plant-side dynamics are declared data in a document *beside* the model, not schema *inside* it: a JSON list of `ProcessElement` declarations naming the points each element drives. `dcs-assembly`'s open `sim_channel_map` seam is the join — `dcs-plant-server` loads the model for the control contract and the dynamics document for the physics, merging them into the `ChannelMap` it serves. The plant model stays the single contract for control; the dynamics document is the testbench's account of the process it stands in for — engineered and validated, never read by a controller.
- **Alternatives considered:** Elements as `PlantModel` schema (the contract would carry simulation-only content real deployments cannot produce, and every consumer would have to know which sections are testbench); keeping dynamics in Rust constructors (the pre-#81 status quo — works for `dcs-demo`, but the shared plant server and every integration rig then need code to describe the same plant); folding dynamics into device `kind` strings (opaque, unvalidated, and duplicating point identity the model already owns); a versioned document like the model's (decision 3's discipline is for a long-lived cross-consumer contract — the dynamics list is simulation tooling's own format, so a bare declaration list suffices until it grows one).
- **Consequences:** The simulated plant is declaratively reproducible — `dcs-plant-server` and test rigs load the same document instead of reimplementing physics, and the scripted-scenario device kind (#67) gets a sibling pattern for declared behavior. The boundary stays explicit: the model describes what the controller sees, the dynamics document describes what the world does. Checkpointing already treats element accumulators as driver state (decision 13), so the redundancy path needs no change.
- **Affected tickets:** #89 (this record); #81 implemented it; #67 follows the same declared-behavior pattern; #200 widened the vocabulary with `bool_flow` and `flow_sum`; #252 added `scaled_flow` (requirement `WW-CTL-003` enabler — the dosing loop's demand-proportional drawdown and discharge rate). Implemented basis: #8, #12, #21, #31, #37.

### 25. Rolling model-revision deployment

- **Status:** Implemented (#87). The standby's checkpoint pull is `Peer::transfer`, which routes on the fingerprint negotiation (decision 27): a matching fingerprint applies as ordinary convergence exactly like `Peer::apply`; a foreign fingerprint on a peer armed by `dcs-controller --revised` (`Peer::with_revision`) crosses the model boundary through `Executor::reinitialize`, while an unarmed peer still degrades on the mismatch — a revision is a declared intent, never an assumption. The crossing classifies every checkpoint section before applying anything, so a refused revision leaves the revised run untouched: carried are writable internal `In` points matched by declared point id and value kind, `Out` image samples the revision still declares, and the force set under the same writability and kind rule; named dropped in the `dcs_core::CarryoverReport` are checkpointed points the revision does not serve, synthesized link points whose allocated ids are not declared identity, checkpointed component state whose name the revision does not register, and driver state — component and driver state always reinitialize (no per-kind compatibility rules exist yet, and tuned parameters ride the checkpoint's component section, so a tuning-preserving revision must keep the component's declared identity). The armed peer reports `StandbySync::Reinitialized` carrying the report — source and destination fingerprints, resumed tick, the carried and dropped and initialized sets — journaled once per transition as `JournalEvent::Reinitialized`; `promote` and `self_promote` accept it beside `Tracking`, so the documented demote-then-promote order (decision 15) moves the field writer to the revised model at a scan boundary with exactly-one-writer preserved. A carryover break — a retyped carried point, an unserved force, an unsupported checkpoint version — is a named `CarryoverError` the peer reports as `Degraded`, and `POST /promote` answers `not_converged` with that state. The scripted rig is `crates/dcs-controller/tests/model_revision.rs`: a `dcs-plant-server` tank loop with a v1 active and a `--revised` v2 standby runs the full revise-reassemble-promote path asserting the named carryover outcomes, the breaking-change refusal before promotion, and bit-identical repeats.
- **Problem:** Plant engineering changes in service — a retyped point, a new component instance, rewired connections — produce a revised model the running pair must adopt without interrupting the process. A checkpoint cannot carry a run across model revisions: decision 11's strictness makes the mismatch a named rejection, which is correct for same-model peers but leaves no path for intentional change.
- **Chosen approach (implemented):** Model revision is a deliberate rolling deployment through the redundant pair, not a synchronization. The standby loads the revised model, validates and reassembles against it, and enters a named `reinitialized` state — distinct from `converged`, since no checkpoint carried it there. A documented carryover rule decides what crosses the model boundary: operator-writable internal points (decisions 14, 18) matched by declared identity carry their last values; component state reinitializes by default unless a documented per-kind compatibility rule applies; and every element that cannot carry over is named in a carryover report rather than silently dropped. Promotion then moves the field writer to the new-model instance at a scan boundary with demotion of the old peer (decision 15), preserving exactly-one-writer throughout; a revision that breaks a carryover rule fails before promotion with named errors.
- **Alternatives considered:** Restart-based redeploy (interrupts the process — the vision rules it out); a lenient restore mapping old state onto new components field-by-field (silent divergence — exactly what decision 11's strictness exists to prevent); dual-running old and new revisions with output comparison (doubles the field-writer question and defines no merge for differing control logic).
- **Consequences:** In-service model change becomes an engineered, auditable operation: the carryover report is the record of what survived the revision, and `reinitialized` is an honest state the UI can display instead of a false convergence. Component kinds that want tuning-preserving upgrades document per-kind compatibility rules — the same obligation decision 20 puts on runtime tuning. The mechanism composes existing pieces — load, validate, assemble, promote — rather than adding a second deployment path.
- **Affected tickets:** #89 (this record); #87 implements it; #114 adds `dcs-model diff`, the human-facing review surface reporting what a revision adds, removes, and changes before the pair rolls it; relies on #46 (promotion), #66 (fingerprinted checkpoints), and #32 (standby machinery); decisions 10–15.

### 26. Standby divergence detection

- **Status:** Implemented (#88): `dcs-runtime`'s `Peer` stashes each non-field-owning scan's staged field `Out` image (`Executor::staged_field_outputs`, the writes the scan would have issued through the closed `WriteGate`) and, when an applied checkpoint's tick equals the stashed tick, compares it point by point against the peer's own reads of the same `Out` points through its driver surface (`divergence::compare_staged`). A mismatch moves the peer to the named `StandbySync::Diverged` carrying the `Divergence` evidence — point, staged value, field value — and `promote` refuses it with `SwitchError::NotConverged` carrying that state; the gate stays closed throughout. A fresh checkpoint whose same-tick staged image compares cleanly restores `Tracking`, and the transition is journaled as `JournalEvent::DivergenceDetected` at the compared tick and rendered beside the convergence state on the monitoring surface.
- **Problem:** A checkpoint-converged standby can still diverge — skewed parameters, a plant restart, a missed transfer — and nothing would notice until it promotes and writes wrong outputs to the shared field. As implemented, `StandbySync::Tracking` means "the last checkpoint applied cleanly", not "proven to track the run".
- **Chosen approach (implemented):** The standby proves tracking before it may promote. While quiesced it stages the `Out` writes its scan would issue; the field-side truth is the standby's **own reads of the same `Out` points** through its driver surface — the field-observing path it already uses for inputs, needing no plant-server record and seeing exactly the field the standby would write. The per-value-kind rule is documented in `dcs-runtime::divergence`: `Bool` and `Int` compare exactly, `Float` agrees within `FLOAT_TOLERANCE = 1e-9` relative to the larger magnitude (absolute floor 1.0, `NaN` agreeing only with `NaN`) — sized to absorb transport re-encoding without masking real divergence; a failed field read is skipped, a communication fault not being divergence evidence. The check pairs a staged image with the checkpoint of the *same* tick — under the pull-per-scan cadence that is the transfer after the staged scan, the stated detection bound — because the field then holds the active's write for the tick the staged image describes; an unmatched staged image is discarded rather than compared against a different tick's field. Mismatch lands the peer in `Diverged`, which `promote` refuses like any unconverged state; the transition — not each recurring mismatch — is journaled once with the compared tick, and a clean same-tick comparison is the resync that clears it.
- **Alternatives considered:** Promoting on checkpoint freshness alone (a checkpoint proves delivery, not that the standby's subsequent scans track — a skewed component diverges the tick after restore); comparing input images only (two peers reading the same field agree on inputs while computing different outputs — the divergence that matters is in what would be written); post-promotion detection (the wrong outputs are already in the field before anyone looks).
- **Consequences:** Promotion gains an evidence gate: the pair reports not just "holds a checkpoint" but "tracks the field". Divergence is a named, journaled, resync-clearing state — a redundancy fault the UI shows as pair health (decision 19), never a plant fault. The per-kind comparison rule keeps the verdict deterministic: same inputs, same comparison, same answer on every run.
- **Affected tickets:** #89 (this record); #88 implements it; relies on #46 and #64; decisions 13, 15, and 19.

### 27. Checkpoint versioning for rolling upgrades

- **Status:** Implemented (#66). The shape #66 was left to fix: `Checkpoint` carries `format_version` — written as `CHECKPOINT_FORMAT_VERSION` (1), absent on pre-versioning checkpoints where serde defaults it to the accepted version 0; `SUPPORTED_FORMAT_VERSIONS` lists every version a restore accepts — and `model_fingerprint`, an `Option<dcs_core::ModelFingerprint>` holding FNV-1a over the canonical model document (`PlantModel::fingerprint`: the hash covers the parsed model's own reserialization, so key order and whitespace cannot perturb it while any semantic difference can). The assembling layer supplies the fingerprint — `dcs-assembly::assemble` stamps it via `Executor::with_model_fingerprint`, and `Executor::restore` takes the restoring model's as an argument — because the executor stays model-agnostic. Restore negotiates before any state applies: an unsupported version is `RestoreError::UnsupportedVersion` carrying the found and supported versions, a fingerprint mismatch is `RestoreError::FingerprintMismatch` carrying both values, and only then do decision 11's structural checks run — the standby's existing rollback leaves the rejected apply a named, recoverable `Degraded`. Implemented basis: the checkpoint contract itself exists — `dcs-runtime::Checkpoint` (`tick`, `components`, `driver`, `outputs`, `internal`) carries a run between executor instances (#21, decision 11), `GET /checkpoint` serves it and a `--standby` instance pulls and applies one per scan (#32, decision 12), and strict structural restore makes every incompatibility a named, recoverable `Degraded` state rather than a silent divergence. The pair-level path is exercised under #133: `crates/dcs-controller/tests/cross_version.rs` runs the scripted two-peer rig with the standby's per-scan `POST /scan` pull pointed at a fixture `GET /checkpoint` endpoint that proxies the active's live document rewritten into the negotiated wire shapes — the version-0 `format_version`-absent shape, an out-of-set version, and a foreign fingerprint — the recorded crafted-checkpoint-through-the-pull-path serving choice (a JSON document, not a `Checkpoint` value, because only the document can express "the field absent"). The version-0 stream converges the standby to `tracking` and the decision-15 switchover completes bumplessly; each refusal surfaces its named `RestoreError` as `degraded` carrying the found and supported versions or both fingerprints, `POST /promote` answers `not_converged`, and the active's run and field writes stay undisturbed.
- **Problem:** The M3/M4 hot-swap replaces a controller with an identical build. M5's lifecycle goal is replacing it with a *different* build — a bug fix, a component kind whose captured `StateMap` vocabulary grew — while the process runs. As implemented, strict restore rejects any skewed vocabulary: correct, but it cannot distinguish "incompatible build" from "transferable evolution", and a new-build standby discovers incompatibility only when its first checkpoint apply fails — there is no negotiation, so the pair's compatibility is unknowable until the transfer is attempted.
- **Chosen approach (implemented):** The checkpoint gains an explicit `version` field under decision 3's convention: a `CHECKPOINT_VERSION` constant, the loader accepting only known versions, a bump on any breaking format change — mirroring `MODEL_VERSION` on the model document. Beside it the checkpoint carries a fingerprint naming what it was captured under — the model identity, hash of the canonical serialized declarations — so compatibility is negotiated at fetch time: a standby rejects an unusable checkpoint before applying it, and the refusal reports through the existing `StandbySync`/`Degraded` surface (decision 19). The rolling-upgrade sequence then composes existing pieces: start the new build as a standby on the same model; if it accepts the checkpoint version and fingerprint it converges and decision-15 promotion/demotion completes the swap; if not, it reports the named incompatibility and the old build keeps running the process untouched.
- **Alternatives considered:** Keeping structural-only validation (every build change then forces a cold restart — the rolling upgrade M5 exists to provide); a negotiation handshake on the monitoring transport before any checkpoint moves (a second protocol beside the artifact it negotiates — versioning the checkpoint itself answers "can you consume this" directly); on-load migration of old checkpoint formats like the model's (premature for transient peer traffic — a version floor plus the strict per-element checks already name every genuine mismatch); loosening restore to tolerate field drift (rejected in decision 11 — quiet divergence).
- **Consequences:** Checkpoint format joins the versioned-contract surface alongside the model document, and the same optional-field discipline applies: additive changes ride the existing version, breaking changes bump it. Compatibility becomes a pre-flight question with a named answer instead of a failed apply. Per-kind `StateMap` vocabularies remain deploy-together wire contract; now the checkpoint says so explicitly.
- **Affected tickets:** #70 (this record); #66 implemented it; relies on #21 (the format) and #32 (the transfer); #87 consumes the fingerprint to distinguish a model revision from a build skew (decision 25); decisions 3, 11, and 12.

### 28. Automatic failover: heartbeat, self-promotion gating, and fencing

- **Status:** Implemented — #65. `Peer` counts consecutive failed checkpoint pulls (`misses`, reset by any produced checkpoint — the active served, so it is alive) against the budget `Peer::with_failover` arms; `failover_due` reports at the budget-th miss and `self_promote` promotes at that scan boundary exactly like a manual request — reporting `promoting` until the first scan under the lifted gate settles `active`. The convergence gate carries over as a proof that must still *stand*: the last applied verdict was `Tracking` — a diverged or rejected apply voids it — and the miss run has not exceeded the budget, so a single transient miss neither promotes nor re-arms, and an `Unsynchronized`, `Diverged`, or over-budget `Degraded` peer reports its named `StandbySync` state on `GET /role` instead of promoting. Fencing is the field's own arbitration: `dcs-sim-net`'s `PlantRequest::ClaimWriter` takes the plant's write-ownership under an opaque owner token — one token per controller instance covers all its attachments — granted unconditionally and held until preempted; once an owner is claimed, `write` answers a non-owner attachment with `IoError::Fenced` and `step` with `PlantError::Fenced`, while reads and plant tooling stay open. Every promotion — manual `promote` or `self_promote` — runs the claim before the gate lifts, refusing with `SwitchError::FieldClaimFailed` when the claim fails. Device kinds arbitrate through `DeviceBackend::claim`: `sim-tcp` installs the plant-server claim, `sim-bus` the device server's (#154 extends the rule to the register protocol — `claim_writer`/`release_writer` with the claim bound to its attachments, released on a holder's disconnect or explicit release so a dead owner frees the field for a promoted peer), and `FanoutDriver::unfenced_field_devices` names any field-facing device that cannot arbitrate — `--auto-promote N` refuses to start a model that has one, leaving manual promotion. `dcs-controller` wires both paced and `--driven` standby loops to count misses per scan cycle and self-promote when due.
- **Problem:** Promotion is operator- or test-triggered only: an active that dies leaves the field unwritten until someone posts `/promote` to the standby — a human in the loop the vision's hot-swap does not assume. Automating takeover needs recorded rules: how active loss is detected and on what cadence, when a standby may promote itself rather than wait, and how a merely *partitioned* old active is prevented from writing the field beside the new one — the fencing or split-brain rule.
- **Chosen approach:** The checkpoint pull is the heartbeat: a successful `GET /checkpoint` is proof of liveness, so no second channel is introduced. Cadence stays the implemented one — one pull per scan cycle — and loss is declared only after a configured budget of consecutive failed transfers: the budget, not the transport, is the new knob, sizing detection time in scan periods. Self-promotion stacks on the existing convergence gate: only a standby whose convergence proof still stands within the budget may promote itself; `Unsynchronized`, `Diverged`, or over-budget `Degraded` verdicts all block. The fencing rule: exactly one peer writes the field (decision 15), and a heartbeat loss is not proof of death — a partitioned active still believes it owns the field. The field is therefore the arbiter: the plant-facing protocol gains a single-writer claim — the plant server grants write ownership to one owner token at a time, so the promoted standby takes it and a still-alive old active's writes fail as `IoError`s instead of doubling the field writes. Where a field kind cannot enforce single-writer, automatic self-promotion is not enabled: the write gate still lifts on the manual path, but unattended takeover stays off — the split-brain risk is a documented deployment constraint, not a silent hole.
- **Alternatives considered:** A separate heartbeat channel (duplicates the liveness signal the checkpoint pull already carries, and can disagree with it — a peer can heartbeat while serving stale checkpoints); promotion on the first failed pull (network jitter becomes a takeover — the miss budget exists to absorb it); unconditional self-promotion after a timeout (split-brain by design: a partitioned active keeps writing); a third arbiter process or quorum (another deployed component M5 does not otherwise need — the shared field is already the natural arbiter).
- **Consequences:** Loss detection, promotion gating, and fencing become recorded, testable semantics instead of operational convention. The heartbeat rides existing machinery — one new configured budget plus one new protocol element (the writer claim). Fencing honesty is the cost: deployments whose field cannot arbitrate single-writer get manual failover only, and the documentation says so.
- **Affected tickets:** #70 (this record); #65 implements it and #154 extends the fencing to the sim-bus register protocol; relies on #32 (checkpoint pull and named transfer failure) and #46 (roles, gate, switch errors); #88 supplies the divergence half of the gate; decisions 12, 13, 15, and 19.

### 29. Device integration as a registered seam

- **Status:** Implemented — the `DriverRegistry` seam exists (#47): model `Device.kind` strings resolve to factories through `resolve_drivers`, producing a `DriverPlan` whose `build` yields a `FanoutDriver` presenting one `IoDriver` over every backend. `DriverRegistry::standard` installs three registrations covering both contribution shapes: the `sim*` prefix (each device's points join the shared local simulated channel map — the `DeviceDriver::Sim` fragment shape), the exact `sim-tcp` kind (a `RemoteDriver` attached to a `dcs-sim-net` `PlantServer` — the `DeviceDriver::Backend` shape with a step hook and an inspect handle, #31), and the exact `sim-scripted` kind (a `ScriptedDriver` replaying a tick-indexed script declared in the device's `parameters` — `Backend` shape, #67). The integration guide documents the path end to end (#71).
- **Problem:** Per the vision, "adding a device" must be model data, not per-layer engineering: declare the device, bind its channels to logical I/O, and obtain driver resolution, wiring checks, diagnostics, checkpoint participation, and monitoring access from the platform. Before #47 the `sim` backend was hard-coded into assembly, so every new device family would have edited the platform itself.
- **Chosen approach (implemented):** The seam is a registered factory `Fn(&DeviceSpec) -> Result<DeviceDriver, DeviceError>` keyed by the model's opaque `kind` string; exact registrations outrank prefix matches, which is how `sim-tcp` and `sim-scripted` route to their own backends while the rest of `sim*` stays local. `DeviceSpec` hands the factory everything the model declared — the device's id and actual kind string, its `parameters` as arbitrary JSON the factory alone validates, its channels, and the `DevicePoint`s model validation already guarantees agree with them. The factory returns `DeviceDriver::Sim(ChannelMap)` to merge into the shared local simulated backend, or `DeviceDriver::Backend { io, step, inspect }` for a self-contained kind — `step` an optional `StepHook` advancing a simulated backend per `FanoutDriver::step`, `inspect` an optional typed handle reaching beyond the `IoDriver` surface (the scripted driver's recorded-write log). Parameter and construction failures are `DeviceError::parameters`/`backend`, surfacing as `AssemblyError`s naming the device before any scan; an unregistered kind is `UnknownDeviceKind`. Cross-backend wiring becomes `FanoutDriver` routes applied at `step`, so one model can split a loop across device kinds.
- **Alternatives considered:** Hard-coded kind dispatch in assembly (the pre-#47 status quo — every device family edits platform code); per-kind fields in the model schema (splits device knowledge between schema and driver, and the schema would grow with every integration); link-time self-registration (rejected for components in decision 6 — kinds silently absent when unlinked — and the same reasoning applies to devices); driver kinds selected by `io_point` rather than by `Device` (points would carry transport detail — the device is the unit the model already declares).
- **Consequences:** "Adding a device" is: implement `IoDriver`, write the factory, register the kind — resolution, wiring checks, assembly-time error naming, `capture_state` participation (decision 13's per-kind choice), `step`/`inspect` hooks, and monitoring all arrive from the shared layers. The two contribution shapes are both proven: `sim*` merges, `sim-tcp`/`sim-scripted` stand alone. A real fieldbus kind follows the identical path — the seam does not distinguish simulated from physical backends, per decision 5.
- **Affected tickets:** #70 (this record); #47 implemented the registry, #31 the remote kind, #67 the scripted kind, #71 the guide; relies on #19 (the assembly seam) and decisions 5 and 6.

### 30. Plant-side simulation tooling outside the operator contract

- **Status:** Implemented — fault injection and explicit stepping live on the plant side, never on the operator surface: `SimDriver` carries `inject_fault`/`clear_fault`/`step` beside its `IoDriver` impl (#8); the `dcs-sim-net` plant protocol serves `step`, `inject_fault`, `clear_fault`, and `list_points` next to read/write (#31); `dcs-plant-ctl` exercises them as a command-line tool (#68); `dcs-plant-server` runs the shared plant as a process with `--dynamics` declarations merged beside the model (#81, decision 24); and `sim-scripted` replays declared tick-indexed field behavior from device parameters (#67).
- **Problem:** Developing, testing, and demonstrating the system requires perturbing the *world*, not the controller: inject a sensor fault, force a field input, advance plant time, replay a scenario. These are testbench actions on the simulated plant. Routing them through the operator `Command` path would corrupt the operator contract — a fault is the world misbehaving, not an operator action; it should not produce a command receipt, occupy the writability surface (decision 18), or read as an operator entry in the journal.
- **Chosen approach (implemented):** Plant-side tooling addresses the plant, not the controller. `dcs-plant-ctl` speaks the plant server's wire protocol directly — one subcommand per `PlantRequest` — and tests and rigs call the same `SimDriver` API in process. The controller observes only effects through the `IoDriver` boundary: an injected quality fault arrives as an `Uncertain`/`Bad` sample, an error fault as the point's `IoError` — indistinguishable from a real field fault, which is the point. Stepping is explicit and owned: the field-owning peer steps the shared plant once per scan and a standby never does, so plant time advances exactly once per scan regardless of pair membership (implemented in `dcs-controller`'s step closure consulting `owns_field`). Injected faults are part of the simulated plant's state: `SimDriver`'s checkpoint section carries them (decision 13), so a local-sim standby's tracking copy faults identically. Scripts and dynamics are declared device parameters and standalone documents — never `PlantModel` schema (decision 24's boundary).
- **Alternatives considered:** Fault injection through the `Command`/receipt path (corrupts the operator contract with testbench actions and would have to reach through the controller to a plant it does not own); scenario-driving only inside test code (no live demonstration or ad-hoc perturbation of a running pair — `dcs-plant-ctl` exists precisely for that); a private test-only protocol for the plant (blocks the standalone `dcs-plant-server` deployment shape and the second-controller-observes-same-plant property M3 needs); implicit plant time (a plant that advances on its own wall clock reintroduces the nondeterminism decision 4 excludes).
- **Consequences:** Two deliberately separate surfaces result: the operator contract (`Command`/`Receipt`, journal, role endpoints) stays clean of simulation concerns, while the plant protocol is a documented development surface — the monitoring page never offers fault injection because it is not an operator capability. The tooling composes with redundancy and determinism rather than bypassing them: faults checkpoint with driver state, stepping follows field ownership, and scripted scenarios replay identically on every run.
- **Affected tickets:** #70 (this record); #8, #31, #67, #68, and #81 implemented the surfaces; decisions 4, 13, and 24 supply the determinism, checkpoint, and declared-dynamics boundaries.

### 31. Typed model composition

- **Status:** Implemented (#112): the `dcs-build` crate carries the seam — `PlantBuilder` emitting exactly the versioned `PlantModel` document `dcs-model` loads, the per-kind specs in `dcs_build::specs` declaring each kind's string, typed port handles, and parameter set as data, `connect`/`bind` checking port direction and `ValueKind` at compile time through the typed handles, and `build()` reporting named errors for what types cannot carry; `dcs-blocks/tests/spec_drift.rs` pins the specs to the registered kinds' `describe()` output and `dcs-build/tests/tank_loop.rs` reproduces the checked-in tank-loop fixture. Implemented basis: the artifact being composed is already a plain serde document — `PlantModel`'s sections are public fields `PlantModel::validate` checks whether the model arrived as JSON or was built programmatically (#7, decision 3); every registered component kind already publishes its surface as a `ComponentDescriptor` — kind string, port names with direction and `ValueKind`, parameter names with kinds and ranges (#50, #61, decision 16) — and `dcs-assembly`'s `ComponentRegistry` resolves kind strings to constructors (#19, decision 6). Nothing emits a model from Rust today: composition is a hand-written or tool-written JSON document whose port names, directions, value kinds, required parameters, and channel references are all checked only at load.
- **Problem:** The vision asks that an engineer compose plant software in Rust with the contracts expressed through types and enforced at compile time as far as possible. As implemented, a typo'd port name, a `Float` port wired to a `Bool` point, or a missing required parameter is a `ValidationError`/`AssemblyError` discovered when the document loads — never a compile error — and every model edit round-trips through JSON even when the author is writing Rust anyway.
- **Chosen approach (implemented):** A new workspace crate `dcs-build`, depending on `dcs-core` and `dcs-model` only — never `dcs-blocks` or `dcs-runtime`, so the seam stays a model producer with no runtime entanglement. Two halves: per-kind *specs* and a *model builder*. A spec is a data struct per registered kind — declared beside the kind's descriptor vocabulary — carrying its kind string, its named ports as typed handles (name, direction, `ValueKind`), and its parameter set; registering a component from its spec returns an instance whose ports are typed references, and `connect`/`bind` calls check the endpoints' direction and value kind at compile time where the kind is statically known. What types cannot express — resolvable ids, required parameters, channel references — `build()` checks and reports as named errors naming the offending element. `build()` emits the identical versioned `PlantModel` document `dcs-model` loads: the builder is a producer of the one contract, not a second schema, and `validate` remains the backstop for graph-level rules. Because specs are data kept in sync with the registered kinds' descriptors by convention, a checked drift test comparing specs against each kind's `describe()` output is part of the seam, and a worked example reproduces the checked-in tank-loop model.
- **Alternatives considered:** A builder API inside `dcs-model` (the schema crate is deliberately serialization-plus-validation only — decision 6 keeps `dcs-model` free of runtime knowledge, and per-kind spec data is kind knowledge; a separate crate also keeps `dcs-model` small for non-Rust tooling that only parses the document); having `dcs-blocks` kinds emit builders themselves (adds a `dcs-model` dependency to the component library and couples every kind crate to the document format — spec data in `dcs-build` keeps the dependency direction one-way); a proc-macro or external DSL (more machinery than the problem needs — ordinary generics and typed handles already carry the compile-time checks); building documents with `serde_json::json!` (the de facto route tests use today — no checks beyond load-time validation).
- **Consequences:** Two authoring surfaces produce one artifact: a Rust-composed plant serde-roundtrips, loads, validates, and assembles identically to a hand-written document, so versioning, diffs, and tooling are unchanged. Compile-time enforcement covers port existence, direction, and value kind — the checks types can carry; everything else stays a named build-time or validation error, and the boundary between the two is explicit in the spec's types. Spec-versus-descriptor drift is the standing cost: a kind that evolves without its spec is caught by the drift test, and a kind with no spec fails `build()` by name rather than silently compiling a weaker check.
- **Affected tickets:** #104 (this record); #112 implements it; relies on #7 (the document), #19 (the registry the emitted model feeds), and #50/#61 (descriptors as the spec's source of truth); decisions 3, 6, and 16.

### 32. A register-mapped fieldbus device kind

- **Status:** Implemented (#113): `dcs-sim-bus` serves a `RegisterBank` over a length-prefixed binary TCP protocol — per-register read and write, `list_registers`, and an explicit `step` advancing the bank's logical tick — `BusDriver` implements `IoDriver` translating each `PointId` through its channel's declared register and mapping the wire's error vocabulary onto `IoError` (`UnknownPoint`, `TypeMismatch`, `Disconnected`, `Timeout`), `dcs-sim-bus-device` is the device-server binary, `SIM_BUS_KIND` registers in `DriverRegistry::standard` with `DeviceParameters` declaring the server address and each channel's register index — the first kind where `DeviceSpec.channels`/`parameters` do real addressing work — and the `field_facing` backend contributes its `StepHook` and `None` capture under `FanoutDriver` beside `sim`, `sim-tcp`, and `sim-scripted` (the `mixed_bus.json` fixture proves the mixed-kind fan-out). #213 extends it with the declared-dynamics seam: the bank is a `SimDriver` whose point ids are the register addresses, `dcs-sim-bus-device --dynamics FILE` merges the same `ProcessElement` declaration list `dcs-plant-server --dynamics` serves (decision 24) with the element's point-valued fields carrying register addresses — the recorded binding encoding — and `step` carries the caller's `dt` on the wire, advancing the merged elements in lockstep under the same named `dt` validation the plant protocol applies. It was built on implemented machinery: the `IoDriver` boundary (#6, decision 5), the `DriverRegistry`/`DeviceSpec`/`DeviceBackend` seam (#47, decision 29), the one remote transport — `dcs-sim-net`'s `PlantServer`/`RemoteDriver` (#31) — `FanoutDriver` routing with `StepHook` and `field_facing` (#47, #64), and explicit, field-owned stepping (decision 30).
- **Problem:** The hardware-abstraction claim — the same component runs against any field interface unchanged — is so far exercised by exactly one remote transport, and it is a friendly one: the `dcs-sim-net` plant protocol addresses `PointId`s in the controller's own logical space and carries `IoError`s verbatim, so it validates the seam's mechanics without stress-testing the translation a real fieldbus needs. A real field interface is register-mapped: device-local numeric addresses, polled or batched exchanges, and a bus-level failure vocabulary. A second transport shaped that way is what proves the boundary is a boundary.
- **Chosen approach (implemented):** A new workspace crate `dcs-sim-bus` carrying a documented register-mapped wire protocol over TCP — deliberately distinct from the line-JSON plant protocol — a `BusDriver` implementing `IoDriver` against a device server, the server binary for integration rigs, and a `sim-bus` device kind registered in `DriverRegistry` so one model mixes `sim`, `sim-tcp`, `sim-scripted`, and `sim-bus` channels under `FanoutDriver`. The three places it differs from `sim-tcp`, and what stays shared:
  - *Addressing.* `sim-tcp`'s wire speaks `PointId`s: the plant server is configured with the model's own point ids and the factory merely probes that each declared point is served — `DeviceSpec.channels` is effectively nominal there. A `sim-bus` device's `parameters` carry real addressing: the server endpoint plus each declared channel's register index — the first kind where `DeviceSpec.channels`/`parameters` do addressing work the model guarantees the shape of and the factory validates. The `IoDriver` surface stays `PointId`/`Value`; the driver translates point → channel → register internally, and a malformed register map is `DeviceError::Parameters` surfacing as `AssemblyError::InvalidDeviceParameters` naming the device.
  - *Error mapping.* The plant protocol returns the server's own `IoError` (`PlantError::Io`) because the remote is itself a `SimDriver`; transport failures collapse to `Disconnected`/`Timeout` at the requesting point. A register bus has its own failure vocabulary the driver owns the mapping for: no answer → `Timeout`, dead link → `Disconnected`, an unmapped register → `UnknownPoint`, a kind- or width-mismatched write → `TypeMismatch`. Polled register ranges mean one failed exchange faults every point the poll covered — each covered point reports the same named failure, and transport-level health reports through the optional `IoDriver::diagnostics` hook (#83), so a dead bus reads as link degradation, not scattered per-point faults.
  - *Step ownership.* Identical rule to `sim-tcp`: the register file is the shared field — server-side state, not the driver's — so the backend is `field_facing: true`, contributes a `StepHook` forwarding `step(dt)` to the device server, leaves `capture_state` at its `None` default (a standby observes the actual registers, per decision 13), and is stepped once per scan by the field-owning peer while `FanoutDriver::step_local` leaves its clock alone on a tracking peer (decision 30). What stays shared through the boundary is everything decision 5 promises: `read`/`write` over `PointId` with declared-kind checks, the `DeviceBackend` contribution shape, write-gate quiescing of field-facing points, fan-out routing, and assembly-time error naming.
  - *Declared dynamics.* `dcs-sim-bus-device --dynamics FILE` merges a JSON list of `ProcessElement` declarations — the identical serde vocabulary decision 24 records — over the bank's registers rather than logical points: the recorded binding encoding carries register addresses in the element's point-valued fields, so a document's `"input": 20` reads register 20. Each element validates as it merges, so a rejection names the element's position and the register it drives. Element behavior is `SimDriver`'s unchanged — caller-supplied `dt`, the documented non-`Good` input rule per variant (the #197 quality-injection path exercises it), named errors for non-finite or negative parameters and wrongly-kinded endpoints — because the bank literally is a `SimDriver` keyed by register. Element state lives in the bank for the bank's lifetime: it is shared field state exactly like the `dcs-sim-net` plant's, never checkpointed controller state — `BusDriver::capture_state` stays `None`, and a tracking standby observes the stepped registers rather than restoring a process copy.
- **Alternatives considered:** Extending the plant protocol with register operations (one protocol serving two address shapes proves nothing about foreign transports — the seam's value is that the second kind needs no contract change); encoding register addresses in channel names (loses the validated, kind-checked parameter surface `DeviceSpec` already provides); a controller-local register image stepped like a `sim` backend (`field_facing: false` — a loopback, not a transport; the field-observing shape is what exercises the redundancy and step-ownership semantics); a per-point request protocol like the plant's (misses the batch-failure semantics a polled bus actually has — the mapping must account for one failed poll faulting a whole range).
- **Consequences:** Device `parameters` become the documented addressing contract for real transports — opaque to the schema, validated by the kind's factory at assembly, naming the device on failure. Components and the executor cannot distinguish `sim-bus` from `sim-tcp` except through diagnostics — which is precisely the property the milestone exists to demonstrate. A second `field_facing` kind also exercises the write gate, `step_local`, and fan-out routing under a genuinely shared field rather than a single-remote shape.
- **Affected tickets:** #104 (this record); #113 implements it and #213 adds the declared-dynamics seam; relies on #31 (the first transport), #47 (the registry and fan-out), and #64 (field-ownership rules); #83 supplies the diagnostics hook it reports link health through; decisions 5, 13, 24, 29, and 30.

### 33. Alarm acknowledgment

- **Status:** Implemented (#109, filed under M6): the `latching-alarm` kind declares `in` (`Float`, `In`), `ack` (`Bool`, `In`), `alarm` (`Bool`, `Out` — the standing limit state under `AlarmMonitor`'s hysteresis rule exactly), and `unacknowledged` (`Bool`, `Out` — latched on a fresh trip, cleared on every scan `ack` reads `true`, never clearing `alarm` itself); the model wires a writable channel-less `In` point to `ack` so acknowledgment rides the ordinary receipted `WriteValue` path and journals as `CommandSettled`, and the latch rides `capture_state` as `unacknowledged` so a tracking standby inherits it. Implemented basis: `AlarmMonitor` (#22) is strictly auto-clearing — its `alarm` output follows the documented hysteresis rule and carries no notion of operator awareness; channel-less internal `In` points hold operator values and accept `Command::WriteValue` at the scan boundary through the receipt-answered path (#30, #20, decisions 14 and 7); the `writable` flag narrowing that surface to model-declared `In` points is implemented (#49, decision 18); and the journal already records settled receipts and point quality transitions (#35, decision 17).
- **Problem:** Auto-clear alarms answer "is the process outside limits now" but not "did anyone see it": a transient excursion trips and clears before an operator looks, leaving no acknowledged record in history or journal. The missing vocabulary is the standard two-flag alarm model — a *standing* indication reporting process truth plus a *latched* unacknowledged flag reporting operator visibility — and a recorded answer to where the acknowledge action rides: a declared `ack` input the model wires, or a dedicated `Command` variant.
- **Chosen approach (implemented):** Latching is per-kind declared behavior, not a mode of the existing kind: `alarm-monitor` keeps its auto-clear semantics and a sibling latching kind (#109) declares `in` (`Float`, `In`), `ack` (`Bool`, `In`), `alarm` (`Bool`, `Out` — the standing limit state, following `AlarmMonitor`'s hysteresis rule exactly), and `unacknowledged` (`Bool`, `Out` — set when the limit state trips, held until acknowledged). The ack rides the path already built for operator values: the model wires a channel-less `In` point declared `writable` (decision 18, #49) to the `ack` port, and the operator acknowledges with `Command::WriteValue` on that point — a writable internal point holds the written value until the next command, which is what makes the level-observed ack below well-defined. No new `Command` variant and no new `Component` hook: an ack is an operator value like a setpoint (decision 14), so it inherits the receipt contract, the writability surface, telemetry visibility, and checkpoint carryover through the `internal` section for free. `ack` is level-observed like every input: while it reads `true` the `unacknowledged` latch releases — a standing alarm then reads as acknowledged-but-active, which is the two-flag model's point — and a fresh trip after the latch cleared re-asserts it; the kind documents its precise edge cases per #109's convention. Journaled: the ack write settles as `JournalEvent::CommandSettled` naming the ack point — the identical audit every operator action gets — while `unacknowledged` and `alarm` transitions appear in the points' history streams; no dedicated alarm event type is added. The latch is run state: `capture_state` carries it so a tracking standby inherits unacknowledged alarms — decision 20's checkpoint obligation applied to latched state.
- **Alternatives considered:** A dedicated `Command::Acknowledge { component }` variant with a `Component::acknowledge` hook (a component-targeted command carrying an empty payload — it duplicates what a point write expresses, needs a new hook plus a journal event to stay as auditable, and names only a component, so a kind raising several alarms could not say which was acked); auto-clear as the only behavior (loses the operator-visibility record entirely — a transient alarm leaves no trace it stood); making `alarm` itself latch until ack (conflates the two flags — the standing indication would report operator state, not process truth); edge-triggered ack requiring the component to observe the `false`→`true` transition (workable, but level semantics make a checkpoint-restored `ack` point's meaning obvious — `true` means the acknowledgment is held).
- **Consequences:** Acknowledgment needs no contract extension — a model-declared point, an ordinary command, a journaled receipt — and the page's ack affordance is a writable-point edit like any other, so #108's alarm summary pane consumes `alarm` and `unacknowledged` as ordinary status-role ports. The two-flag split keeps process truth and operator acknowledgment independently visible in telemetry, history, and checkpoints. The `writable` mark makes the ack surface explicit model data: an unmarked `In` point rejects `WriteValue` with `CommandError::NotWritable`, so the ack affordance exists only where the model declares it.
- **Affected tickets:** #104 (this record); #109 implements the latching kind; relies on #22 (the auto-clear baseline), #30 (internal points), and #20 (the command path); #49 supplies the writable surface; #108 consumes the flags; decisions 7, 14, 17, 18, and 20.

### 34. Faceplate parameter display

- **Status:** Implemented. `ParameterDescriptor` serves each tunable's name, `ValueKind`, and declared `ParameterRange` — the editing authority (#50, #61, decision 16) — and `Command::SetParameter` applies a tune at the scan boundary with named rejections and a receipt, tuned values riding the checkpoint (#82, decision 20). Under #105 (M4) the descriptor-driven faceplate renders edit controls typed and range-labeled from the descriptor and submits through the receipted path. Implemented under #132: `TelemetrySnapshot.parameters` carries one `ComponentParameters` per registered component in scan order — `name` plus a `values` map of exactly the descriptor-declared parameter names to their current `Value`s — fed by the `Component::report_parameters` hook whose default reports nothing so kinds without tunables are unaffected. Every `dcs-blocks` kind declaring parameters reports them from the same fields `capture_state` checkpoints, and the executor filters each report to the descriptor's declared names, keeping the descriptor the surface's sole authority.
- **Problem:** An editable faceplate without the current value is a blind edit: the operator cannot see what `kp` is before retuning it, cannot confirm a tune persisted except through downstream behavior, and cannot see a value a standby inherited or a checkpoint restored. Yet current values are run state, not static metadata — putting them on `ComponentDescriptor` would corrupt the descriptor's role (decision 16) and make the same instance's descriptor change between snapshots.
- **Chosen approach:** Record the division of authority: the descriptor owns the *editable surface* — name, kind, range — and current values are telemetry belonging on the snapshot. Implemented under #132: `TelemetrySnapshot` gained a per-component current-parameter section carrying `name → Value` for exactly the descriptor-declared parameters, fed by the `Component::report_parameters` hook whose default reports nothing so kinds without tunables are unaffected. The hook reports from the same fields `capture_state` checkpoints — one vocabulary feeds the standby and the faceplate — and the executor filters each report to declared names only, keeping the descriptor the surface's sole authority. The page then joins descriptor plus current value plus edit affordance exactly as it joins descriptor plus live points today — that rendering half remains follow-up work. The interim accepted for #105 — the faceplate rendering the declared metadata and submitting through the receipted path — was the first cut, not the end state.
- **Alternatives considered:** Display-only descriptors as the permanent answer (blind editing — the receipt confirms an apply but nothing shows what was there before or what a restored standby carries); declaring each tunable as an internal `In`/`Out` point pair (doubles the representation — every parameter needs a declared point, wiring, and component code reading it, duplicating the parameter map the kind already owns); serving the model document to the page (construction-time values only — it misses runtime tuning, the exact case that makes display matter); folding current values into `ParameterDescriptor` (breaks the descriptor's static-metadata contract — the type is kind-level metadata, not per-tick state).
- **Consequences:** Tuned state becomes first-class telemetry the page, tests, and tooling read identically. The reporting hook adds a per-kind obligation shaped like `apply_parameter`'s checkpoint obligation — report declared names only, from the same fields the checkpoint captures — keeping descriptor, live display, and standby coherent by construction. The snapshot grows by each component's declared parameter count: bounded, static in shape, and absent for kinds declaring none.
- **Affected tickets:** #104 (this record); #105 landed the declared-metadata edit surface; #132 implemented the current-value section; relies on #82 (tuning and checkpoint carryover) and #50/#61 (descriptors); decisions 16 and 20.

### 35. Checkpoint persistence for restart recovery

- **Status:** Implemented (#115): `dcs-controller --state-file PATH` writes the serde `Checkpoint` — the same versioned, fingerprinted contract `GET /checkpoint` serves (decisions 11, 27) — at the end of every completed scan cycle, after the scan and the plant step, by write-then-rename to a sibling `PATH.tmp`. When `PATH` exists at startup the run resumes: the checkpoint is applied in place to the freshly assembled executor (`Executor::apply`) before pacing begins; a missing file is a cold start. Every resume failure — unreadable or unparseable file, unsupported format version, fingerprint or structural mismatch — exits nonzero naming the file and the reason.
- **Problem:** The checkpoint machinery moves a run between *peers* (decisions 11–13): a lone controller restart has no standby to converge from and would cold-start, interrupting the process the vision wants uninterrupted. The checkpoint format already carries everything a continuation needs — tick, component state, image, and driver-side state (decision 13) — so persisting it locally is the no-peer recovery story.
- **Chosen approach (implemented):** Reuse the one checkpoint contract rather than a second persistence format: the file holds exactly the JSON `Checkpoint` a standby pulls, so the negotiation a peer performs — `format_version` against `SUPPORTED_FORMAT_VERSIONS`, `model_fingerprint` equality, then decision 11's structural checks — is the same negotiation a restart performs, with the same named failures. The recorded boundary is the end of each scan cycle, after `step` has run: the checkpoint then captures the post-step driver state the next scan's input phase reads, so a resumed run re-enters the loop at the point the capture was taken — writing mid-cycle would resume with the driver one step behind. Writes are atomic (temporary sibling plus rename) so a crash mid-write cannot leave a torn file that a later resume must reject. Resume is automatic on the file's presence — no separate flag — because the flag's purpose is recovery: a restarted process given the same command line resumes, while a missing file is an unambiguous cold start. Restore is all-or-nothing per decision 11: a version or fingerprint mismatch, a corrupt file, or a changed model exits nonzero naming the reason rather than silently starting fresh — a state file that exists but cannot be resumed is a fault to surface, never a hint to ignore. The choice also extends to the `--driven` mode, where the write rides the per-requested-scan `after_scan` hook — now receiving the `Peer` — at the same end-of-cycle boundary.
- **Alternatives considered:** A separate `--resume` flag instead of resume-on-existence (a restarted process would need its command line changed to recover, and a stale `--resume` with no file is its own failure mode — existence is the honest signal); checkpointing every N scans or only on each checkpoint the monitor serves (both leave the persisted state behind the run by a configurable or request-driven amount — every cycle is the simplest boundary that loses at most the in-flight cycle); a bespoke run-state document instead of the `Checkpoint` (a second versioned contract to keep compatible with restore semantics forever).
- **Consequences:** Restart recovery needs no peer: the process dies, restarts with the same arguments, and continues at the last completed cycle — determinism makes the continuation identical to the uninterrupted run. The standby path is unchanged and remains the preferred recovery where redundancy exists: a tracking peer converges continuously and takes over without a restart, while the state file resumes only from the last persisted cycle after the process has died. What the checkpoint does not carry — receipt history, I/O-health counters, journal and history buffers — restarts fresh; the file recovers the run, not its diagnostics. A state file is bound to its model by fingerprint: a changed model fails resume by name, per the all-or-nothing rule.
- **Affected tickets:** #115 implemented it; relies on #21 (the checkpoint contract and all-or-nothing restore), #66 (version and fingerprint negotiation), and #64 (the driven-mode scan boundary); decisions 11, 12, 13, and 27.

### 36. Journal persistence beyond restart

- **Status:** Implemented (#163): `MonitorConfig::journal_file` — wired on the controller as `--journal-file PATH`, requiring `--listen` since the recorder lives in the monitor — appends every journaled entry to `PATH` as a line-delimited `{"entry":{...JournalEntry...}}` record at the recorder's documented point, replays the file at bind to seed the ring and continue `seq` numbering, and opens each process lifetime with a `{"run_boundary":{"run":N,"tick":T}}` marker — `tick` recording the tick the run starts at, so a `--state-file` resume's restored tick domain is visible in the file. A record that cannot be replayed fails startup naming the file, the line, and the offending record; a missing file is a cold start; an append the file cannot take is fatal rather than a silent gap in the audit trail. Implemented basis: `dcs-monitor`'s `Recorder` already appends to the bounded per-point history rings and the transition journal once per completed scan — the documented recording point after the write phase, never mid-scan — with oldest-first eviction and never-reused `seq`s making eviction visible (#35, decision 17); `JournalEntry`/`JournalEvent`/`HistorySample` are serde contract types served over `GET /journal` and `GET /history`; and the checkpoint state file (#115, decision 35) gives a lone controller restart its resume story — its consequence section naming exactly this gap: "the file recovers the run, not its diagnostics". Point history remains volatile per decision 17; only the journal persists.
- **Problem:** The journal is the run's audit record — settled commands, quality transitions, step failures, role changes, divergence verdicts — and it lives only in the bounded in-memory ring. A controller restart drops it: the operator-action trail the receipts and journal exist to provide (decisions 7, 17) resets with the process, and a post-incident review of who commanded what cannot reach back across a restart. Decision 35's checkpoint file resumes the run's *state*; nothing preserves the run's *record*.
- **Chosen approach (implemented):** A file-backed journal sink as monitor-local tooling, not a contract concern. When a path is configured on the controller or monitor, every entry the `Recorder` appends to the transition journal is also appended to a line-delimited JSON file at the same recording point — after the write phase, under the monitor lock, never mid-scan. On startup the monitor replays the file to seed the in-memory ring and continue `seq` numbering where it left off, so `GET /journal` answers continuously across a restart and a consumer still sees a retained gap as a numbering gap. The file reuses the contract's serde serialization but is the monitor's own format — its readers are tooling and the replaying monitor itself, not contract consumers — and a run-boundary marker record distinguishes process lifetimes in one file. Point *history* stays volatile per decision 17's original reasoning: it is high-volume diagnostics that re-accumulates after a restart, where the journal is the low-volume record that cannot be reconstructed. Interaction with decision 35's state file: the two are deliberately separate — the checkpoint file is state to resume a run, overwritten per save and consumed by restore; the journal file is the append-only record of what happened, consumed by review. A run resumed through `--state-file` continues appending to the same journal file in the restored tick domain, so the audit record spans the restart the state file healed.
- **Alternatives considered:** Persistence as contract — a `JournalSink` trait or mandated file schema in `dcs-core` (elevates an operations tool to wire contract every consumer must version, for a payload that is already contract-serialized); folding the journal into the checkpoint file (conflates resumable state with audit record — the checkpoint's overwrite-per-save shape fights append-only history, and a standby pulling checkpoints would drag a growing record); an embedded database or external log shipper (operational machinery the single-binary deployment does not otherwise need); persisting history samples too (volume and re-accumulation argue against it — the ring is a diagnostic window, not an archive).
- **Consequences:** The audit record survives restarts with no contract churn: endpoints, event types, and payloads are unchanged, and the ring stays memory-bounded while the file is the durable tail — file growth is a deployment-managed bound, not the contract's. Determinism is unaffected since appends ride the existing recording point. The choice is reversible: because the file serializes contract types, a later contract-level persistence story can adopt the same payloads.
- **Affected tickets:** #137 (this record); #163 implements it; interacts with #115/decision 35 (the sibling persistence surface, implemented); relies on #35 (the recorded streams) and decisions 7, 8, and 17.

### 37. Plant-wide supervision

- **Status:** Implemented (#164) — the page's overview mode is configured by repeated `?pair=<name>=<host:port>,<host:port>` URL parameters, the implementing ticket's recorded parameter shape: each parameter names one pair outright — every peer listed in the same `host:port` convention `?peer` uses, the serving origin not implied — with an optional display name before the `=`, the first peer's address standing in when absent. Implemented basis: the pair-as-one-controller view is implemented — `PairClient` polls both peers' `GET /role`, reads every data endpoint from the peer reporting settled `active`, routes commands active-only with mid-transition retry, and reports an unreachable peer as the named `PeerStatus::Unreachable` redundancy fault (#63, decision 19); the page is configured by `?peer=<host:port>` URL parameters beside its serving origin, and the JSON endpoints carry `Access-Control-Allow-Origin` for cross-origin reads.
- **Problem:** A plant is more than one control loop: several controller pairs, each running its own model and serving its own monitor, operate one process. Today supervising a second pair means a second browser tab with its own `?peer` set — nothing presents the plant as a whole, and there is no recorded answer to whether plant-wide aggregation is page-side presentation or a served contract some process must own.
- **Chosen approach (implemented):** Aggregation stays page-side; the served contract remains per-controller. The page gains an overview mode configured by repeated pair-naming URL parameters in the same `?peer` convention, rendering one summary card per configured pair — the active peer's name, the reported roles and convergence, the unreachable-peer redundancy fault, and the I/O-health line — each card linking into the existing per-pair view for faceplates, trends, and commands. Plant topology — which pairs exist and what they are called — is deployment configuration carried in URLs and bookmarked links, not plant-model data: the model describes one controller's view, and nothing above it needs a new contract type. Cross-origin reads already enabled on the JSON endpoints let a page served by one monitor poll every configured pair.
- **Alternatives considered:** A served plant-aggregation contract — a `GET /plant` endpoint rolling up every pair (requires a process owning plant-wide state: either a new supervisor binary aggregating monitors, or monitors made aware of controllers beyond their pair — a second authority beside the per-pair contract for data already fetchable pair-wise); a dedicated supervisor process (a deployed component M8 does not otherwise need — the same reasoning that rejected a brokered bus in decision 12); stretching one `?peer` list across all controllers on the existing page (conflates pairs: the page would merge distinct tick domains and command targets into one faceplate set); plant-wide sections in the model schema (the model is per-pair engineered data; topology is deployment knowledge).
- **Consequences:** Plant-wide supervision is presentation, not contract: each pair's served surface stays the single authority for its loop, the overview degrades per pair — an unreachable pair is a card fault, not a page fault — and no new process or wire type enters the system. The cost is client-side polling per pair, bounded by the configured pair count and the existing cadence. A future served roll-up remains open: the per-pair contract types are already what it would serialize.
- **Affected tickets:** #137 (this record); the M8 overview-page ticket implements it; relies on #63 (the pair view) and #34/#51 (the page and its panes); decisions 8, 17, and 19.

### 38. Alarm lifecycle depth

- **Status:** Decided — the two-flag model stands as recorded in decision 33; no implementation ticket. Implemented basis: `latching-alarm` (#109) landed the two-flag semantics — `alarm` reporting standing limit state under `AlarmMonitor`'s hysteresis rule, `unacknowledged` latching on a fresh trip and clearing while the model-wired writable `ack` point reads `true` — acks journaled through the ordinary settled receipt and the latch carried in checkpoints. `AlarmMonitor` (#22) stays the auto-clearing sibling, and the alarm summary pane consuming the flags is filed as #108.
- **Problem:** Industrial alarm practice carries more lifecycle than acknowledgment: *shelving* (operator-temporary removal with automatic return), *suppression* (design- or state-based exclusion), and *alarm classes* (priority or severity grouping for display and response). M8 must decide whether the two-flag model grows these now or whether the recorded model is the lifecycle's extent.
- **Chosen approach (decided):** The model stays as decided — `alarm` plus `unacknowledged` plus the writable `ack` point is the whole alarm lifecycle for M8. Each candidate extension is per-kind declared behavior needing no contract change, so nothing forces a decision now: a class would be a declared parameter or descriptor metadata on an alarm kind, shelving a stateful kind holding a shelved-for duration in ticks, and suppression-by-state is already expressible through existing interlock wiring gating the `in` port's observed value (decision 14 links). The platform's convention — new behavior lands as a new or extended kind beside descriptors and checkpoint vocabulary — covers each extension when a real plant's operations demand it, and #108's summary pane proceeds on the two flags alone.
- **Alternatives considered:** Adding shelving now (a timer-bearing kind and a `shelve` writable point are buildable on the same machinery, but invent lifecycle depth no consuming surface yet renders); alarm classes as contract metadata — a `severity` field on the descriptor or signal (premature vocabulary for one alarm kind); suppression as a `Command` variant (suppression is engineered or derived state, not an ad-hoc command — and decision 30 keeps testbench-shaped controls off the operator path); dedicated alarm-lifecycle journal event types (rejected with decision 33 — transitions already appear in point history and the ack receipt).
- **Consequences:** The alarm contract stays minimal and every alarm kind speaks the same two-flag language the pane, history, and checkpoints already carry. The deferral is recorded rather than silent: when shelving or classes arrive they arrive as per-kind declarations — the same seam #109 used — so the extension path is proven, not speculative. Operators get one consistent acknowledgment semantic in the interim.
- **Affected tickets:** #137 (this record); #108 consumes the decided model; #109 implemented it; decision 33 records the semantics; decisions 16, 17, and 20 supply the descriptor, journal, and checkpoint machinery.

### 39. Command-path audit identity

- **Status:** Accepted and implemented (#165): `CommandReceipt` carries a serde-optional `actor` beside `command`/`outcome`; `POST /command` accepts the attributed envelope `{"command":…,"actor":…}` — the wire-shape choice this decision left to the ticket — beside the still-accepted bare `Command`; `Executor::submit_command_as`/`Peer::submit_command_as` are the attributed submit variants, and the monitor's `not_active` refusal stamps the actor identically. The page sources the identity from its `?operator=<name>` URL parameter — the page-side source choice — and submits the envelope on every command; `dcs-ctl` declares it through `--actor <name>` on the receipted subcommands with `DCS_ACTOR` as the configured default the flag overrides (#175) — the tool-side source choice — while `promote`/`demote` stay unattributed: the switch-request path carries no actor field. Implemented basis: every submitted `Command` settles into exactly one `CommandReceipt` — echoed command plus outcome (#20, decision 7) — journaled as `JournalEvent::CommandSettled` carrying that receipt (#35, decision 17).
- **Problem:** The vision names safety-related constraints among the platform's encapsulated concerns, and audit's first element is actor identity: the journal records that a setpoint moved at a tick but not who asked. `POST /command` is anonymous as implemented — a receipt cannot distinguish one console's write from another's, and the durable journal of decision 36 would preserve an unattributed record.
- **Chosen approach (implemented):** Operator commands carry a declared actor identity, journaled for audit. The `POST /command` submission accepts an optional `actor` — a free-form identity string the submitter supplies — carried through onto the `CommandReceipt` as a serde-optional field beside `command`/`outcome`, so existing payloads and receipts parse unchanged and the journaled `CommandSettled` entry gains the attribution for free by echoing the receipt it already carries. The implementing ticket recorded the envelope wire shape — `{"command": <Command>, "actor": "<identity>"}` beside the bare `Command` body, selected by either envelope key so a stray top-level `actor` on a bare command is a `400` rather than a silently dropped attribution. The page sources the identity from its `?operator=<name>` URL parameter and includes it on every submission; an absent actor journals as unattributed rather than rejected. Recorded honestly: this is *declared* identity — attestation, not authentication. The simulated transport has no auth layer; verifying an actor is deployment machinery (a fronting proxy or gateway) the contract deliberately does not prescribe. The field still earns its place: it makes attribution part of the receipt-answered contract, journaled at the scan boundary like every other audit entry, and a deployment that fronts the monitor can fill it from authenticated context without a contract change.
- **Alternatives considered:** An authenticated transport first (identity without verification is weaker — but authentication is a cross-cutting deployment concern: folding it into the command contract now couples the audit record to a mechanism the simulated system cannot exercise, while declared identity leaves the seam exactly where a deployment needs it); a separate audit API beside the journal (splits the run's ordered record — attribution belongs on the settled receipt the journal already carries); `actor` on `Command` itself (the enum's externally-tagged serde shape gains the field awkwardly, and identity is submission metadata — about who sent it, not part of the operation the executor applies); per-endpoint tokens mapped to identities (authentication by another name — the same deferral).
- **Consequences:** The audit trail gains attribution with no new event types, endpoints, or required fields — an old client's commands journal exactly as before, actorless. Checkpoint and peer traffic are unaffected: the actor lives on the submission and receipt path, not in executor state. The honest limit is recorded: until an authenticating front exists, actor strings are operator-declared and the journal proves *what was claimed*, not *who truly sent it* — sufficient for the simulated system and the documented extension point for a hardened deployment.
- **Affected tickets:** #137 (this record); #165 implemented it; relies on #20 (receipts), #35 (the journal), and #163 (the durable record it attributes); decisions 7, 8, and 17.

### 40. JSON Schema for the plant-model document

- **Status:** Accepted and implemented (#159): `dcs-model schema` prints the draft 2020-12 JSON Schema `PlantModel::json_schema` emits, and `crates/dcs-model/tests/schema.rs` sweeps the checked fixture corpus against it.
- **Problem:** Decision 3 chose JSON so non-Rust tooling can read the model, but nothing external could check a document without linking the Rust validator. The model is the shared contract, so its checkable surface should travel with the document format, not only with `PlantModel::load`.
- **Chosen approach (implemented):** The schema is *emitted as data* — a hand-maintained document in `dcs-model`'s `schema` module, not generated from the serde types (e.g. via `schemars`). Hand authorship is deliberate: the schema intentionally diverges from deserialization in both directions, and deriving it from the types would lose the divergences that make it useful. The schema is *stricter* than serde where that serves engineering: `additionalProperties: false` everywhere (serde silently ignores unknown keys, so a misspelled field would evaporate), `version` as a `const` stamped from `MODEL_VERSION`, and `uniqueItems` on the four id-keyed collections (a sound approximation of id uniqueness — identical elements necessarily share an id, so nothing valid is rejected). It also expresses the intra-element rules `validate` owns but the type shape alone does not: an internal point's required `initial`, the bound-point ban on `initial`, `initial`'s variant matching `value_type`, and `writable` restricted to `In` points — plus the standard registry's known device-kind parameter shapes where conditional (`if`/`then`) subschemas can write them down: `sim-tcp`'s `address`/`timeout_ms`, `sim-bus`'s `registers` map, and `sim-scripted`'s `script` entries. `Option` fields admit `null`, matching serde. The drift check runs every checked-in `*.json` fixture through the emitted schema with a dev-dependency validator (the `jsonschema` crate): valid model documents — including their serde re-serializations — must pass, non-model documents must fail, and every `invalid/` fixture carries a pinned per-layer verdict so a schema or validator change flips a checked expectation instead of drifting silently.
- **Alternatives considered:** Generating the schema from the serde types (schema-on-derive cannot express the conditional intra-element rules, would mirror serde's silent unknown-key acceptance, and adds a derive dependency to `dcs-model` and `dcs-core` — rejected); a checked-in `.schema.json` file the CLI prints verbatim (equivalent, but the embedded document keeps `MODEL_VERSION` stamped from code and one canonical source; external tools fetch it through the subcommand); encoding the `sim*` prefix family's no-parameters rule (rejected — the open prefix cannot be pinned per-kind, and a checked-in valid document carries `sim*` parameters the standard factory would reject only at assembly, proving the constraint is not document-shaped).
- **Consequences:** What the schema cannot express is recorded rather than implied: cross-reference checks (channel bindings, signal `source`, connection endpoints naming declared elements), wiring checks (`from` produces / `to` consumes / kinds agree — they depend on the *referenced* element's declarations), general id uniqueness beyond identical elements, the semantic halves of the device-kind contracts (resolvable addresses, register-map coverage and uniqueness, script channel binding and tick ordering, script values matching channel kinds), and JSON's integer/float lexical distinction (schema compares numbers mathematically, so `1.0` passes an integer field serde rejects). Those stay with `load` and the assembly factories: the schema is a first screen for external tooling, not a replacement validator — a document passing it may still fail `PlantModel::load`, and the schema in turn rejects some documents `load` accepts (unknown keys, malformed known-kind parameters). The known-kind parameter sections encode the *standard* registry's contract; a deployment registering other device kinds extends or regenerates them.
- **Affected tickets:** #159 (this record and implementation); decision 3 (the serialization contract it describes); decision 29 (the device-kind seam whose parameter shapes it mirrors).

### 41. Duty/standby pump-group contract

- **Status:** Accepted — the contract below is proposed; the M9 implementation tickets (to be filed) land it. Implemented basis it builds on: `motor`'s per-equipment actuation verifies run feedback against the command and flags `fault` after `fault_ticks` disagreeing scans — fail-to-start and fail-to-stop detection in one mechanism (#52); `interlock`'s permissive-plus-trips gating and `bool-gate`/`sr-latch` cover interlock-chain wiring (#38, #52); `counter`, `timer`, and `totalizer` cover start counting and Float-rate integration (#85, #111); indexed per-element ports are an established convention — `interlock`'s `trip_1`…`trip_N` and `median-voter`'s `in_1`…`in_3` are discovered from the bound port names at construction (#38, #111); writable internal `In` points carry operator values through the receipted path (#30, #49, decisions 14, 18); and the descriptor and checkpoint conventions every kind follows are implemented (decisions 16, 11, 20).
- **Problem:** `docs/research/pumping-station.md` (#195) shows every owner specification treats the duty/standby group as one coordinated policy object: which of N pumps holds duty; rotation of that assignment by a declared policy — per-cycle alternation (sources [6], [9]), a timed or daily interval ([1] §42.4), or least accumulated run hours ([18], [19]); a lag pump staged on unmet demand at its own start level ([4], [9]) behind a simultaneous-start delay ([9], [10]); unavailable pumps excluded from staging ([5], [19]); and duty reassigned when a running pump's feedback proves failed ([5], [4], [18]). Whether those policies compose from the existing motor, latch, gate, counter, timer, manual-station, and interlock kinds — or need a new pump-group or process-assembly contract — is the M9 question, and if a contract, its declared I/O, parameter vocabulary, descriptor roles, and checkpoint obligations must be recorded.
- **Chosen approach (proposed):** A new `pump-group` component kind in `dcs-blocks` — the duty/standby manager — plus a `dcs-build` builder-level station composition helper. The split answers the note's open composition question (its gap 7): the group's behavior is per-scan stateful policy — a rotation position and run-hours ranking are run state — so it belongs in an executor component, while the station as one typed artifact (N motors, level measurement, the alarm set, operator points) is construction-time composition, decision 31's seam. Composing the policy itself from `bool-gate`/`sr-latch`/`counter`/`timer` wiring was evaluated and rejected per the note's gap 1: it re-implements the policy per station with no named semantics, and two required pieces have no existing home — the rotation position, and per-pump Bool run-hours accumulation with least-run ranking (`totalizer` integrates a Float rate; Bool-feedback hours have no feed — gap 3).
  Declared I/O (proposed; per-pump ports indexed `_1`…`_N` under the `trip_N` convention, the pump count discovered from bound port names):
  - `demand` (`In`, `Int`) — the stage count the station requests: 0 all stopped, 1 duty only, 2 duty plus lag. One input carries whatever produces the station's pump request — the decision-42 threshold chain or staged continuous control — and covers duplex through lead/lag/standby shapes [12].
  - Per pump `i`: `cmd_i` (`Out`, `Bool` — the group's run request, wired to that pump's `motor.cmd`), `run_i` (`In`, `Bool` — run feedback, normally the same field point the motor verifies), `fault_i` (`In`, `Bool` — the motor's proven feedback-failure flag, so the group responds to verified failure rather than raw signal states), and `avail_i` (`In`, `Bool` — the aggregated availability: not faulted, not locally selected, not maintenance-inhibited, permissives satisfied — the note's gap 2 resolved as a declared group input; the aggregation itself composes upstream from `bool-gate`/`interlock` wiring because which conditions feed "available" is plant-specific data, not kind semantics).
  - `duty` (`Out`, `Int` — which pump holds duty), `staged` (`Out`, `Int` — how many are commanded), `none_available` (`Out`, `Bool`), and `all_faulted` (`Out`, `Bool`) — the group-raised station conditions the alarm set consumes (gap 6).
  Parameters (proposed — all within the `Value` vocabulary of `Bool`/`Int`/`Float`, so the rotation policy is a documented `Int` code): `rotation` (`Int` — 0 alternate each pump-down cycle [6, 9], the reference default and an unvalidated customer assumption per the note's assumptions; 1 timed interval [1 §42.4]; 2 least-run-hours first [18, 19]), `rotation_ticks` (`Int` — the timed policy's interval), `start_delay_ticks` (`Int` — minimum ticks between successive pump starts, the simultaneous-start delay [9, 10]), `restage_delay_ticks` (`Int` — a de-staged pump held out [19]), and `min_off_ticks` (`Int` — the tick-domain form of the engineered starts-per-hour bound [7, 8]).
  Behavior: `demand` ≥ 2 stages the lag at its own `start_delay_ticks`; on falling demand the lag de-stages first [4]. A running pump whose `fault_i` asserts or whose `avail_i` drops hands duty to the next available pump automatically at the scan — the evidence-supported response [5, 4, 18]; the alarm-and-wait alternative is the note's recorded unvalidated assumption. Latched lockout-until-reset [18] is not group machinery: it composes in wiring as an `sr-latch` on `fault_i` feeding `avail_i`, cleared by a writable internal reset point — the decision-33 writable-point pattern — so whether the reference station latches a failed pump out is likewise assumption-marked station wiring. Non-`Good` per-pump inputs take the fail-safe reading the library already uses (`avail_i`/`run_i`/`fault_i` non-`Good` reads as unavailable/failed, matching `motor`'s cannot-prove-agreement rule); a non-`Good` `demand` holds the last `Good` stage request — the implementing ticket documents the precise rule.
  Descriptor roles: the existing four-role `PortRole` vocabulary suffices, recorded as no extension — `demand` → `Setpoint`, `cmd_i` → `Output`, `run_i`/`avail_i`/`fault_i` → `ProcessValue` (measured equipment state the component acts on), `duty`/`staged`/`none_available`/`all_faulted` → `Status`.
  Checkpoint obligations: the rotation position, the per-pump run-hours accumulators, and the in-flight start/restage/min-off timers are run state the kind must capture and restore under decision 20's rule — tuned parameters folded into the same vocabulary — so a tracking standby inherits the duty assignment and the counters (`WW-LCM-001`'s station-facing reach; the note records that no source addresses controller redundancy — this carryover is our proposal, assumption-marked).
- **Alternatives considered:** Per-station composition from `bool-gate`/`sr-latch`/`counter`/`timer` (rejected — the note's gap 1: no named semantics, and rotation position and run-hours ranking have no home); extending `motor` into a group kind (couples the per-equipment actuation contract to group policy, and `motor` stays reusable alone for non-pump loads); a builder-level assembly spec only, no kind (the policy is per-scan stateful behavior — construction-time wiring cannot hold a rotation position); a general process-assembly contract (premature — the evidence supports the pump group specifically, and inventing assembly vocabulary no source names repeats what decision 31 already covers at the composition layer).
- **Consequences:** One new kind plus one builder-level helper carry the whole group semantics; the status outputs feed the station alarm set with no new alarm contract (decision 43). Availability aggregation stays declared station wiring — the descriptor's `avail_i` port gives it a named landing instead of an ad-hoc gate. The `Int`-coded policy vocabulary is recorded honestly: `Value` carries no enum, so the parameter's documented codes are the contract. The checkpoint vocabulary — rotation position, run-hours, delay timers — deploys to both peers together under decision 11's strictness, and every assumption-marked behavior above is flagged for customer validation before the acceptance scenario fixes its defaults.
- **Affected tickets:** #202 (this record); the M9 pump-group kind ticket and the M9 station-composition ticket (to be filed); requirements `WW-CTL-001` and `WW-LCM-001`; relies on #52, #38, #85, #111, #30, #49 and decisions 5, 11, 16, 20; evidence `docs/research/pumping-station.md` (#195).

### 42. Station level control and measurement failover

- **Status:** Accepted — proposed contracts on implemented seams. Implemented basis: `alarm-monitor` plus `sr-latch` can already hand-wire threshold behavior (#22, #52); `pid`, `analog-input`, and `signal-filter` implement the continuous path and `median-voter` the 2oo3 redundant-measurement path (#11, #110, #111); `manual-station` gives a bumpless operator/manual transfer on an analog demand and `override-select` an operator-driven source switch (#110); writable internal `In` points carry operator setpoints and mode switches through the receipted path, and `set_parameter` tunes descriptor-ranged parameters (#30, #49, #82, decisions 14, 18, 20); the quality model every input carries is decision 2's.
- **Problem:** The note's evidence gives the station two control semantics — the dominant ordered start/stop threshold chain (low-water cut-off, duty start/stop, lag start, high-level alarm — sources [4], [10]) with operator-adjustable setpoints ([5], [7]), and continuous constant-level control for variable-speed stations ([2], [8]) — plus an engineered bad-measurement rule (hardwired float backup [6], four-point float fallback [10], backup-float inputs [20], multi-transmitter master-level selection [5]) and a manual-takeover path that is a distinct mode with declared protections ([6], [5]). The decisions needed: which seam expresses each, and which behaviors are contract gaps.
- **Chosen approach (proposed):** Four parts, each naming its seam.
  *Threshold control* is a new `threshold-chain` kind: one declared ordered setpoint table as parameters — `cutoff`, `stop`, `start`, `lag_start`, `high` — reading `level` (`In`, `Float`) and emitting `demand` (`Out`, `Int` — the stage count feeding `pump-group.demand`) plus per-threshold `Bool` flags the alarm set consumes. The start/stop pairs are the hysteresis. Setpoints are descriptor-declared parameters, so the operator-adjustable requirement [5] lands on the implemented `set_parameter` path with declared ranges — no writable-point wiring per setpoint. A non-`Good` `level` falls to the declared `on_bad_demand` (`Int` parameter) — the declared safe behavior when measurement cannot be trusted; which demand that is (hold, stop, or run) is the note's recorded all-measurement-bad assumption, customer-validation marked.
  *Continuous control* composes from existing kinds: `analog-input`/`signal-filter`/`pid` hold the wet well at a level setpoint [2], with owner bounds like minimum-speed cycling [8] expressed as declared parameters. The reference station ships threshold mode; which form is primary for the first client is an unvalidated assumption the note records (`WW-CTL-002` keeps both). A variable-speed group's per-pump analog commands are beyond this contract — `pump-group`'s `demand` is a discrete stage count.
  *Bad-measurement failover* is a new `failover-select` kind: `primary`/`backup` (`In`, `Float`), `out` (`Out`, `Float` — primary while `Good`, else backup), and `backup_active` (`Out`, `Bool` — the alarmed transition source; backup-mode engagement is alarmed as critical [5]). `override-select` cannot express this — its `select` is an operator input, not quality-driven — and `median-voter` is 2oo3 voting, not primary/backup failover (the note's gap 5). When every source is bad, `out` carries `Bad` quality and the threshold chain's `on_bad_demand` rule applies. The measurement architecture itself — single transmitter plus float backup [6, 10, 20] versus redundant transmitters with master selection [5] — is an unvalidated assumption; the kind covers the analog failover shape, and a float-direct backup is station wiring (`digital-input`-conditioned floats feeding the demand path), the simulated stand-in for [6]'s electrically separate circuit.
  *Manual takeover* composes from existing kinds: a writable internal mode point per pump — an operator value through the receipted path (decisions 14, 18) — selects between `cmd_i` and the operator's hand request through `bool-gate`/`override-select` wiring; `manual-station` covers the analog-demand case and reports `manual_active`. Which protections persist in manual — over-temperature does while the low-level cut-off may not [6] — is station wiring (which signals feed the `interlock`'s permissive and trips versus which gate only automatic mode), not contract, and its scope is assumption-marked.
- **Alternatives considered:** Hand-wiring the chain from `alarm-monitor` plus `sr-latch` per setpoint (the note's gap 4 — it works, but scatters one engineered table across many instances, each with its own quality handling, and nothing can check the ordered-setpoint invariant at build time); setpoints as `pump-group` parameters (couples station-level semantics to the equipment group — the same group serves demands that are not level-driven, and the table belongs to the measurement); quality-switching added to `override-select` (its `select` port is a `Bool` — failure-driven switching is a new semantic, not a parameter, and conflating operator and failure switching muddies the alarmed-transition distinction [5]); failover inside `pump-group` (measurement failover is station scope — it serves the threshold chain and any other level consumer, not the pump group); a `Bad`-quality default inside `threshold-chain` with no `on_bad_demand` (silences the all-bad case — the note records it as an engineered, owner-specified behavior, so it must be declared).
- **Consequences:** Two small kinds (`threshold-chain`, `failover-select`) plus wiring conventions carry the whole level-control contract; every station behavior names its seam. The assumption-marked behaviors — primary control form, measurement architecture, all-sources-bad demand, manual-protection scope — stay visible for customer validation rather than silently defaulting. Alarmed transitions (backup engaged, measurement fault) surface as ordinary status outputs feeding decision 43's set.
- **Affected tickets:** #202 (this record); the M9 `threshold-chain`, `failover-select`, and station-composition tickets (to be filed); requirements `WW-CTL-002` and `WW-OPS-003`; relies on #22, #110, #111, #82, #49 and decisions 2, 14, 18, 20; evidence `docs/research/pumping-station.md` (#195).

### 43. Station alarm set on the two-flag lifecycle

- **Status:** Accepted — the lifecycle is implemented, the Bool-input sibling landed as the dedicated `bool-latching-alarm` kind (#219 — the new-small-kind option, since the Bool `in` and the parameterless signature are a distinct contract one kind string would hide), and the set's mapping is proposed. Implemented basis: `latching-alarm` (#109) carries the two-flag semantics — `alarm` reporting standing limit state under `AlarmMonitor`'s hysteresis rule, `unacknowledged` latching on a fresh trip and clearing while the model-wired writable `ack` point reads `true` — acks journaled through the ordinary settled receipt and the latch carried in checkpoints (decision 33); `alarm-monitor` stays the auto-clearing sibling (#22); the page's alarm pane consumes `alarm`/`unacknowledged` status-role ports and offers the ack affordance on writable `ack` points (#108); the durable journal survives restart (#163, decision 36); and decision 38 records that shelving, suppression, and classes stay deferred as per-kind extensions.
- **Problem:** The note fixes which alarms a representative station carries — the owner/regulator minimum (power failure, pump failure, dry-well sump and wet-well high water, unauthorized entry, or any other station malfunction, on a backed-up supply [1] §46), the owner lists' additions (per-pump failure-to-start and failure-to-pump, thermal and moisture faults, float/transducer failure, VFD failure, generator running/failed [10, 11]; backup-control-mode engagement alarmed as critical [5]) — and each must map onto the decision-38 lifecycle as either a wired `latching-alarm` instance or a kind behavior, without re-opening the lifecycle itself.
- **Chosen approach (proposed):** The reference station's alarm set, each element named to its seam:
  - *High wet-well level* and *low level / dry-run* — wired `latching-alarm` instances on the level path (fed by `threshold-chain`'s flags or the selected level directly); the standing flag is process truth and the latch awaits the writable `ack` point, matching the latched high-level-alarm reset [10] requires. No contract work.
  - *Per-pump fail-to-start / fail-to-stop* — detection is `motor`'s implemented `fault` kind behavior (feedback disagreement after `fault_ticks` covers both directions [18]); the alarm view is a wired latching instance — which exposes the one contract gap: `latching-alarm`'s `in` is `Float` and the typed port contract never coerces, so Bool-sourced alarms need a Bool-input latching sibling — a new small kind, or a `point_kind`-dispatched variant through the seam `ComponentSpec::point_kind` already provides; the implementing ticket's documented choice, either way preserving the `in`/`ack`/`alarm`/`unacknowledged` vocabulary exactly.
  - *Measurement fault* and *backup-mode engaged* — `failover-select`'s `backup_active` plus the level point's non-`Good` quality, each wired into the same Bool latching behavior; the transition is alarmed per [5].
  - *No pump available* and *all pumps faulted* — `pump-group`'s `none_available`/`all_faulted` status outputs wired into the same Bool latching behavior ([1] §46's "any other station malfunction"; the note's gap 6: no new alarm contract — the group must expose the conditions, which decision 41 does).
  - *Station power failure* — a field `Bool` point into the same Bool latching behavior. *Communication failure* is served by decision 22's I/O-health surface — link state, per-direction counters, last `IoError` — rendered as health, distinct from process alarms (#83, #107); whether a discrete comm-fail alarm is additionally required is a customer assumption the note records.
  - *Equipment-fault conditions* (thermal, moisture, check-valve failure-to-pump [10, 11]) — field `Bool` points feeding the same Bool latching behavior, and simultaneously feeding `avail_i` wiring where they should exclude the pump.
  Which alarms latch until acknowledged versus auto-clear is per-alarm model wiring — `latching-alarm` versus `alarm-monitor` — and the required latch set is the note's recorded customer-validation question, assumption-marked.
- **Alternatives considered:** Extending `latching-alarm`'s `in` to accept `Bool` by loosening the declared port kind (the value-kind matching is strict and never-coercing by decision 2 — loosening it for one port undermines the wiring checks every kind relies on); `alarm-monitor` for the equipment alarms (auto-clear loses the acknowledged record the owner lists imply [10] and decision 33 chose the two-flag model to provide); a dedicated alarm `Command` variant or alarm-specific journal events (rejected with decision 33 — the writable `ack` point and the settled receipt already carry the semantics and the audit); promoting alarm classes or priorities for this set (decision 38's recorded deferral — per-kind declared extensions when a customer's operations demand them, not contract additions).
- **Consequences:** The station alarm set needs at most one small kind — the Bool-input latching sibling — plus wired instances of existing kinds; every alarm speaks the same two-flag language the pane, history, checkpoints, and journal already carry, and acknowledgment needs no new command path. Decision 38 deferred priority, shelving, and suppression at the time. The later IJmuiden incident evidence and accepted `WW-ALM-001`–`WW-ALM-005` requirements reopen that roadmap scope while preserving this two-flag acknowledgment axis as the compatible base; a new decision must define the additional contract before implementation. The latch-until-ack set and the comm-fail question remain customer policy rather than silently decided.
- **Affected tickets:** #202 (this record); the M9 Bool-latching-alarm and station-alarm-wiring tickets (to be filed); requirement `WW-OPS-002`; relies on #109, #108, #22, #163 and decisions 33, 38, 22, 17; evidence `docs/research/pumping-station.md` (#195).

### 44. Station dynamics document stays beside the model

- **Status:** Accepted — decided; no implementation beyond M9's checked-in documents. Implemented basis: `dcs-plant-server --dynamics FILE` merges a plain JSON list of `ProcessElement` declarations into the served `ChannelMap`, validating each element as it merges (#81, decision 24); the element vocabulary is `first_order_lag`, `second_order_lag`, `integrator`, `dead_time`, `noise` (#8, #37, #135, #156); and `dcs-build` emits exactly the versioned `PlantModel` document, depending on `dcs-core` and `dcs-model` only (#112, decision 31).
- **Problem:** The reference station needs two artifacts — the plant model (control contract) and the dynamics document (the simulated world's physics: wet-well inflow and level response). M9 must record whether the dynamics document also composes through the typed-build path, or stays a checked-in document beside the model.
- **Chosen approach (decided):** The dynamics document stays a checked-in declaration list beside the model — decision 24's boundary applied unchanged. `dcs-build`'s product is the `PlantModel`; the dynamics document is `dcs-sim`'s `ProcessElement` vocabulary — testbench content no controller reads ("the model describes what the controller sees, the dynamics document describes what the world does"). A typed dynamics builder would put a `dcs-sim` dependency into the model producer — inverting decision 31's dependency direction — to gain compile-time checks for a document whose only consumer already validates it at merge. The station therefore checks in both documents: the model emitted through `dcs-build` (`WW-ENG-001`'s typed-composition evidence), the dynamics beside it, merged and validated through `dcs-plant-server --dynamics`; the acceptance scenario reproduces from the two checked-in artifacts.
- **Alternatives considered:** Extending `dcs-build` to emit dynamics too (a `dcs-sim` dependency in the model producer — the inversion decision 31's crate boundary exists to prevent); dynamics as `PlantModel` schema (rejected with decision 24 — the control contract would carry simulation-only content a real deployment cannot produce); a separate typed dynamics-builder crate with its own spec table pinned to the `ProcessElement` vocabulary under the drift-test convention (a real option, deferred — M9's acceptance needs the document reproducible and validated, which merge-time checking already gives; reopen if dynamics authoring grows error-prone).
- **Consequences:** `WW-ENG-001`'s typed-composition acceptance applies to the model document; the dynamics document's check is the plant server's validating merge plus the scripted scenario. The direction stays clean — testbench content never enters the control contract's authoring seam — and no new crate or vocabulary joins M9's scope.
- **Affected tickets:** #202 (this record); the M9 station-composition and acceptance-scenario tickets (to be filed); requirements `WW-ENG-001` and `WW-FND-001`; relies on #81 and decisions 24, 31.

### 45. Declared input freshness budgets

- **Status:** Accepted and implemented (#196): `io_point` declarations carry an optional `stale_after_ticks`, the point map rides it into the executor, and the input phase stamps `Uncertain(Stale)` once a field input's driver-stamped sample tick lags the scan tick past the budget.
- **Problem:** WW-OPS-003 asks operators and control components to distinguish stale data. `QualityReason::Stale` has existed in the contract since the signal model — every quality-propagating kind honors it — but no runtime path produced it: only scripted playback or fault injection could name the reason. A field source that stops refreshing looked exactly like a healthy one, because the executor re-stamps every input read with the scan tick and the last value reads `Good` forever.
- **Chosen approach (implemented):** Freshness is *declared*, not inferred. `stale_after_ticks` is an optional non-negative integer on field `in` `io_point` declarations, following the optional-field convention (`writable`, `channel`): documents predating it load and serialize unchanged, the model version stays 1, and validation confines it to channel-bound `in` points (`StaleOut`, `StaleInternal` elsewhere) — an `out` point is never read and an internal point never reaches a driver, so a budget there could only mislead. Assembly carries the budget onto the field point's `PointSpec`; the executor's input phase consults the *driver-stamped* tick before re-stamping — when `scan_tick - sample_tick` exceeds the budget, the landed quality merges `Uncertain(Stale)` by the worst-of rule, so a driver-reported `Bad` or worse-named `Uncertain` is never improved, and the first in-budget read returns the driver's own quality. The executor remains the only timestamp authority: the image stamp is the scan tick either way, and the driver tick is evidence for the check only — so drivers need no new contract beyond the `Sample::tick` the existing sim, remote, and bus protocols already carry. The transition reaches operators through the existing machinery: the recorder's quality diff journals `QualityChanged` at the scan tick and the page renders the named `stale` reason — no new event, endpoint, or wire type. A driver whose samples carry no usable freshness signal — stamping every read with the current tick, or a fixed tick — makes the declaration inert or always-stale; `docs/model-reference.md` records the rule to declare the field only where the source distinguishes fresh samples from held ones.
- **Alternatives considered:** An executor-side refresh detector watching every field input (no declared budget — a uniform timeout would guess at per-source rates the model knows better, and an undeclared point could never opt out; the declaration also travels with the document reviewers sign off); deriving staleness from wall-clock or scan-period time (ticks are the run's only deterministic clock — wall-clock would break the replayable, identical-runs guarantee the scripted tests pin); a new journal event or wire type for staleness (rejected — the quality-transition path already attributes and renders named reasons, and `Stale` was already a first-class `QualityReason`); drivers marking their own samples stale (the field does not know the consumer's scan cadence — freshness is a contract between the declared budget and the scan tick, which only the executor holds).
- **Consequences:** Field `in` points gain an opt-in freshness contract with no change for undeclared points, failed reads, forced points, or internal points; `PointSpec` carries the budget beside `writable` as point metadata; `dcs-build` composes it through `field_input_stale_after`; and a stale input's transition is journaled, rendered, and checkpoint-neutral like any other quality change — staleness is recomputed from field evidence every scan, never latched.
- **Affected tickets:** #196 (this record and implementation); relies on #5 (the signal model supplying `QualityReason::Stale`), #9 (the executor's input phase and timestamp authority), #83 (the I/O-health surface staleness sits beside), #35 (the quality-transition journal the transition rides), and WW-OPS-003.

### 46. Model and dynamics artifact distribution

- **Status:** Decided — the implemented mechanism stands: every process loads its documents from paths the deployment supplies, and no served or pushed distribution enters the contract. Implemented basis: `dcs-controller <model.json>` loads and validates the model from a local path (#19); a `--revised` standby loads the revised document as its own positional model path — the flag requires `--standby` (#87, decision 25); `dcs-plant-server` takes the model positionally plus `--dynamics FILE` (#81, decision 24); the restart state file and the journal file are likewise operator-placed paths (#115, #163); the container images mount their documents read-only and take their container paths — the model is the shared contract each container mounts (#39, #157, `docs/packaging.md`); and the engineering surface — `dcs-model diff`/`lint`/`schema` and `dcs-controller --check` (#114, #136, #159, #160) — operates on checked-in documents. What crosses the wire is identity, not content: the checkpoint's `model_fingerprint` names the document it was captured under (#66, decision 27), which is what makes #87's crossing deliberate.
- **Problem:** A revision roll needs the revised model at the new peer, and a plant container needs its model and dynamics at startup — and the pair must provably agree on which document runs. The open question is who carries content: a contract mechanism — a monitor serving the model for a peer or a rig to pull, or a pushed document — or the deployment itself, with the wire carrying only identity.
- **Chosen approach (decided):** Distribution stays operator-supplied paths: each process loads the document its deployment hands it, and the pair's agreement on which revision runs is negotiated by fingerprint rather than by moving content over the wire. The documents are engineered artifacts — produced, diffed, linted, and checked against the emitted schema before they reach a rig — so the deployment mechanism (read-only bind mounts today) places exactly what engineering approved. A revision roll is then: start a standby whose own model path is the revised document, armed `--revised`; the fingerprint crossing carries the intent, and the carryover rule names what transfers. No endpoint serves the model — none exists — and none is added.
- **Alternatives considered:** A `GET /model` endpoint a standby or a rig pulls from a running peer (makes a running controller a distribution authority — a revision would depend on the active's availability at the moment a new peer starts, and the document's home is engineering tooling, not a process); a pushed `POST /model` deployment path (the same content-over-wire coupling, plus a mutating endpoint the contract deliberately lacks — the monitor's POST surface is commands, promotion, and scans); artifacts baked into each image (couples the image to one plant — the image is the binary, the mount is the contract, per the packaging discipline); a fetched manifest or artifact-registry service (another deployed component answering a question the fingerprint already answers).
- **Consequences:** Every consumer of a document gets it from the deployment, never from a peer — distribution is uniform across the revision roll, plant startup, the state file, and the journal file. What the contract must guarantee — that the peers agree which document runs — the fingerprint and its named negotiation errors already provide: skewed mounts surface as `FingerprintMismatch`/`Degraded`, not silent divergence. The recorded limit: nothing checks that two mounted documents are the *intended* pair beyond the fingerprint — which is exactly its job. A later served distribution would still be only a transport choice beneath the same identity contract.
- **Affected tickets:** #179 (this record); relies on #87 (the revision roll), #81 (the dynamics document), #66 (the fingerprint), #115 and #163 (the sibling file artifacts), and #39/#157 (the container packaging); decisions 3, 24, 25, and 27.

### 47. Plant-level topology stays deployment configuration

- **Status:** Decided — URL configuration stands; no plant-level document or index enters the contract. Implemented basis: decision 37's overview mode is implemented — repeated `?pair=<name>=<host:port>,<host:port>` parameters each name one pair outright, one card per pair polls that pair's `/role` and `/snapshot`, and an unreachable pair degrades to a named card fault while the others keep updating (#164).
- **Problem:** With the overview landed, plant topology — which pairs exist, what they are called, where they are reachable — lives only in the page's URL. Decision 37 deferred the contract question; the open issue is whether a plant-level document or index now earns a contract place — a schema section, a served topology endpoint, or a checked-in topology artifact — or whether deployment configuration remains the answer.
- **Chosen approach (decided):** URL configuration stands. Topology is deployment knowledge, not engineered plant data: the same station model deploys as a test pair or a production pair, and which pairs exist is a fact about the deployment, not about any one model. Each candidate contract home was weighed: a `PlantModel` section is the scope violation decision 37 recorded (the model is per-pair engineered data); a served roll-up endpoint needs a process owning plant-wide state (decision 37's supervisor alternative); a checked-in topology document would be an artifact with no consumer — the page takes URLs, and no tooling exists to generate them from a document. The recorded mechanism is the `?pair=` convention plus bookmarks and deployment templates; a deployment that automates it generates the query from its own inventory.
- **Alternatives considered:** A served `GET /topology` (a monitor cannot serve a topology it is part of without self-knowledge — the data would be configured into it anyway, moving the problem rather than removing it); a `PlantModel` topology section (rejected with decision 37); a checked-in plant index — a document naming the pairs and their roles that a generator or page tooling consumes (the real candidate, deferred: inventing it now defines a schema nothing validates against a consumer; it earns its place when a deployment's pair count makes hand-authored URLs the error source); deriving topology from `Signal.group` areas (display metadata, not deployment reachability).
- **Consequences:** The contract stays per-pair; supervision stays page-side presentation per decision 37. Plant topology has exactly one recorded interface — the overview's `?pair=` parameters — and the honest limit is recorded with it: there is no authoritative plant index, so a deployment's pair set lives in its own tooling. The decision is reopenable the moment a consumer exists that the URL cannot serve.
- **Affected tickets:** #179 (this record); #164 implemented the URL convention; decision 37 records the per-pair boundary.

### 48. Audit depth: the declared-actor seam stands

- **Status:** Decided — declared identity plus the recorded fronting-proxy seam is the audit answer; no next contract step is warranted now. Implemented basis: `POST /command` accepts the attributed envelope beside the bare `Command`, `CommandReceipt` carries `actor` serde-optionally, `CommandSettled` journals the attribution, and the page stamps its `?operator=` identity on every submission (#165, decision 39); the journal persists across restart through `--journal-file` (#163, decision 36).
- **Problem:** Decision 39 recorded the honest limit — `actor` is attestation, not authentication; the journal proves what was claimed, not who sent it — and deferred verification to deployment machinery. With M8 landed the question is whether a next contract step — transport authentication, signed identity, authorization — earns a place now, or whether the recorded seam is the answer.
- **Chosen approach (decided):** The seam stands. The contract's obligations are met in full: every settled command carries its attribution into a durable, tick-ordered record. Verification is deployment machinery by design — an authenticating proxy fronts the monitor and supplies `actor` from authenticated context with no contract change; that is the seam working as recorded, not a gap in it. Adding authentication to `dcs-monitor` itself would couple the audit contract to machinery the simulated system cannot exercise — no identity provider, credential store, or threat model exists inside the no-live-deployment bound for it to be honest against. A next contract step is reopened by a concrete requirement — a customer access-control or audit-integrity specification — not invented ahead of one.
- **Alternatives considered:** TLS plus authentication inside the embedded monitor server (duplicates what a standard fronting proxy does at the cost of certificate and identity machinery in a crate whose discipline is a small, dependency-light surface — and unexercisable within the bound); signed actor assertions (key distribution and verification are deployment infrastructure the contract would have to carry — a PKI concern with no simulated counterpart); command authorization roles on the contract (no requirement names roles; the writable flag already bounds *what* is commandable — who may command is the proxy's question); retracting `actor` until verification exists (loses the journaled attribution the seam exists to carry — an unattributed durable record is worse than a declared one).
- **Consequences:** The journal remains a faithful record of declared attribution; the documented limit stands until a deployment fronts the monitor — at which point `actor` is the field the proxy fills, with no contract or client change. The decision keeps the safety-related-constraints surface honest: what the platform guarantees — a complete, durable, attributed record — is separated from what the deployment guarantees — that the record's claims are verified.
- **Affected tickets:** #179 (this record); #165 implemented the attribution; #163 the durable record; decisions 7, 8, 17, 36, and 39.

### 49. The demonstration rig as a checked-in definition

- **Status:** Accepted — decided; a rig-definition ticket (to be filed) lands the checked-in compose file; everything below the file is implemented. Implemented basis: the controller image (#39) and the plant-server image (#157) are checked-in packaging artifacts; `docs/packaging.md` documents the full rig — network, the plant container with mounted model and dynamics, the redundant controller pair on `--remote`/`--standby`/`--listen`, promotion by `POST /promote` or `--auto-promote` — including the cross-host form with published addresses; and the scripted tests prove the semantics end to end with real processes (`hot_swap.rs` #64, `failover.rs` #65, `model_revision.rs` #87, `lifecycle.rs` #161, `full_stack.rs` #116).
- **Problem:** The vision's demonstration is the two-machine hot-swap: a redundant instance on another machine taking over without interrupting the process. The packaged form of that demonstration — the plant-plus-pair container rig — has its images and its documented run commands, but the rig's topology exists only as prose: which containers exist, what they mount, how the standby is wired to the active's monitor, which ports the UI reaches. The question is whether a checked-in multi-container rig definition — a compose file or a documented equivalent — completes the demonstration within the no-live-deployment bound, and what beyond the two Dockerfiles the packaged demo requires.
- **Chosen approach (decided):** The rig definition earns a checked-in place as a compose file — the artifact the two Dockerfiles presuppose. The packaged demo requires, beyond the images: the rig's topology as data (services, network, mounts, flags, peer wiring, published monitor ports), the mounted artifacts (the checked-in model and dynamics documents, per decision 46), and the switchover procedure — already documented in `docs/packaging.md`. A compose file is the standard, statically inspectable declaration of the first item: `docker compose config` can check it where prose commands cannot be checked, and `docker compose up` reproduces the rig without transcription. The recorded boundaries keep it honest: it is a single-host orchestration declaration — the two-machine shape remains the documented cross-host form (published `host:port` in place of network names) — and within the no-live-deployment bound nothing executes either; like the Dockerfiles, it is a checked-in packaging artifact that defines no deployment. The scripted tests remain the behavioral proof — the rig definition makes the packaged demonstration reproducible, it does not re-prove the semantics.
- **Alternatives considered:** The documented command sequence as the permanent rig definition (works — `docs/packaging.md` is already the documented equivalent — but prose cannot be statically checked and drifts silently from the binary's flags; the compose file becomes the declaration the document references); a bespoke rig-runner script (non-standard tooling duplicating what compose already is); Kubernetes or other orchestrator manifests (machinery the demonstration does not need); treating the scripted tests as the whole demonstration (they prove behavior with real processes — they do not exercise the packaged artifact set, which is what this decision covers).
- **Consequences:** The packaged demonstration is three checked-in artifacts plus a procedure: two Dockerfiles, the rig definition, and the documented switchover steps. The file must stay in lockstep with `docs/packaging.md` — one is the declaration, the other the explanation. The two-machine demonstration within the bound is the same declaration plus the documented published-port variant; an actual second machine stays outside the workflow, recorded rather than silently absent.
- **Affected tickets:** #179 (this record); the M9 rig-definition ticket (to be filed); relies on #39 and #157 (the images) and #116/#161 (the scripted proof); decisions 9, 10, and 15.

### 50. Flow-paced ratio demand

- **Status:** Accepted — the contract below is proposed; the M10 implementation tickets (to be filed) land it. Implemented basis it builds on: every input carries decision 2's quality, so a bad pacing-flow signal is already visible to components; `pid`, `analog-input`, and `signal-filter` implement the residual-analyzer loop a trim rides on (#11, #110); `failover-select` covers a declared backup pacing source (#211, decision 42); `rate-limiter` bounds demand slew (#85); writable internal `In` points carry operator values through the receipted path (#30, #49, decisions 14, 18); `set_parameter` tunes descriptor-declared parameters against declared ranges (#82, decision 20); optional ports are an established seam — `ComponentSpec::get` binds a port the model may leave unwired; and the descriptor and checkpoint conventions every kind follows are implemented (decisions 16, 11, 20).
- **Problem:** The note's gap 1: no kind computes "dose per flow unit × measured flow → bounded actuator demand" with declared dose and rate limits, a clamped indication, and a declared response to a bad flow signal. Composing it from `pid` misuses the controller — pacing is feed-forward scaling with no error term — and `bool-gate` wiring has no arithmetic semantics. The mode is the owner-specified norm: feed proportional to measured flow with the dose per flow unit operator-adjustable [1 §5.1.2, 2 §2.2.4.1.1, 7 §3.5, 9, 10]. The contract must fix the kind's declared I/O, where the operator dose setpoint lives, the parameter vocabulary, descriptor roles, checkpoint obligations, and the `dcs-build` spec obligation.
- **Chosen approach (proposed):** A new `flow-paced-ratio` kind in `dcs-blocks`. Each scan it computes `dose × flow`, scaled by `trim` while that port is bound and `Good`, clamps the dose term to `[min_dose, max_dose]` and the resulting demand to `[min_rate, max_rate]`, and flags the saturation — the demand-saturates-at-the-bound behavior the evidence records [7, 14].
  Declared I/O (proposed): `flow` (`In`, `Float` — the measured process flow); `dose` (`In`, `Float` — the operator dose setpoint per flow unit, e.g. mg/L [2 §2.2.4.1.1, 7]); `trim` (`In`, `Float` — the optional analyzer-`pid` correction scaling the paced demand, the note's gap-2 port-wired cascade, bound through `ComponentSpec::get` so an unwired `trim` means unity); `demand` (`Out`, `Float` — the bounded actuator demand feeding decision 52's actuation path); `clamped` (`Out`, `Bool` — asserts while either bound engages); `fallback_active` (`Out`, `Bool` — asserts while a declared bad-signal response is engaged, the alarmed fallback-transition output decision 55's alarm set consumes).
  The operator dose setpoint is a port wired to a writable internal `In` point — the `pid.sp`/`manual-station.manual` convention — not a descriptor-declared parameter: the dose is a standing operator value whose writes ride the journaled, receipted `WriteValue` path and whose held value crosses checkpoints through the `internal` section (decision 14), and a port keeps the setpoint composable from a non-operator source — the deferred schedule kind of decision 54 — without a kind change. The declared dose bounds still constrain the outcome: an out-of-range entry saturates the computed demand at the bound with `clamped` asserted.
  Parameters (proposed — descriptor-declared, tuned through `set_parameter` where the descriptor declares a range, under the decision-41/42 conventions; `Int` codes where `Value`'s vocabulary has no enum): `min_dose`/`max_dose` (`Float` — the engineered bounds on the dose term; the feed-range floor and mid-range guidance make them engineering data, not conventions [1 §5.0.3(g), 3, 5]); `min_rate`/`max_rate` (`Float` — the bounds on the actuator demand, the engineering-adjustable minimum and maximum pump running speed [7] and the vendor speed limit [14]); `on_bad_flow` (`Int` code — `0` stop, `demand` falls to zero, the conservative reading of the no-flow interlocks [3, 6]; `1` hold the last `Good` demand; `2` drive the declared `fallback_rate`); `fallback_rate` (`Float` — the fixed demand code `2` drives); `on_bad_trim` (`Int` code — `0` pace untrimmed on flow alone, the note's proposed response; `1` hold the last `Good` trim). A non-`Good` `dose` holds the last `Good` dose — the implementing ticket documents the precise rule — and `demand` carries the merged worst-of input qualities so a bad flow marks the demand untrusted even under a hold or fallback response. The default `on_bad_flow`, whether the response must be operator-selectable, and whether analyzer trim is in scope at all are the note's recorded customer-validation assumptions, not defaults this decision fixes.
  Descriptor roles: the four-role `PortRole` vocabulary suffices — `flow`/`trim` → `ProcessValue` (measured state the kind acts on), `dose` → `Setpoint`, `demand` → `Output`, `clamped`/`fallback_active` → `Status`.
  Checkpoint obligations: the held last-good demand, dose, and trim values the hold responses need are run state captured and restored under decision 20's rule — tuned parameters folded into the same vocabulary — so a tracking standby computes identically.
  The `dcs-build` obligation under #151's drift-guard convention: the kind ships a spec in `dcs_build::specs` declaring its kind string, typed port handles, and parameter set as data, joins `dcs_blocks::KINDS`, and takes its `check` call in `dcs-blocks/tests/spec_drift.rs` — a kind added without its spec fails the drift test.
  Fixed-rate service is no kind mode: where the treated flow barely varies [3, 6] the operator's fixed demand rides the `manual-station` path of decision 52, or `on_bad_flow = 2` makes the declared fallback rate the fixed rate. Flow pacing is the mode owner specs require to exist; which mode the first client calls primary is the recorded assumption.
- **Alternatives considered:** `pid` as the ratio computer (feed-forward scaling has no error term — the integral accumulates a meaningless quantity and the clamped and bad-signal semantics hide inside tuning); arithmetic composition from `bool-gate`/`rate-limiter` (no multiplication seam exists, and per-skid rewiring re-implements policy — the note's gap 6); the dose setpoint as a descriptor-declared parameter (range-enforced at submission, but parameters are the tuning surface — the dose is operator data the point path already carries, journals, and checkpoints, and a port keeps the schedule-source composition open); bad-flow handling delegated to downstream `interlock` wiring (silences the declared response — the note's gap 6 puts it on the kind the way `threshold-chain.on_bad_demand` carries the station's all-bad answer, decision 42); permissive inputs on the kind itself (decision 51 records why the chain stays wiring).
- **Consequences:** One kind carries the whole ratio contract — declared bounds, clamped indication, and named bad-signal responses — and every assumption-marked behavior (primary mode, default `on_bad_flow`, trim scope) stays flagged for customer validation rather than silently defaulting. The `Int`-coded parameter vocabulary is recorded honestly, as decision 41 records its `rotation` codes. The kind's checkpoint vocabulary deploys to both peers under decision 11's strictness.
- **Affected tickets:** #240 (this record); the M10 `flow-paced-ratio` kind ticket — the kind plus its `dcs-build` spec under the drift guard (to be filed); the M10 dosing-composition fixture ticket and the scripted acceptance-slice ticket (to be filed); requirement `WW-CTL-003`; relies on #30, #49, #82, #211 and decisions 2, 11, 14, 16, 18, 20, 42; evidence `docs/research/chemical-dosing.md` (#232).

### 51. Dosing permissive chain

- **Status:** Accepted — recorded as composition on implemented kinds; no new kind. Implemented basis: `interlock` gates an analog value behind a `permissive` and `trip_1`…`trip_N` inputs to a declared `safe_value`, fail-safe on non-`Good` inputs and auto-resetting (#38, #52); `bool-gate` and `sr-latch` compose Bool conditions (#38); `sequencer` walks a declared ordered step table (#134); debounced `digital-input` conditions field contacts (#11); `failover-select` switches the pacing signal to a declared backup source on quality (#211, decision 42).
- **Problem:** The owner-specified permissives — process flow proven [1 §5.1.2(b), 2 §2.2.4.2, 3, 6], metering pump available, chemical tank above the low-level inhibit [7, 12, 13], external inhibits such as wet weather [7] — are the load-bearing safety surface: no flow means no feed, and a flow switch in series with the metering pump is required over a pump interlock alone [6]. The chain can also be sequenced — the storage-tank valve opens and proves on its limit switches before the duty pump starts [7]. The decision: which seams express the chain, whether the ratio kind takes declared permissive inputs, and the recorded loss-of-permissive rule.
- **Chosen approach (proposed):** The chain is declared model wiring on implemented kinds, not ports on `flow-paced-ratio`. The demand passes through an `interlock` instance whose `permissive` input carries the aggregated "dosing permitted" condition and whose `trip_N` inputs carry the individual inhibits — flow not proven, tank at or below the low-level inhibit, bund flood, external inhibit, pump unavailable — the Bool aggregation composed upstream through `bool-gate`/`sr-latch` wiring, because which conditions feed "permitted" is plant-specific data, not kind semantics — the same division decision 41 records for `avail_i`. The sequenced start composes through `sequencer` driving the valve-open-proven-then-pump-start order [7] where the skid carries an actuated valve; simpler skids wire the permissive directly. The recorded loss-of-permissive rule — demand drops to the safe value and restart stays inhibited until permissives return — is exactly `interlock`'s implemented semantics: `out` drives `safe_value` while any condition fails and pass-through resumes the first scan every condition holds again, with no latch; a skid whose owner requires a manual reset declares an `sr-latch` in the permissive wiring rather than changing the kind. Two flow concerns stay separate: `flow-paced-ratio.on_bad_flow` answers *untrusted* flow — the signal cannot be believed — while the interlock's permissive answers *unproven* flow — the process is not running: flow below its declared threshold, the service pump stopped, or the flow switch open [2 §2.2.4.2, 3, 6]. Where the pacing signal has a declared backup flowmeter, `failover-select` feeds `flow` the same way it feeds the station's level path (decision 42). Anti-siphon and overfeed protection is physical design the logic does not replace [1 §5.1.5, 2 §2.2.3, 3] — the contract's part is the no-feed-without-flow permissive above.
- **Alternatives considered:** Declared permissive inputs on `flow-paced-ratio` (couples kind semantics to a plant-specific condition set and duplicates the fail-safe, non-`Good`-cannot-vouch machinery `interlock` already owns — keeping the kind permissive-free leaves it reusable for non-dosing ratio service); aggregating availability inside the kind (rejected with decision 41 — which conditions feed "available" is declared wiring); a dedicated dosing-permissive kind (no named semantics beyond the and/or/latch the existing kinds already carry); latching restart-inhibit as the default (the recorded rule is inhibit-*until*-permissives-return — auto-reset — where a latched lockout is an owner option the `sr-latch` wiring covers, assumption-marked like the station's, decision 41).
- **Consequences:** The permissive chain is engineered model data — declared, checkable, and per-skid — needing no contract work. The untrusted-versus-unproven flow split is explicit: the ratio kind owns the declared bad-signal response, the interlock owns the safe-value gate. A latched inhibit is a declared wiring choice, not a kind variant.
- **Affected tickets:** #240 (this record); the M10 dosing-composition fixture ticket carries the permissive wiring (to be filed); the scripted acceptance-slice ticket exercises permissive loss and return (to be filed); requirement `WW-CTL-003`; relies on #38, #52, #134, #211 and decisions 2, 5, 14, 41, 42; evidence `docs/research/chemical-dosing.md` (#232) sources [1, 2, 3, 6, 7].

### 52. Metering-pump actuation

- **Status:** Accepted — decided: the motor-plus-analog-output composition stands; no `metering-pump` kind. Implemented basis: `motor` echoes the run command to the field and flags `fault` after `fault_ticks` of disagreeing feedback — fail-to-start and fail-to-stop in one mechanism (#52); `analog-output` carries the Float speed demand and `digital-input`/`digital-output` the status and command bits (#11); `manual-station` gives bumpless operator takeover of the demand and `override-select` an operator-driven source switch (#110); `pump-group` manages a duty/standby pair where feed is essential (#210, decision 41); and decision 35's state file plus the redundant pair cover the remote-restart-after-power-loss expectation [8, 14].
- **Problem:** The note's gap 4: the hardwired-interface evidence is run/stop/reset Booleans plus an analog dose-rate setpoint plus running, fault, isolator, and local/remote-selected indications [8 §6.6.3]; the 4–20 mA speed command is the dominant actuation [9, 10, 12–15]; run-only on/off skids exist beside it [8]; and two-axis command — speed plus stroke-servo adjustment — exists where a servo is fitted [8, 13, 16]. Whether the library models a metering-pump contract (run Bool plus speed-demand Float plus fault in) or stays at the `motor` + `analog-output` composition must be recorded against that evidence, with stroke length's place fixed.
- **Chosen approach (decided):** Stay at the composition: `motor` — run command out, run feedback in, the proven `fault` flag — plus `analog-output` carrying the ratio kind's `demand` through the decision-51 permissive interlock. The hardwired contract [8] is exactly run command plus analog setpoint plus status bits, each half already exists with its diagnostics, and no per-scan policy couples the halves that a kind would own. Stroke length is recorded as an engineering/operator setting, never a control output: sizing guidance wants the pump mid-range on both axes [3, 5], stroke-servo adjustment is equipment configuration where fitted [8, 13], and the manual practice pairs operator-set speed with operator-set stroke at the pump [10] — the model represents a remote stroke setpoint, where the skid has one, as an ordinary field point, not a component output. A run-only skid simply omits the `analog-output`: the demand path then ends at the operator's fixed rate through `manual-station`. The pump's own proven-fault path — no-discharge detection, diaphragm or drive fault [13, 16] — is a declared field `Bool` input feeding the decision-55 alarm set and the availability wiring beside `motor`'s computed `fault`, because pump stroke alone does not prove delivery [13]. Duty/standby dosing pairs [1 §5.1.1, 7, 9] compose through `pump-group`, the fault and availability signals landing on its `fault_i`/`avail_i` inputs exactly as the station records. Manual and local takeover stays `manual-station` on the demand plus the equipment's local/remote-selected field indication [8, 15], matching the station pattern (decision 42); remote-manual-versus-pump-local scope and which permissives persist in manual [1 §5.1.2(a), 6] are the note's recorded customer-validation assumption. The per-pump actuation contract — speed-only, speed-plus-stroke-servo, or run-only — and whether pulse-per-stroke contact pacing must be an input form [12, 13] are likewise recorded assumptions.
- **Alternatives considered:** A `metering-pump` kind bundling run, speed, and fault (the kind would be a wiring bundle — no per-scan policy joins the halves — and would freeze one actuation shape where the evidence shows three; the builder composition already assembles the bundle per skid); extending `motor` with an analog demand port (couples discrete actuation to analog pacing, and `motor` stays reusable alone for non-pump loads — decision 41's reasoning); stroke length as a component output (the hardwired interface does not give the controller that authority in the common case — an output would promise control the contract cannot honor [8, 10]); pulse-per-stroke pacing as a second demand form on the ratio kind (the contact-meter form [12, 13] is equipment-side pacing — where required it lands upstream of `flow`, and its necessity is assumption-marked).
- **Consequences:** Actuation needs no contract work: every form the evidence shows — speed-demand, run-only, two-axis — is expressible by including or omitting composition parts, and the demand path ends at a typed field point like every other output. The assumption-marked choices stay open for the customer answer; a `metering-pump` kind remains available to a later decision if a real skid couples the halves with policy wiring cannot express.
- **Affected tickets:** #240 (this record); the M10 dosing-composition fixture ticket wires the `motor` + `analog-output` actuation (to be filed); the scripted acceptance-slice ticket exercises the proven-fault, takeover, and standby-handover paths (to be filed); requirement `WW-CTL-003`; relies on #52, #11, #110, #210 and decisions 5, 35, 41, 42; evidence `docs/research/chemical-dosing.md` (#232) sources [8–16].

### 53. Dose confirmation and consumption accounting

- **Status:** Accepted — totalization composes on implemented kinds; the expected-versus-measured check is a proposed small kind; the M10 tickets (to be filed) land both. Implemented basis: `totalizer` integrates a Float `rate` with `reset`, `rollover`, and frozen-on-bad-quality semantics (#111); `counter` accumulates Bool edges — the pump stroke count (#85); `alarm-monitor`, `latching-alarm`, and `bool-latching-alarm` cover threshold and latched alarming (#22, #109, #219); per-point history and the journal supply the trend a tank-drawdown cross-check reads (#35, decision 17).
- **Problem:** Owner specs require measuring the quantities of chemical used [1 §5.1.2(e)] and confirm dosing against delivered water — the master meter read daily [3], the storage-tank level trend monitored [7], the calibration standpipe timing a measured drawdown [2 §2.2.6]. The note's gap 3: expected-versus-measured verification compares the commanded demand integrated over time against a measured consumption — tank drawdown, discharge flow, or stroke count — and `totalizer` + `alarm-monitor` can compare totals only against static bounds. The decision: whether verification needs a small deviation kind or composes, and what totalization alone must deliver where no measured-consumption signal exists.
- **Chosen approach (proposed):** Two layers, recorded separately.
  *Commanded totalization* is unconditional: a `totalizer` integrates the ratio kind's `demand` — the commanded chemical rate — satisfying the measure-quantities-used requirement [1 §5.1.2(e)] with its semantics recorded honestly: the commanded total proves what was requested, not what was delivered — a ruptured diaphragm still strokes [13]. Where the pump reports a stroke pulse, a `counter` accumulates it beside the total [14, 16].
  *Expected-versus-measured verification* is a small new kind where a measured-consumption signal exists — proposed as `deviation-monitor`: `expected`/`measured` (`In`, `Float` — the commanded and measured chemical rates or totals), `deviation` (`Out`, `Float` — the current relative deviation), `deviating` (`Out`, `Bool` — the dose-not-confirmed condition decision 55 alarms), with declared `deviation_limit` and `window_ticks` parameters under the decision-41/42 descriptor conventions. The composition alternative cannot express the check: `alarm-monitor` bounds a value against static limits while the expected side here moves with the command — that is the gap. Where the measured signal is a tank drawdown trend rather than a flow, `expected`/`measured` take the two rate signals and the windowed comparison covers the slow-trend case [7]. Where no measured-consumption signal exists, totalization alone delivers the commanded total plus the observable surfaces for the operator cross-check the evidence describes — the tank-level trend in history [3, 7] — and no automated dose-confirmed verdict is claimed; that limit is recorded, not silently absent. Whether the first client requires a measured-consumption signal is the note's recorded assumption. Tank-inventory ownership — day-tank limits, fill interlocks, delivery logging [1 §5.1.11, 7] — is recorded as outside the dosing contract: a tank is its own plant element whose level signals feed the dosing permissives and alarms, pending the customer answer the note marks. `deviation-monitor`'s windowed accumulators are run state under decision 20's checkpoint rule, and the kind ships its `dcs-build` spec under #151's drift guard.
- **Alternatives considered:** `totalizer` + `alarm-monitor` as the whole answer (an absolute bound on a measured total cannot express "measured tracks commanded within a declared bound over a declared window" — the note's gap 3); verification inside `flow-paced-ratio` (couples pacing to a measurement many skids lack — the kind stays honest about what it observes); a per-stroke discharge-verification kind like the vendor flow monitors [13, 16] (equipment-level function the field device owns — the contract consumes its fault contact as a field Bool, it does not re-implement stroke verification); claiming dose-confirmed from commanded integration alone (silences the delivered-versus-requested distinction the diaphragm-rupture evidence makes load-bearing [13]).
- **Consequences:** One more small kind where measured consumption exists; where it does not, the contract delivers commanded totalization plus the telemetry cross-check, and the limit is recorded. The deviation kind follows every kind convention — descriptor, spec under the drift guard, checkpointed accumulators — and feeds the alarm set as an ordinary status source.
- **Affected tickets:** #240 (this record); the M10 `deviation-monitor` kind ticket — the kind plus its `dcs-build` spec under the drift guard (to be filed); the M10 dosing-composition fixture ticket carrying the `totalizer`/`counter` wiring (to be filed); the scripted acceptance-slice ticket (to be filed); requirement `WW-CTL-003`; relies on #111, #85, #22, #109, #219 and decisions 16, 17, 20; evidence `docs/research/chemical-dosing.md` (#232) sources [1, 2, 3, 7, 12, 13, 14, 16].

### 54. Time-profiled (diurnal) dosing

- **Status:** Accepted — deferred out of scope with a recorded revisit condition; no kind now. Implemented basis relevant to the recorded shape: components own no wall clock — all timing is the executor's virtual tick (decision 4); `sequencer` walks a declared tick-timed step table (#134); writable internal points carry operator multiplier and offset values (#30, #49); declared `stale_after_ticks` budgets mark a held external input stale (#196, decision 45).
- **Problem:** Sydney Water's Mode 1 runs a 24-setpoint hourly diurnal profile — separate weekday/weekend tables, an operator multiplier and offset, bounded by min/max pump speed [7 §3.5]. A diurnal mode needs time-of-day, which decision 4's no-wall-clock rule forbids inside components: the choice is a schedule kind fed by an external tick-mapped time input, or an explicit out-of-scope deferral — the note's gap 5.
- **Chosen approach (decided):** Deferred. The revisit condition is concrete: the customer answer to the primary-mode assumption (decision 50) naming diurnal profiling a required mode — or any first-client specification carrying a time-of-day demand — reopens this decision. The recorded shape when it reopens, so the deferral costs no contract churn: a `schedule` kind holds the declared profile table — 24 hourly setpoints per day-kind — and reads an externally supplied tick-mapped time, a field `In` `Int` point carrying e.g. minutes-since-midnight plus a day-kind flag, driven by a device kind whose source is the deployment's clock. The kind emits the profiled dose onto the decision-50 `dose` port's composition point — which is why `dose` is a port — with the operator multiplier and offset as writable internal points and the result bounded by the same declared limits downstream. The external input's freshness composes through a declared `stale_after_ticks` budget, and a non-`Good` time input takes a declared `on_bad_time` response under the decision-41/42 convention. `sequencer` was evaluated as the substrate and rejected for this use: its steps count ticks, so a restart or a missed scan shifts the table's phase — wall-clock alignment is exactly what the tick domain excludes.
- **Alternatives considered:** Building the schedule kind now (no customer names the mode — inventing contract ahead of the validation answer, however well-evidenced [7], is the pattern these decisions exist to prevent); diurnal as a `sequencer` table (phase drift — the mechanism and the requirement disagree about time itself); a wall-clock read inside the kind (violates decision 4 outright); deferral with no revisit condition (a silently dropped specified mode — the recorded condition keeps `WW-CTL-003`'s acceptance honest about what it does not cover).
- **Consequences:** `WW-CTL-003`'s acceptance excludes diurnal profiling until the revisit condition trips, and the requirement entry records the exclusion. The deferral costs nothing structurally — the `dose` port keeps the composition point open and the tick-mapped-time-input pattern needs no change to decision 4 — while the specified mode stays on the record rather than disappearing.
- **Affected tickets:** #240 (this record); the M10 schedule-kind ticket is filed only if the revisit condition trips; requirement `WW-CTL-003`; relies on #134, #196 and decisions 4, 45, 50; evidence `docs/research/chemical-dosing.md` (#232) source [7 §3.5].

### 55. Dosing skid alarm set

- **Status:** Accepted — the lifecycle is implemented; the set's mapping is proposed. Implemented basis: `latching-alarm` (#109) carries the Float two-flag semantics and `bool-latching-alarm` (#219) the Bool sibling — `alarm` the standing condition, `unacknowledged` latched on a fresh assertion until the model-wired writable `ack` point reads true, acks journaled through the ordinary settled receipt, the latch carried in checkpoints (decisions 33, 43); `alarm-monitor` stays the auto-clearing sibling (#22); the alarm pane consumes `alarm`/`unacknowledged` status-role ports (#108); decision 38 holds shelving, suppression, and classes deferred as per-kind extensions.
- **Problem:** The owner-specified skid lists fix the dosing alarm set — pump fault and no-discharge, tank low and empty, loss of the pacing signal, bund flood, external inhibit, dose not confirmed [7, 9, 12–16] — and each must map onto the decision-38/43 two-flag lifecycle as a wired `latching-alarm` or `bool-latching-alarm` instance without reopening the lifecycle, including the note's open question of whether a clamped demand alarms or only flags.
- **Chosen approach (proposed):** The set, each element named to its seam:
  - *Pump fault / no discharge* — `motor`'s computed `fault` plus the pump's own fault and no-discharge field signals [8, 13, 16], each into a `bool-latching-alarm`.
  - *Tank low* and *tank empty* — the two-stage level evidence distinguishes the warn-at-low from the stop-at-empty condition [12, 13]: a Float tank level feeds `latching-alarm` instances at declared limits, or field Bool switch points feed `bool-latching-alarm` instances; the empty condition also feeds the permissive chain's trip wiring (decision 51).
  - *Loss of pacing signal* — the ratio kind's `fallback_active` output (decision 50) into `bool-latching-alarm`; the vendor evidence treats signal loss as a fault-relay condition [13, 15].
  - *Bund flood* and *external inhibit* (wet weather) — field `Bool` points into `bool-latching-alarm` instances [7, 9], simultaneously feeding the permissive trips; whether bund, cabinet, and environmental alarms live in the dosing contract or the station's is the note's recorded assumption.
  - *Dose not confirmed* — `deviation-monitor`'s `deviating` output (decision 53) into `bool-latching-alarm` where a measured-consumption signal exists; absent one, no automated condition exists — the recorded limit.
  - *Clamped demand* — `clamped` is a `Status`-role flag on the faceplate and an ordinary trendable point; whether it additionally alarms is the note's open assumption, and the wiring supports either answer — a `bool-latching-alarm` on the flag if the customer requires the alarm.
  Which alarms latch until acknowledged versus auto-clear is per-alarm model wiring — `latching-alarm`/`bool-latching-alarm` versus `alarm-monitor` — and the required latch set is the note's recorded customer-validation question, assumption-marked.
- **Alternatives considered:** A dosing-specific alarm kind or alarm vocabulary (no new semantics — every element is a standing condition plus a latch the existing two kinds already express, and a skid-specific kind would fork the two-flag language the pane, history, and checkpoints speak); alarming every condition including `clamped` by default (invents alarm load the owner specs do not state — the note records the question as open, so the flag stands and the alarm stays a wiring choice); folding the skid alarms into `flow-paced-ratio` as computed outputs (the conditions' sources are distributed — field points, the motor, the deviation check — and alarm composition is model wiring per decision 43's pattern, not kind internals).
- **Consequences:** The skid alarm set needs no contract work beyond the decision-50/53 outputs it consumes: every alarm speaks the two-flag language the pane, history, checkpoints, and journal already carry, and acknowledgment needs no new command path. The open assumptions — clamp-alarm-versus-flag, the latch set, alarm ownership between the dosing and station contracts — stay flagged for customer validation.
- **Affected tickets:** #240 (this record); the M10 dosing-composition fixture ticket wires the alarm set (to be filed); the scripted acceptance-slice ticket asserts each raised alarm (to be filed); requirements `WW-CTL-003` and `WW-OPS-002`; relies on #109, #219, #22, #108 and decisions 33, 38, 43; evidence `docs/research/chemical-dosing.md` (#232) sources [7, 9, 12–16].

### 56. Backwash-supply arbitration — the bank coordinator

- **Status:** Accepted — the contract below is proposed; the M10 implementation tickets (to be filed) land it. Implemented basis it builds on: `pump-group` (#210, decision 41) is the group-kind precedent — indexed per-member ports discovered from bound names under the `trip_N`/`in_N` convention (`interlock` #38, `bool-gate` #152), group status outputs, descriptor roles, and checkpointed group state — though its stage-all-demanded semantics are the opposite of mutual exclusion; `bool-gate`/`sr-latch`/`edge-trigger` compose Bool conditions and edges (#38, #152); writable internal `In` points carry operator values through the receipted, actor-attributed command path (#30, #49, #165, decisions 14, 18, 39); `dcs-build`'s `PlantBuilder` composes multi-kind plants with the per-kind spec table pinned by the spec-drift guard (#112, #151, decision 31); and the reference station's builder-level assembly is the composition precedent (#220).
- **Problem:** The note's gap 3 — the central contract question. The washwater supply serves one filter at a time at the declared rate — the structural norm across the evidence [1 §6.3.a.12, 3, 5, 6, 14] — so contending backwash requests need an exclusive grant, an ordered queue with observable position [8, 9], grant permissives on the shared resources (washwater supply [9, 14], waste capacity [5, 9], the online-flow disturbance bound [8]), and release on completion or abort. Nothing existing expresses this: `pump-group` stages demanded members rather than serializing them; a FIFO-plus-reorder queue has no `bool-gate`/`sr-latch` expression; a `dcs-build` composition could emit the wiring but holds no per-scan run state and no descriptor surface. The queued filter's meanwhile state — keep filtering until granted versus offline with standby cover — varies by owner [8, 9] and must be declared, and operator queue reorder [9] needs a receipted surface where the contract exposes it.
- **Chosen approach (proposed):** A new `backwash-coordinator` kind in `dcs-blocks` plus a `dcs-build` builder-level bank-composition helper — the decision-41 split applied again: arbitration is per-scan stateful policy (an ordered queue and the held grant are run state), so it belongs in an executor component, while the N-filter bank as one typed artifact — one coordinator, N sequences, the shared-supply equipment — is construction-time composition on decision 31's seam, resolving the note's gap 10 alongside gap 3.
  Declared I/O (proposed; per-filter ports indexed `_1`…`_N` under the established convention, the filter count discovered from bound port names): per filter `i`, `request_i` (`In`, `Bool` — the armed backwash request from decision 57's sequence), `grant_i` (`Out`, `Bool` — the exclusive supply grant, at most one asserted at a time), and `position_i` (`Out`, `Int` — the 1-based queue position, `0` while not queued; what the granted filter reports is the implementing ticket's documented convention); bank-level `active` (`Out`, `Int` — which filter holds the grant, `0` while none does), `queued` (`Out`, `Int` — the pending count), and `resource_blocked` (`Out`, `Bool` — a request stands first in queue while a grant permissive fails) — the status outputs decision 61's alarm set and the note's gap-9 journaled bank events consume.
  Grant permissives are declared inputs — `supply_ok`, `waste_ok`, `flow_ok` (`In`, `Bool`: washwater supply available [9, 14], downstream waste capacity [5, 9], the online-flow disturbance bound [8]) — each aggregated upstream through `bool-gate`/`interlock` wiring, because which conditions feed each permissive is plant-specific data, not kind semantics — the `avail_i` division decision 41 records. A non-`Good` permissive reads as not-OK, the fail-safe reading the library already uses. The grant asserts only while every declared permissive holds, so a request can stand armed and first in queue — `resource_blocked` — without the wash starting.
  Parameters (proposed — `Int` codes where `Value` has no enum, the decision-41 convention): `queue_policy` (`Int` — `0` FIFO, the evidenced default [8, 9]; `1` priority-by-trigger, plausible but not evidenced; `2` operator-managed) and `queued_state` (`Int` — `0` keep filtering until granted; `1` offline with standby cover [8, 9]). Both are the note's recorded customer-validation assumptions carried as declared data rather than defaults; `queued_state`'s effect — which equipment actions "offline" and "standby cover" mean — composes in per-plant wiring from `request_i`/`grant_i` (decision 57), while the parameter makes the choice checkable contract data.
  Operator reorder rides the receipted command path: a writable internal `In` point bound to a declared reorder input carries the queue-order change as a receipted, actor-attributed `WriteValue` — the precise encoding is the implementing ticket's documented choice. Whether the first client requires reorder authority at all is the note's recorded assumption; a plant that does not expose it leaves the point unbound.
  Grant lifecycle: the grant holds while the granted filter's `request_i` stands and releases the scan it drops — completion and abort release identically through the sequence's request drop (decision 57), so the arbiter needs no completion vocabulary of its own.
  Checkpoint obligations: the queue contents and order and the held grant are run state captured and restored under decision 20's rule — a promotion mid-queue must not drop or reorder requests (the note's proposal; sources do not address controller redundancy — assumption-marked).
- **Alternatives considered:** A documented wiring convention from `bool-gate`/`sr-latch` (rejected per the note — the queue is ordered run state no Bool fold holds, and a convention gives every bank its own unverifiable arbitration); `dcs-build` builder-level composition emitting the arbitration as model data (rejected — construction-time wiring cannot hold a queue, decision 41's identical reasoning for `pump-group`); mutual exclusion through `interlock` trips per filter (expresses "not both" for a pair but no N-way order, queue position, or permissive-conditioned grant); arbitration left to operator discipline (the grant permissives and one-at-a-time norm are engineered constraints the evidence treats as control behavior, not procedure [3, 8, 9]).
- **Consequences:** One kind carries the exclusive-grant contract and every bank behavior names its seam — permissive aggregation stays declared wiring, reorder rides the existing command path, and the status outputs feed the alarm and journal surfaces with no new contract there. The assumption-marked choices — queue policy, reorder authority, the meanwhile state, the grant-permissive set — stay flagged for customer validation rather than silently defaulting. The `Int`-coded vocabulary is recorded honestly per decisions 41 and 50; the kind ships its `dcs-build` spec under the drift guard, and its checkpoint vocabulary deploys to both peers under decision 11's strictness.
- **Affected tickets:** #253 (this record); the M10 `backwash-coordinator` kind ticket — the kind plus its `dcs-build` spec under the drift guard (to be filed); the M10 filter-bank composition ticket — the builder-level bank helper and the checked-in fixture (to be filed); the scripted acceptance-slice ticket (to be filed); requirement `WW-CTL-004`; relies on #210, #38, #152, #30, #49, #165, #112, #151 and decisions 11, 14, 16, 18, 20, 31, 39, 41; evidence `docs/research/filter-backwash.md` (#233) sources [1, 3, 5, 6, 8, 9, 14].

### 57. The backwash step contract — measured advance and per-step equipment pattern

- **Status:** Accepted — a proposed contract on implemented seams; the M10 tickets (to be filed) land it. Implemented basis: `sequencer` walks a declared tick-timed step table reporting `step`/`done` under `run`/`reset`, tunes per-step `step_<n>_ticks`/`step_<n>_out` through `set_parameter`, and carries an in-flight sequence across checkpoint and promotion (#134, #82, decision 20); `valve` and `motor` carry command-with-feedback and the proven `fault`/`discrepancy` flags (#52); `bool-gate`/`sr-latch`/`interlock` compose Bool patterns and Float gating (#38, #52, #152); `rate-limiter` covers ramped rates and the graduated return-to-service [1 §6.3.a.12, 13, 17] (#85); `manual-station`/`override-select` cover operator takeover of a demand (#110); `totalizer` covers delivered-volume accounting (#111); and variable-arity bound-port discovery is the `in_N`/`trip_N` convention (#38, #152).
- **Problem:** The note's gaps 1, 2, and 7. The canonical backwash is timed cleaning phases bounded by measured-state steps — drain-down ends on level reached [20], wash phases on declared durations [9, 10, 12, 13], filter-to-waste on a turbidity and/or volume bound [4, 12, 15] — while `sequencer` advances on ticks alone (gap 1). Each phase drives a plant-specific combination of valves, pump, and blower while `sequencer.out` is one Float (gap 2) [10, 12, 23]. The sequence must start only when the arbiter grants the shared supply and must release the grant on completion or abort (gap 7). And the phase-internal guards the note names — never air scour together with high-rate wash [15], the drain-down filtration-rate hold [8] — are interlock-like constraints inside the step pattern, not just ordering.
- **Chosen approach (proposed):** A dedicated `backwash-sequence` kind rather than a grown `sequencer` — measured advance, the grant handshake, fault and abort steps, and trigger attribution are sequence-domain contract a general timed table should not carry, and `sequencer` stays the plain substrate simpler sequences keep. The kind walks a declared step table in `sequencer`'s shape — `step_count` plus per-step entries — extended per the gaps:
  *Measured-state advance* (gap 1): each step declares `step_<n>_advance` (`Int` code — `0` timed on `step_<n>_ticks` alone, the implemented `sequencer` rule; `1` measured — advance when the selected measured input crosses `step_<n>_bound`, `step_<n>_ticks` standing as the declared overrun timeout; `2` whichever comes first — measured bound or ticks). Measured steps name their input through `step_<n>_meas` (`Int` — selecting among the kind's indexed `meas_1`…`meas_K` `Float` inputs, discovered from bound names), so drain level, effluent turbidity, and delivered bed volume [4, 12, 20] land as ordinary signals. A measured step overrunning its tick bound takes the declared `step_<n>_on_overrun` behavior (`Int` code — `0` advance anyway; `1` hold and raise `overrun`) — the timeout semantics the note requires but no source fixes, so the code set is proposed and the per-plant choice assumption-marked. `overrun` (`Out`, `Bool`) asserts while a measured step stands past its bound — the surface decision 61's alarm set consumes.
  *Per-step equipment pattern* (gap 2): the kind emits declared per-phase outputs — `phase_<n>` (`Out`, `Bool`, one per declared step, generated from `step_count`) asserting while step `n` is active — beside `step` (`Out`, `Int`) and `out` (`Out`, `Float`) driving `step_<n>_out` for rate-valued phases, as `sequencer` already does. Each valve, pump, and blower command then composes as a `bool-gate` OR over the phase flags that drive it, and each rate demand as `out` gated by its phase flag through `override-select`/`interlock` — plant-specific equipment wiring, never kind vocabulary, the division decisions 41 and 51 record. Phase-internal guards land two ways: single-active-step semantics structurally exclude cross-phase pairs — air scour and high-rate wash declared as distinct steps can never assert together — and a declared `interlock` on the high-rate demand path tripped by the air-scour phase flag guards a decode-table error, the belt-and-suspenders form of the [15] exclusion; the drain-down rate hold [8] composes as a `rate-limiter`-bounded demand wired through the drain-down phase flag.
  *Grant-gated start and release* (gap 7): `request` (`Out`, `Bool`) asserts from the first armed trigger (decision 58) until the sequence completes or aborts, wired to the coordinator's `request_i`; `grant` (`In`, `Bool`), wired from `grant_i`, is the run permissive — the table advances only while granted, so a queued filter sits armed on its first step with `request` standing. `done` (`Out`, `Bool`) asserts at table end and `aborted` (`Out`, `Bool`) on the decision-60 paths; either drops `request`, which is the grant release — no separate release protocol. `active` (`Out`, `Bool`) reports stepping-under-grant, and `request`/`active` supply the meanwhile-state conditions decision 56's `queued_state` selects between: `0` gates the filter's in-service path on `active` — keep filtering until the wash starts — and `1` on `request` — offline once queued, the standby cover composed in bank wiring [8, 9]; the gating itself is an `interlock` trip on the in-service demand, so the declared choice lands as one wiring difference the bank helper emits from the declared parameter. Non-`Good` inputs take the fail-safe reading — a `grant` that cannot be proven holds the table; a `meas_i` that cannot be trusted cannot satisfy its step's bound, so the declared ticks/overrun rule governs — the implementing ticket documents the precise rules.
  Checkpoint obligations: the active step, banked count, armed request and held trigger, and overrun state are run state under decision 20's rule — the 60–75-minute cycle [12] crossing a promotion is the note's recorded `WW-LCM-001` reach, a proposal the sources do not address.
- **Alternatives considered:** Extending `sequencer` with per-step measured advance (rejected — the extension is the smaller half of this contract; grant gating, fault and abort steps, trigger attribution, and `aborted` would still need a backwash-aware home, and loading them onto the general kind splits one contract across two — the note's gap-1 "or" resolves to the dedicated kind once gaps 5–7 land beside it); an Int `step` code decoded to per-phase conditions by external comparators (no Int-equality kind exists — banding `threshold-chain` per step is the only composable decode, an indirection the generated `phase_<n>` outputs remove while keeping the equipment mapping in wiring where the plant-specific data lives); declared per-equipment outputs on the kind (freezes one equipment vocabulary — valve set, pump count, blower — where the evidence shows the pattern is plant-specific [10, 12, 23]); the phase guards as engineering review only (the [15] air-plus-high-rate exclusion is a safety-shaped constraint the contract must make structurally expressible — single-active-step plus the declared interlock give it two independent enforcements).
- **Consequences:** One kind carries the sequence contract — timed and measured advance, the grant handshake, the phase-flag surface, and the `aborted`/`overrun`/`trigger_source` status vocabulary `sequencer` lacks — while the equipment pattern and guards stay declared, checkable wiring. `sequencer` itself is untouched; the measured-advance need is answered by the new kind and non-backwash sequences keep the simpler contract. The `Int`-coded per-step vocabulary (`advance`, `meas`, `on_overrun`) is recorded honestly, and every assumption-marked element — the client's step vocabulary, which steps end measured versus timed, the overrun responses — stays flagged. The `dcs-build` spec obligation and checkpoint coverage follow every kind's convention.
- **Affected tickets:** #253 (this record); the M10 `backwash-sequence` kind ticket — the kind plus its `dcs-build` spec under the drift guard (to be filed); the M10 filter-bank composition ticket carrying the phase-decode and guard wiring (to be filed); the scripted acceptance-slice ticket exercising measured advance, overrun, and the grant handshake (to be filed); requirement `WW-CTL-004`; relies on #134, #52, #38, #152, #85, #110, #111, #82 and decisions 11, 16, 20, 41, 51; evidence `docs/research/filter-backwash.md` (#233) sources [4, 8, 9, 10, 12, 13, 15, 17, 20, 23].

### 58. Backwash triggers, auto-start permission, and attribution

- **Status:** Accepted — a proposed contract on implemented seams. Implemented basis: `timer`/`counter` express elapsed run time and filtered volume (#85); `analog-input` plus `alarm-monitor`/`threshold-chain` express headloss and effluent-turbidity thresholds (#11, #22, #211); `edge-trigger` converts levels to edges (#152); `bool-gate` ORs conditions (#152); `sr-latch` holds an armed condition (#152); writable internal `In` points carry the operator-start request through the receipted, attributed path (#30, #49, #165, decisions 14, 18, 39).
- **Problem:** The declared trigger set — elapsed run time, terminal headloss, effluent turbidity, operator start — is owner-convergent [3, 8, 9, 12, 17, 18, 20, 21], but two things are not universal: whether automatic triggers may *start* a wash is jurisdiction-mandated in both directions ([3] requires turbidity-initiated automatic backwash; [1 §6.3.a.12, 7] require operator initiation), and owners require the firing trigger recorded per backwash [8] — the note's gap 4, since a `bool-gate` OR emits the union, not which input fired. The decision: which seams express the trigger primaries, where auto-start permission is declared, and how attribution lands.
- **Chosen approach (proposed):** The primaries compose upstream — `timer` on the filter's in-service state for run time, `alarm-monitor`/`threshold-chain` flags on headloss and effluent-turbidity analog inputs, a writable internal `In` point for operator start — each landing on its own named input on `backwash-sequence`: `trig_time`, `trig_headloss`, `trig_turbidity`, `trig_operator` (`In`, `Bool`). Separate inputs, not a pre-OR'd condition, because the kind owns attribution: `trigger_source` (`Out`, `Int` — `0` none, `1` time, `2` headloss, `3` turbidity, `4` operator) captures the first asserted source when the request arms and holds it until the backwash's record completes — the journaled status transition plus point history are the per-backwash record [8] requires, no new journal event type (gap 4 resolved as a declared output, not derivable after the fact).
  Auto-start permission is declared data on the kind — `auto_start` (`Int` code — `0` an automatic trigger's armed request asserts `request` directly, the [3]/[8] direction; `1` an automatic trigger arms and latches a pending state only `trig_operator` releases to `request`, the [1 §6.3.a.12, 7] direction) — declared per plant because the evidence mandates both directions; the first client's answer is the note's recorded assumption. `pending` (`Out`, `Bool`) reports the armed-but-held state so the operator sees a request awaiting their start; `trig_operator` under `auto_start = 0` queues a wash unconditionally — the natural reading, the implementing ticket documents it.
- **Alternatives considered:** A pre-OR'd `trigger` input with the set kept in wiring (loses attribution — gap 4's whole point; the kind cannot say which source fired); attribution reconstructed from per-source point history (the record would live outside the contract — the sources require the firing trigger as the sequence's observable record [8], and a derived reconstruction cannot distinguish simultaneous assertions from a race); auto-start permission as plant wiring — an `sr-latch` on the OR'd triggers AND-ed with operator start (expressible, but it buries a regulator-mandated behavior in ad-hoc wiring where the mandate differs by jurisdiction; a declared parameter makes the permission checkable contract data — the same reason `on_bad_flow` is a parameter in decision 50); an indexed `trigger_i` input set with per-plant source naming (the four-source set is the convergent vocabulary — named inputs self-describe in the descriptor where opaque indices would not).
- **Consequences:** The trigger primaries need no contract work; the two genuinely jurisdictional behaviors — auto-start permission and source attribution — land as a declared parameter and a declared output on the sequence kind. The operator-start input is the same writable-point seam every operator command uses, so a manual start is receipted, attributed, and journaled like any command. Which sources a plant declares, the `auto_start` answer, and whether the turbidity tier additionally drives the 1 NTU shutdown path [3] stay assumption-marked.
- **Affected tickets:** #253 (this record); the M10 `backwash-sequence` kind ticket carries the trigger inputs, `auto_start`, and `trigger_source` (to be filed); the M10 filter-bank composition ticket wires the primaries (to be filed); the scripted acceptance-slice ticket asserts each trigger's attribution (to be filed); requirement `WW-CTL-004`; relies on #85, #11, #22, #211, #152, #30, #49, #165 and decisions 14, 18, 39; evidence `docs/research/filter-backwash.md` (#233) sources [1, 3, 7, 8, 9, 12, 17, 18, 20, 21].

### 59. The backwash operator command surface

- **Status:** Accepted — proposed; the carried surface is evidenced, the rest assumption-marked. Implemented basis: writable internal `In` points carry operator commands through the receipted, actor-attributed `WriteValue` path with named rejections (#30, #49, #165, decisions 7, 14, 18, 39); `set_parameter` tunes descriptor-declared parameters at the scan boundary (#82, decision 20) — the per-phase rate/duration tuning the owner evidence requires [7, 10, 15]; `manual-station`/`override-select` cover operator takeover of analog demands and equipment source selection (#110).
- **Problem:** The note's gap 5 — which of start, abort-to-a-declared-safe-step, hold, advance, and manual-step the contract carries. The owner evidence is consistent on start [3, 7, 9, 12, 13, 21] and abort [10], plus per-phase rate/duration tuning and valves-to-auto/manual requests [7, 10, 15]; hold/advance/manual-step appear only in integrator practice [23], not owner documents — so the surface must be recorded with the evidenced half carried and the unevidenced half honestly open.
- **Chosen approach (proposed):** The contract carries *start* and *abort*; *hold*, *advance*, and *manual-step* are recorded open customer-validation questions with their seam named, not silently omitted.
  - *Start* — the `trig_operator` input (decision 58) bound to a writable internal `In` point: the receipted write arms or releases the request per `auto_start`, attributed and journaled [3, 7, 9, 12, 13, 21].
  - *Abort* — a declared `abort` (`In`, `Bool`) input bound to a writable internal `In` point; asserting it drives the sequence to its declared `abort_step` and raises `aborted` — the Tulsa object's `BW_Abort_CMD`/`BW_Aborted` pair [10] — receipted and attributed like every operator action.
  - *Queue reorder* — the coordinator's reorder input (decision 56), a writable internal point where the plant exposes reorder authority — itself an assumption-marked choice.
  - *Rate and duration tuning* — the per-step `step_<n>_ticks`/`step_<n>_out` parameters tune through `set_parameter` under descriptor-declared ranges, the implemented convention `sequencer` already follows [7, 10, 15]; equipment-level override — valves to manual, bank auto/manual requests [10] — composes per equipment through `manual-station`/`override-select` and the `valve`/`motor` wiring the station pattern uses (decisions 41–42).
  - *Hold / advance / manual-step* — recorded open assumptions: [23] is integrator practice, not owner evidence. If the customer answers yes, the declared shape lands as further `In` inputs (`hold`, `advance`, `step_select`) on the same receipted-point convention — no contract or transport work — and this decision reopens; the implementing ticket does not build them on spec.
- **Alternatives considered:** Carrying the full integrator surface now (invents operator authority no owner source requires — decision 54's identical reasoning for diurnal dosing); abort as a `reset` equivalent (a bare reset abandons the bed mid-state — the evidence's abort drives to a declared safe step with an `aborted` indication [10], which `sequencer.reset` cannot express); a dedicated command variant beside `WriteValue`/`set_parameter` (rejected with decisions 7 and 33 — point writes on declared inputs are the established journaled surface, and a new variant adds vocabulary without semantics); the operator surface through a `run`/`reset` idiom alone (holds and restarts exist but express neither abort-to-step nor the attributed start the receipts must carry).
- **Consequences:** The carried surface is exactly the owner-evidenced vocabulary — start, abort, reorder where declared, tuning — all through the receipted path under existing conventions, so backwash adds no command machinery. The unevidenced commands stay visible as assumptions with a named landing seam, so a customer "yes" is an additive input set, not a contract change.
- **Affected tickets:** #253 (this record); the M10 `backwash-sequence` kind ticket carries `abort` (to be filed); the M10 `backwash-coordinator` ticket carries the reorder input (to be filed); the filter-bank composition ticket binds the writable points (to be filed); the scripted acceptance-slice ticket exercises start, abort, reorder, and tuning with their receipts (to be filed); requirement `WW-CTL-004`; relies on #30, #49, #165, #82, #110 and decisions 7, 14, 18, 20, 39, 54; evidence `docs/research/filter-backwash.md` (#233) sources [3, 7, 9, 10, 12, 13, 15, 21, 23].

### 60. Mid-sequence failure — the declared fault step and abort semantics

- **Status:** Accepted — proposed contract on implemented seams. Implemented basis: `valve`/`motor` prove equipment failure — the `fault`/`discrepancy` flags after declared disagreeing scans (#52); `sequencer`'s hold-on-non-`Good`-input rule is the fail-safe reading convention (#134); the coordinator releases the grant when `request` drops (decision 56); `bool-latching-alarm` carries the fault and aborted alarms (#219, decision 43).
- **Problem:** The note's gap 6 — a proven equipment fault mid-sequence, a documented hazard [22], needs a declared `on_fault` step and a declared resume-versus-abort policy, and `aborted` must stand distinct from `done` per the Tulsa vocabulary [10]. `sequencer` has no fault input and no fault step. The sources publish no uniform rule — which step a failed valve or blower returns the filter to, whether the sequence resumes after repair or aborts and re-queues, whether the aborted filter returns to service — so these are contract parameters the customer answers, not defaults.
- **Chosen approach (proposed):** `backwash-sequence` takes `fault` (`In`, `Bool` — the per-filter equipment-fault aggregate, composed upstream from the participating `valve`/`motor` `fault`/`discrepancy` flags through `bool-gate` OR; where per-phase fault relevance is declared, the aggregation composes from the phase flags) plus declared parameters `on_fault_step` (`Int` — the step a proven fault drives the sequence to: a hold, a drain-to-safe step, or an abort step — inside the safe directions the evidence implies: bed covered, never air plus high-rate water, the filter held out of service, the failure alarmed [10, 11, 15]) and `on_fault_policy` (`Int` code — `0` hold at the fault step pending operator action, resume landing on the receipted start/abort vocabulary; `1` abort: drive `abort_step`, assert `aborted`, drop `request` so the grant releases and the filter may re-queue on a fresh trigger). `aborted` (`Out`, `Bool`) is a distinct status from `done` [10] — a completed wash and a faulted one are different events the alarm set and journal record differently. Whether a held fault step keeps the grant or releases it immediately is the implementing ticket's documented detail under the declared policy; whether the aborted filter returns to service without a completed wash is the note's recorded assumption.
- **Alternatives considered:** Fault handling left to per-plant wiring (the failed-equipment → safe-step transition is per-scan sequence state — wiring can gate inputs but cannot reposition a step table, and leaving it out makes every bank's worst case a bespoke answer); a fixed abort-always policy (the evidence shows owners require override and safe containment but leave resume-versus-abort to the plant's control narrative — a fixed choice silently decides what must be declared); resume-in-place as the default (assumes repair semantics no source states); the operator abort as the fault path (a proven fault must reach the safe step without waiting on an operator [22]).
- **Consequences:** The fault path is declared data — which step, which policy — so the acceptance scenario can assert it and the customer answers it; `aborted`/`done`/`active`/`overrun`/`pending` give the bank the status vocabulary the alarm set (decision 61) and the journaled events consume. The grant interaction is pinned: abort drops `request` and releases the supply for the queue; the fault-step-holding-grant sub-case is recorded as the ticket's documented choice rather than silently settled.
- **Affected tickets:** #253 (this record); the M10 `backwash-sequence` kind ticket carries `fault`, `on_fault_step`, `on_fault_policy`, `abort_step`, and `aborted` (to be filed); the filter-bank composition ticket wires the fault aggregate (to be filed); the scripted acceptance-slice ticket drives a mid-sequence fault and asserts the declared step, `aborted`, and the grant release (to be filed); requirement `WW-CTL-004`; relies on #52, #134, #219 and decisions 20, 43; evidence `docs/research/filter-backwash.md` (#233) sources [10, 11, 15, 22].

### 61. Post-wash verification and the filter-bank alarm set

- **Status:** Accepted — the lifecycle is implemented; the checks' seam and the set's mapping are proposed. Implemented basis: `latching-alarm` (#109) and `bool-latching-alarm` (#219) carry the two-flag lifecycle — `alarm` standing, `unacknowledged` latched until the model-wired writable `ack` point reads true, acks journaled through the ordinary settled receipt, the latch carried in checkpoints (decisions 33, 38, 43); `alarm-monitor` thresholds Floats (#22); the journal and point history record transitions and settled commands (#35, #163, decisions 17, 36); the phase flags and status outputs of decisions 56–57 supply the conditions.
- **Problem:** The note's gaps 8 and 9. The post-wash checks are phase-conditioned comparisons `alarm-monitor` cannot express ungated: clean-bed headloss within a declared deviation of its captured baseline at a given flow [8], and ripening turbidity under declared bounds within declared durations of return to service [4, 8, 15]. And the declared bank alarm set — the effluent-turbidity tiers [1 §6.3.a.11, 3, 6], terminal headloss [8, 12, 18], trigger-attributed start events [8], aborted/failed backwash [10], the resource-blocked queue, online-flow disturbance [8], CBHL and ripening excursions [8, 15], the per-class equipment minimums [11] — must map onto the two-flag lifecycle; gap 9 records that no new alarm contract is needed once the coordinator and sequence expose their status outputs.
- **Chosen approach (proposed):** The phase-conditioned checks land as one small kind — `phase-monitor`: `in` (`In`, `Float` — the measured value), `phase` (`In`, `Bool` — the condition window, wired from the decoded phase flags or the return-to-service state), `capture` (`In`, `Bool` — while asserted the kind holds `in` as the baseline, the CBHL "original at a given flow" capture [8]); `deviation` (`Out`, `Float`), `exceeded` (`Out`, `Bool` — the excursion condition the alarm set consumes), `overdue` (`Out`, `Bool` — a bound not met within `limit_ticks` of the phase opening). Parameters `bound` (`Float` — the absolute bound or maximum deviation), `limit_ticks` (`Int` — the duration bound), and `mode` (`Int` code — `0` absolute bound on `in`; `1` deviation from the captured baseline) under the decision-41/42 descriptor conventions. The composition alternative cannot express it: `alarm-monitor` bounds against static limits while this check needs a phase gate, a value capture, and a deadline — `interlock`-gating the measured value to a benign `safe_value` outside the phase works for gating alone but buries the check's semantics in wiring and still cannot express "below bound within duration" or a held baseline. Ripening composes as `phase` wired to the post-wash condition with `mode 0`, the declared NTU bound, and `limit_ticks` the 30-minute-cap class deadline [4, 8, 15]; CBHL as `capture` on the reference-flow step then `mode 1` deviation monitoring [8]. `phase-monitor`'s captured baseline and timers are run state under decision 20's rule; the kind ships its `dcs-build` spec under the drift guard.
  The alarm set, each element named to its seam:
  - *Effluent turbidity tiers* — the 0.3 NTU per-filter alarm and the 1 NTU shutdown tier [1 §6.3.a.11, 3, 6]: `latching-alarm` instances on the turbidity Float; whether the 1 NTU tier is alarm-only or additionally drives the filter offline is jurisdiction-dependent [3] — assumption-marked, and where it drives, the tier also feeds the sequence's `fault` wiring.
  - *Terminal headloss* — `latching-alarm` on the headloss Float [8, 12, 18], its flag available to the trigger wiring (decision 58).
  - *Trigger-attributed start and backwash status events* — the sequence's `trigger_source`, `active`, `done`, and `aborted` transitions journal through the existing recorder — point history plus the transition journal, no new event types (gap 9); `aborted` additionally feeds `bool-latching-alarm` where the plant alarms it [10].
  - *Resource-blocked queue* — the coordinator's `resource_blocked` into `bool-latching-alarm`.
  - *Online-flow disturbance* — `latching-alarm` on the bank flow-disturbance measurement against the declared bound [8], the same signal the `flow_ok` grant permissive consumes.
  - *CBHL and ripening excursions* — `phase-monitor`'s `exceeded`/`overdue` into `bool-latching-alarm` [8, 15]; the monthly-aggregate target [8] is a history/journal-derived reporting obligation, not a trip alarm — recorded rather than silently absent.
  - *Equipment faults* — the `valve`/`motor` `fault`/`discrepancy` flags per actuator, blower (a `motor` instance), pump, and valve [11] into `bool-latching-alarm`, simultaneously feeding the sequence's `fault` aggregate.
  - *Record-keeping obligations* — documented consistent use of alternate ripening techniques [4] and the no-unsettled-recycle rule [3] ride the journal and history, not the alarm path.
  Which alarms latch until acknowledged versus auto-clear is per-alarm model wiring — `latching-alarm`/`bool-latching-alarm` versus `alarm-monitor` — and the required latch set is the note's recorded customer-validation question, assumption-marked.
- **Alternatives considered:** `alarm-monitor` instances gated by `interlock` safe-values (expresses phase-gating only as a wiring trick — no capture, no deadline, and the check's meaning disappears into parameter values: the unverifiable-composition failure these kind decisions exist to avoid); two separate kinds for CBHL and ripening (one shape covers both — a bounded comparison with an optional captured baseline and an optional deadline; splitting duplicates the contract); a dedicated bank-alarm kind or vocabulary (rejected with decisions 43 and 55 — every element is a standing condition plus a latch the existing kinds already speak); bank events as new journal event types (rejected with decision 33 — status-output transitions already journal).
- **Consequences:** One small kind plus wired instances carry the whole verification-and-alarm surface; the two-flag lifecycle is untouched and acknowledgment needs no new path — gap 9's prediction confirmed. The assumption-marked items — the latch set, the 1 NTU tier's alarm-versus-trip scope, the monthly-aggregate reporting form — stay flagged for customer validation.
- **Affected tickets:** #253 (this record); the M10 `phase-monitor` kind ticket — the kind plus its `dcs-build` spec under the drift guard (to be filed); the M10 filter-bank composition ticket wiring the alarm set (to be filed); the scripted acceptance-slice ticket asserting each raised alarm and the journaled events (to be filed); requirements `WW-CTL-004` and `WW-OPS-002`; relies on #109, #219, #22, #35, #163 and decisions 17, 20, 33, 36, 38, 43; evidence `docs/research/filter-backwash.md` (#233) sources [1, 3, 4, 6, 8, 10, 11, 12, 15, 18].
